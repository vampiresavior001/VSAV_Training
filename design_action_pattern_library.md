# Action Pattern Library — 設計メモ (2026-09-17)

作りかけの実装ではなく、**これから作るものの仕様**。中断しても、この 1 枚で
再開できることを目的にする。

---

## 何を作るのか

ダミーの Action Steps (ステップ列) を**名前を付けて複数保存し、選んで使う**機能。

動機は 3 つ。

- ステップを作るのは手間なので、**作ったものを残したい**
- 残したものを**便利に呼び出したい**
- **人対策**をしたい (「誰々さん対策」を何本も持ちたい)

---

## 決まっていること

| 項目 | 決定 | 理由 |
|---|---|---|
| ライブラリの単位 | **キャラ別** | 今の Action Steps と同じ。他キャラのステップは技が解決できない |
| 対象の状況 | **当面 Reversal のみ** | いずれ起き上がり / ガード / ヒット / 空中復帰を複数選ぶ形にする。だから Guard と Reversal を別機能に分ける必要はない |
| 保存できる数 | **多数** | 人対策が目的なら数十本は要る |
| ランダムの範囲 | **選んだものから** | ライブラリ全体からではない |
| 作成の入口 | **New (空から) と、Action Steps からの取り込み** の両方 | 取り込みは手抜きの近道 |
| 名前の重複 | **許可** | 内部 id で識別し、選択は行位置。同名でも実害なし |
| 文字 | **ASCII のみ** | `gui.text` が日本語を描けない (実測) |

### 既存の Action Steps は一切変えない

今の使い方をしている限り、**深さも操作も表示も変わらない**。Action Pattern
Library は**別の親**として足す。そこへ入るのは利用者が選んだ結果なので、
1 階層深くても納得性がある (本人の言葉)。

---

## 画面

### 一覧

```
REVERSAL ACTION PATTERNS: Sasquatch
>  [x] 01  anti-Bishamon rushdown                >
   [ ] 02  vs Kai  meaty setup                   >
   [x] 03  wakeup DP bait                        >

   New                                           >
   Add from current Steps                        >
   Back

LP: Open   MP: Use in Random (近道)   Left: Back
```

- `[x]` は「使う」。**チェックの数がそのままモードになる**
  - 1 つだけ → 毎回それが出る (Single)
  - 複数 → その中からランダム (Random)
  - **`Play Mode` の行は作らない。** 覚えることが 1 つ減る
  - 代償: 「印は残したまま今は 1 本だけ」ができない。外して戻す操作が要る
- `New` は空から作って、そのまま `Edit` へ
- `Add from current Steps` は今の Action Steps を丸ごと取り込む

### 項目の操作

```
REVERSAL ACTION PATTERNS: Sasquatch  >  01
>  Edit                    ← 既存の Action Steps エディタを開く
   Use in Random : Yes     ← LP / 右で切り替え
   Rename
   Copy
   Move
   Delete
   Back
```

`Edit` は**既存のエディタをそのまま開く**。保存するとこの項目に書き戻る。

---

## 操作の原則 — レバー + LP だけで全機能に届く

MP / HP は**近道に過ぎない**。それでしか届かない機能を作らない。

歩いて確かめた経路:

| 操作 | 到達先 |
|---|---|
| 下 ×n | 目的の項目行 |
| LP または 右 | その項目の子画面 |
| 下 | `Use in Random` |
| LP または 右 | 切り替わる |
| 上 ×n → 左 | 一覧へ戻る |
| 左 | エディタを出る |

**2 択の切り替えは「LP / 右」にする。左右にしない。** 左を値の操作に使うと
その行から戻れなくなる。**左は常に「戻る」に空けておく。**

移植版はこれを破っていた (ランダムが MP 専用、オプションが HP 専用)。

---

## 幅の制約 (実測済み)

パネルは **360px**、文字幅 **4px**、行の左端 **33px**。よって **1 行 81 字**。

| 行 | 字数 | 右端 |
|---|---|---|
| 見出し (最長キャラ Dark Gallon) | 37 | 181px |
| 見出し + 項目 + 下層 | 56 | 257px |
| 一覧の行 (名前 60 字) | 75 | 333px |
| 子画面の最長行 | 22 | 121px |
| 既存エディタの最長見出し | 69 | 309px |

**名前に使えるのは 66 字まで** (`>  [x] 256  ` の 12 字 + `  >` の 3 字を引いた残り)。

### 注意 — パンくずを継ぎ足すと溢れる

```
REVERSAL ACTION PATTERNS: Dark Gallon > 256 > STEP 16 > Action > Super Jump
= 105 字 → 453px   ハミ出し
```

ライブラリから `Edit` で既存エディタを開くときは、**パンくずを継ぎ足さず、
エディタ自身の見出しに切り替える**。編集中は「ステップ列を編集している」ので
意味的にも自然で、表示幅は今のまま変わらない。

---

## 決まっていないこと

- **名前の入力手段**。別ウィンドウ (PowerShell) は実証済みだが、開いている間
  エミュレータが完全に停止する (実測 6.6〜39 秒)。`[[file-dialog-via-powershell]]`
  参照。画面内の文字ホイールと、どちらにするか未定
- **いずれ足す `Triggers`** (起き上がり / ガード / ヒット / 空中復帰) を、
  項目ごとに持たせるか、組ごとに持たせるか
- 保存データの形式。今の `action_sequences` をどう拡張するか

---

## 作る順序

移植版で「動かしたら根本が動いていなかった」ので、**順序を逆にする。**

1. **行の並びを描いて幅を実測するテスト** (`test_pattern_library_rows.lua`)
2. **レバー + LP だけで全機能に歩けることをテストで固定**
3. そのうえで中身 (保存・読み込み・ランダム) を実装

**1 と 2 は実機不要。** 通ってから実機に出す。

---

## 参考情報の在処

外部実装 (ap3 パッケージ) の移植版は**参考情報に過ぎない**。そのまま直して使う
方針ではない。

- ブランチ `action-patterns` / タグ `v11.8.0-wip-action-patterns`
- 元パッケージ:
  `C:\Users\masam\デスクトップ\Claude\その2\VSAV_Training_v11.6.0-reviewfix3-ActionPatterns-ap3.zip`
  (土台は v11.5.2)

### 移植版で見つけた先方のバグ 4 件

いずれもエディタを一度動かせば分かる類。先方も「動作未確認」と明記していた。

1. `patterns` / `pattern_options` / `delete_pattern` の活性化処理が `build()` の
   中にあり、冒頭の `return` で**到達不能**。パターン行を押しても何も起きない
2. 旧 ID (`n.MP` など) の移行が一時コピーにしか当たらず、パターンを開くたびに
   生の値へ戻る
3. `dirty()` の比較元を移行の**前**に撮っており、開いた瞬間から「変更あり」
4. `Loop Patterns` が下書きを更新せず、ラベルが嘘をつき保存で巻き戻る

### ブランチ後半 5 コミットは前提が誤っている

walker をフレームコールバックへ載せ替えた件と、`0xFF8081` の差分で経過ティックを
数える件は、「exec フックが届かない」という**誤診**に基づく。実際はフックは
生きていた (300 フレームで 337 回)。**設計し直すならこの部分は捨てる。**

原因は、`memory.registerexec` のコールバックの**中から `io.open` で書けない**
こと。エラーにもならず消えるので、ログが 0 行なのを「コードが走っていない」と
読んでしまった。**フックの中は数えるだけにして、報告はフレーム側から書く。**

---

## 利用者のデータについて

設定に **v2 形式のデータが 1 件残っている** (サスカッチ、cid 10)。master は
v11.7.2 系なのでこの版では見えないが、**消えてはいない**。移行前のバックアップ
`scripts/training_settings_pre_action_patterns.json` も残っている。

新設計で読めるようにするか、旧形式へ戻すかは未定。
