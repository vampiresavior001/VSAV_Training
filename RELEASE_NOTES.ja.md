# VSAV Training Mode — リリースノート

English: [RELEASE_NOTES.md](RELEASE_NOTES.md)

新しい版が上です。古い版もそのまま残しています。

---

## v11.7.9

### PB Count が、狙った通りに遅らせプッシュブロックができたかを見せるように

Push Block の上手い押しは、**ガードしてから少し遅れて 1 ティック 1 ボタンを
ずらし押しすること**です。同じティックに 2 ボタン押しても ROM の addq は
1 回しか走らないので、1 カウントしか買えません。ずらせば 2 回分。

PB Count の右に、**最後の窓のティックタイムライン**が出るようになりました:

```
PB Count: 6   Guard|----1-1-1-1-1-1|Expired   at:9-14t   MultiPush: 0
```

- **Guard** — ブロックが当たり、受付が始まったティック
- **数字** — そのティックで押したボタンの数(**1 = 単押し・白、2〜6 = 同時押し・赤**)
- **-** — 押しなしのティック
- **Expired** — 受付終了(14 ティック経過)
- **at:9-14t** — 押しの頭と終わりのティック位置。理想は `a:9-14t` で PB Count: 6(窓の終盤 6 ティックに 6 押しを完璧にずらせた状態)
- **MultiPush** — 同じティックに 2 つ以上押した回数(**赤 = ミス**)。0 が理想

**ダミーを Push Block にしたとき、P2 の MultiPush は常に 0 であるべきです。**
数値が出たらこのツールの配送バグを示します。

### タブの名前を Dummy に

Player タブの中身は、Position も Pose も Guard も Action Steps も
すべてダミーの挙動です。名前を **Dummy** にしました。

---## v11.7.8

### Position の行に再配置の近道

Position の行は、値が変わったときだけ配置を適用します。だから
**同じ位置に戻すには、別の項目を挟んで 2 回変えるしかありませんでした。**

Position の行にカーソルを置いたまま:

- **LP** — その位置に**再配置**します (値は変えません)。練習中に
  位置がずれたとき、1 押しで戻せます
- **HP** — 再配置して、そのまま**メニューを閉じます**
- **MP** — Off へのリセット (従来どおり)
- **Off** を選んでいるとき、LP と HP は何もしません

### タブの名前を Dummy に

Player タブの中身は、Position も Pose も Guard も Action Steps も
**すべてダミーの挙動**です。名前を **Dummy** にしました。

---

## v11.7.7

### Action Pattern の単体書き出し

ライブラリ全体でなく、**1 つのパターンだけ**をファイルに書き出せるように
なりました。項目画面（Edit のある画面）の **`Export this Pattern`** から
行えます。

- 保存ダイアログの提案名は `vsav_action_pattern(<キャラ>-<パターン名>).json`
  （名前に使えない文字は自動で落ちます）
- ファイル形式はライブラリ全体の書き出しと同じなので、**Import from a File
  はそのまま受け付けます**（追加型、届いたものは未チェック）
- 一覧の `Export to a File`（ライブラリ全体）は今までどおり

### Export/Import が前回のフォルダを覚える

ファイルの窓が、**前回 OK したフォルダ**から開くようになりました。

- 書き出しと読み込みで共通の 1 つ。キャンセルでは動きません
  （「見に行っただけでは変わらない」Windows 標準の挙動）
- フォルダが削除されていたときは、窓が自分で既定の位置に落ちます
- 場所は `training_settings.json` に保存されます（配布物には含まれません）

### Import はキャラ違いを拒む

書き出したファイルには「誰のパターンか」が入っています。違うキャラの
ライブラリで開くと、そのままでは走らないのに足っていました。
**キャラ違いのファイルは 1 本も足さずに拒みます**:

\ERROR: that file is for Morrigan, not Sasquatch. Nothing was added.
\
character フィールドが無い古い手作りファイルは、これまでどおり通します。

### 読み込みの失敗が理由を出すように

インポートで読めなかったとき、これまでは「that is not a pattern file」だけが
出て、**ファイルが開けなかったのか、中身が壊れていたのかが分かりません**
でした。理由を出すように直しました:

```
ERROR: could not read the chosen file (<理由>)
```

間違ったファイルを選んだときも、その場で分かります。

---

## v11.7.6

### Action Pattern Library — Action Steps に名前を付けて保存し、選んで使う

ダミーの Action Steps (ステップ列) を**名前を付けて複数保存し、選んで使える**
ようになりました。ライブラリは**キャラ別**で、対象は Reversal です。

`Trainer` -> `Guard Action Type` に **`Reversal - Action Patterns`** が付きます。

- 一覧の `[x]` が付いたパターンが走ります
- **チェックの数がそのまま再生モードになります。** 1 つなら毎回それ、
  複数なら**その中から周ごとにランダム**。サスカッチの
  「ショートダッシュ / ロングダッシュ / 下段」を 3 本チェックして
  ループ再生する、という使い方ができます
- 一覧では **MP** でチェックの付け替え。New / 複製 / 並び替え / 削除も
  行えます
- **New** で空から作るほか、**Add from current Steps** で今の Action Steps を
  まるごと取り込めます。編集は既存の Action Steps エディタそのまま
  (Edit で開きます)
- **Export to a File / Import from a File** で JSON の 1 ファイルに書き出し・
  読み込みができます。**インポートは追加で、既存は置き換えません。**
  練習内容をそのまま人に渡せます

名前の入力だけは Windows の入力窓 (PowerShell) を使い、窓が開いているあいだ
エミュレータは一時的に止まります。

### 裏回りの最中のダッシュが逆走しないように

キャラが交錯して向きが変わる最中のダッシュで、**2 タップが画面上で逆向きと
して出る**ことがありました。左右の補正は 2 系統あって読むバイトが違うため
です (ROM で確認)。生の方向だけでできている列 (ダッシュ・歩き) を向きの
バイトで解決し、振り向くあいだはダッシュのタップを待つようにしました。
同じ修正で、メニューを開いているあいだもダミーの入力が届き続ける穴を
塞ぎました。

### 起き上がりのダッシュが、裏回りのダウンでも出るように

Guard Action のリバーサルでダッシュを設定しているとき、**ダウン中に相手と
逆向きだったダウン**では起き上がりのダッシュが出ず、設定のボタンだけが
単独の通常技になっていました。free の瞬間にダミーが振り向くため、ダッシュの
2 番目のタップが逆向きに出ていたのが原因です。

振り向きの前後で押しの向きを追いかけるようにしました。裏回りのダウンでも
起き上がりの瞬間 (free+0/+1) にダッシュが出ます。

実測: 修正前 15 ウェイクアップ全部が失敗 → 修正後 35 ウェイクアップ全部が
ダッシュ。

### Action Steps の After / Landing も調整

- **After** は地上ダッシュのとき、着地するまで入力を出しません。
  空中ダッシュと地上ダッシュは入力が同じなので、実行時に区別します
- **Landing** の期限は、着地予測が出ているあいだ予測に従います。
  以前は期限だけが予測より早く開き、押しが空中に落ちて着地予測が
  使われないことがありました (13 回中 11 回)
- 失敗したダッシュのあとに立ち通常技が残ったとき、その技の硬直明けで
  次のステップへ進むようになりました (以前は Landing 待ちのまま
  止まることがありました)

**既定の動きは変わっていません。** 触らなければ今までと同じです。

---

## v11.7.5

### Tick Data と Action Timeline を、ダミー側でも測れるようにしました

3 行とも、これまでは **1P (自分) の技**だけを測っていました。ダミーが何をしたのか
は入力表示から推測するしかなく、**記録した Action Steps が実際に何になったのかを
読む手段がありません**でした。

`Trainer` -> `Tick Data` の下に **`Tick Data Side`** が付きます。**P2** にすると、

- 発生 / 持続 / 硬直が **ダミーの技**のもの
- 有利不利が **ダミーから見た値**
- Action Timeline が **ダミーの行動**を追う

になります。行の先頭に `P2` と出るので、**画面を見ればどちら側か分かります。**

```
P2  Startup 6t  Active 7 / 3t (Anime 7 / 3t)  Recovery 4t
    Total 27t  Advantage +14t  Hitstun 24t  Hitfreeze 24t
P2  1t Walk > 2t Dash > 6t Air > 12t LP > 14t MK >
    19t Hit > 37t LP > 41t Hit > 53t Landing > 62t Crouch
```

**既定は P1 です。** 触らなければ今までと同じ表示です。

---

## v11.7.4

### 新規インストールで診断表示が出ていたのを直しました

`scripts` を消して入れ直すと、画面左上に

```
P1 Last Got Hit By       : ...
P2 Last Got Hit By       : ...
P1 Push Back Timer Value : ...
P2 Push Back Timer Value : ...
```

が出て、画面上部にも `Projectile Allocation Value` が出ていました。

**Analysis タブの 7 項目が、出荷時に ON になっていました。** 設定を一度でも保存
すれば直るので、**設定ファイルを持っていない人 — つまり初めて使う人だけ**に
起きていました。

### `Wait` の選択画面が、選んだ結果を見せるようになりました

ダッシュの次の攻撃は、ダッシュが**終わってから**ではなく**最中**に出ます。行には
`Auto (11)` と実測値が出ていたのに、選ぶ画面では `After` としか出ておらず、押すまで
何が起きるか分かりませんでした。

**`Fastest (11)` のように、解決した値を出すようにしました。** 実測が無い組み合わせは
`Fastest (Not Measured)` と出ます。攻撃の後は `After` のままです — そこは本当に
「ダミーが終わるまで待つ」ので、その語が正しいからです。

### 2P 側でダークガロンが選べるようになりました

隠しキャラは Start を押しながら PP / KK で選びます。1 本のスティックで 2P を選ぶ
ミラーが Start を渡していなかったため、**ダークガロンだけ選べませんでした。**

Start を渡すようにしました。ステージセレクトの Coin は従来どおり 1P のままです。

あわせて、ダークガロンのダッシュ攻撃の値を入れました。彼のダッシュは通常ガロンと
同じなので、その値を使っています。

---

## v11.7.3

### `Loop Wait` に `Auto (Landing)` を足しました

これまでの `Auto (After)` は、**ダミーが動けるようになってから入力を開始します。**
ダッシュのように入力が続くコマンドは、その助走の分だけ必ず遅れます (ダッシュで
3 ティック)。ガードの練習には向いていますが、最速ではありません。

**`Auto (Landing)` は、着地を予測してコマンドを先行入力します。** 最後の入力が
接地したティックちょうどに乗るので、着地後の最速行動をループで回せます。

ステップ側の `Auto (Landing)` は前からありましたが、**ループの再開には使えません
でした。** そこが埋まりました。

### ジェダの永久が、そのまま再現できます

デミトリ・ビシャモンに対する永久が、**`Loop Wait` を数値で合わせ込まなくても**
出ます。

```
Dummy タブ
  Guard Action Type      : Reversal - Action Steps
  Guard Action Frequency : 100%
  P2 Random Guard %      : 100%
  Guard                  : Stand Block
  Loop Steps             : yes
  Loop Wait              : Auto (Landing)

Reversal Action Steps : Jedah : 2 steps
  1  Auto (Fastest)  Dash   : Forward
  2  Auto (10)       Attack : Forward + HP
```

### 直したもの

**ダッシュ攻撃から着地ダッシュへ繋ぐとき、技によってコマンドが出ませんでした。**
発生の遅い技 (ダッシュ大 P / 大 K など) ほど起きやすく、小技なら問題ないという
形で症状が分かれていました。

ヒットストップが入力の配送と重なることが原因で、**重なり方によって別々の壊れ方を
していました。** 3 種類とも直しています。

- ヒットストップ中のティックも「入力した」と数えていました。ゲームはヒットストップ
  中の押しを受け取らないので、**実際には入っていない入力を入ったことにして次へ
  進んでいました。**
- ニュートラル (レバーを離す区間) でも待つようにしていました。**待っても得るものが
  無く、コマンドの受付時間を食うだけ**でした。
- ヒットストップが長いと、待っても受付時間を超えてしまいます。**その場合はコマンドを
  頭から入れ直す**ようにしました。

### 分かっているが直していないもの

**ガロン・フェリシアは、着地モーションをダッシュでキャンセルできません。** 上の
手順でも最速にはなりません。ゲーム側の仕様で、**人間が操作しても同じ**です。

---

## v11.7.2

### 入力バーをゲームのティック単位にしました

これまでは **1 表示フレームにつき 1 列**しか出せず、コマンドを成立させる 1 ティックの
入力に置き場所がありませんでした。ガードキャンセル 19 回の実測で、**成立させた押し /
離しが描かれていたのは 3 回だけ**。残り 16 回は消えていて、うち 10 回は入力の列が
1 つもありませんでした。**キャンセル自体は毎回出ていて、描画だけが落としていました。**

**GC トレーナー (`Show GC Trainer`) の `SUCCESS` も、成立したティックの列に出るように
なりました。**押しで成立させた場合は次のティック、離しなら同じ列です (押しがずれるのは
ゲーム側の都合で、押した瞬間はまだ技が始まっていないためです)。

**列の下の数字はティックになりました。**これまでは表示フレームで、ターボ 3 では
約 1.33 倍の数字になります。

### ボタンの離し表示を独立させました

**`Show Button Releases` を追加**しました (既定 ON)。これまでは
`Hide Negative Edge Inputs` が離し表示の ON/OFF も兼ねていて、間引きだけしたいのに
離しの印まで消えていました。**2 つは独立し、Hide を ON にしても離しの印は残ります。**

あわせて **`Hide Negative Edge Inputs` の既定を ON** にしました。消えるのは「何も
新しいことが載っていない列」だけになったためです。

**既定値の変更は新規インストールにだけ効きます。**既存の `training_settings.json` は
保存値が優先されます。

---

## v11.7.1

### メニューを整理しました

**タブが変わりました。**`Display` と `Etc` の 2 つに詰め込まれていた 38 行を、
性質ごとに 4 つに分けています。

```
Recording / Gauge / Player / Display / Trainer / Game / Analysis
```

- **Display** — 画面に出しっぱなしにする表示
- **Trainer** — 練習して数字を読むもの (Tick Data、ダッシュ各種、PB 統計)
- **Game** — ゲーム側の設定 (Game Speed、BGM、PB の最低押下数) と、
  キャラクター選択に戻る操作
- **Analysis** — 内部値のタイマーとログ。普段は触らないもの

**親が OFF の行は隠れます。**`Display Hitboxes` を切ると `Display Pushbox X Center`
が、`Show Scrolling Input` を切るとその下の 3 行が消えます。効かない設定が
並んでいるのをやめました。

**キャラクター選択に戻る操作が Game タブに出ました。**これまで Lua Hotkey 4 を
知っている人しか使えませんでした。

**説明文が画面からはみ出していました。**枠に入るのは 4 行までなのに、`Tick Data`
の説明は 12 行ありました。**5 行目から先は凡例の上と画面外に描かれていて、
読めませんでした。**パネルを広げて 6 行に増やし、長すぎた 11 行を書き直しています。

**解説のなかった 11 行すべてに解説を入れました。**Analysis タブの各行には、
読んでいるメモリ番地と画面上のラベルまで書いてあります。

### 直したもの

**プッシュブロックのカウンタがゲームと違う数を出していました。**多段ガードの
2 発目以降でカウンタが 0 に戻りますが、**ゲームは戻しません** ($170 が消えるのは
ガード硬直が明けたときだけ)。11 回の成立のうち 7 回でズレていて、最悪は
`1` と表示しながらゲームは `4` と数えていました。ゲーム自身の数を読むように
変えたので、セーブステートを読んだ直後も合います。

**`PB Count: 0` が緑で表示されることがありました。**「0 回で成立」という
ありえない表示です。数と色を同じ場所から出すようにしたので、もう食い違いません。

**`Use Character Specific Slots` はどこにも書いていませんでした。**引数の順番が
入れ違っていたためで、**「no」と表示しながら機能は ON のまま**でした。

**`Show P2 Inputs` が HUD に従属していました。**`HUD` を切ると効かなくなる作りで、
メニューからは分かりませんでした。独立させています。

**`Push Block Type (PB Recording)` が `: nil` と表示されていました。**既定値が
リストの範囲外だったためです。左を押すと際限なく範囲外へ進む状態でもありました。

**`P2 Infinite Dark Force` が効かないことがありました。**P1 側と `if/elseif` で
繋がっていて、**P1 が ON のあいだ P2 側は何をしても無視されます。**しかも両方とも
出荷時 ON だったので、初期状態がまさにそれでした。両方 OFF で出荷し、分岐も
分けました。

### 説明の誤りを直しました

**プッシュブロックの窓は 12 ティックではなく 14 ティックです。**ROM で確認しました
(`0x023966` が `$1AB` に 14 を書きます)。8 か所ありました。

**`P1 Min PB Presses` の説明が実装と違っていました。**「最低確率を使う」ではなく、
実際は**押下数を 0 に書き換える**ので「一度も押さなかったことにされる」が正しい
動作です。

**ダッシュ関連の 2 行が、互いの画面見出しを指していました。**存在しない見出し名も
書かれていました。

**`BGM On` の説明が誤読を招く書き方でした。**書き直しています。

### 名前を変えた行

| 旧 | 新 |
|---|---|
| HUD | HUD (Life / Meter) |
| Scroll Input Viewer | Scrolling Input History |
| Show Pushblock Counter | Show PB Counter |
| Show Push Block Timer | Show PB Timer |
| Show Push Block Push Back Timer | Show PB PushBack Timer |
| Show Throw Invulnerability Timer | Show Throw Invuln Timer |
| Show Short Hop Counter | Show Short Hop Counter (Sas) |
| Show Damage Calc | Show Damage Calc (on P2) |
| Minimum PB Inputs | P1 Min PB Presses |
| Testing: PB Delayed Pushback Bug | Show Hit Strength + PB PushBack |
| Testing: Projectile Count Limiter | Show Projectile Allocation |

**設定キーは変えていません。**既存の `training_settings.json` はそのまま読めます。

### 分かっているが直していないもの

**`Show Pursuit Indicator` は不完全です。**3 行のうち 2 行は一度も描かれたことが
なく、残り 2 行のラベルはどちらの側を指すのか食い違っています。調べた内容は
`analysis/ISSUE-PURSUIT-INDICATOR-001.md` に残しました。

---

## v11.6.2

### 直したもの

**プッシュブロックが出ないことがありました。**`Guard` を `Push Block (All ...)` に
しても、ガード自体が `P2 Random Guard %` に従います。ところがその行は
`Stand Block` と `All Guard` のときしか出ておらず、**プッシュブロックでは見えない
のに効いていました。**既定が `None` なので、触ったことがなければガードせず、
したがって押しもしませんでした。

行を隠しても依存は消えないので、**値が読まれるときは行が出る**ようにしました。
確率で間引きたい場合はそのまま使えて、既定のままでも行が見えるので気づけます。

**位置ショートカット (Lua Hotkey 2) で、真横の 2 つが効きませんでした。**
レバーを左右に倒して押すと何も起きず、下+左と下+右だけが効く状態でした。

「動ける状態か」の判定が `$06 == 0x00` だけだったためです。**歩きは `$06 = 0x04`**
で、このショートカットはレバーを倒しながら押すもの — **左右に倒せばキャラクターは
歩くので、頼む行為そのものが条件を壊していました。**しゃがみは `0x00` のままなので
斜めだけが通っていました。歩きも通すようにしました。

あわせて、**キーを押しっぱなしにしたときに 1 回だけ効く**ようにしました。ホットキーは
押している間くり返し呼ばれるので、配置を何度も組み直していました。

---

## v11.6.1

### 直したもの

**バレッタを選ぶと 2P 側にコントロールが切り替わりませんでした。**アケコン 1 本で
`P1 キャラ → ステージ → P2 キャラ` と進める機能で、**バレッタのときだけ**先に
進めなくなっていました。

「このプレイヤーはもう選んだか」を `$3BD` で見ていたのですが、そこに入っているのは
**キャラクター id そのもの**でした。バレッタは `0x00` なので、「バレッタを選んだ」と
「まだ誰も選んでいない」が同じ値になります。**18 人のうち 0 番は彼女だけ**なので、
彼女だけが動きませんでした。

決定した瞬間に立つ別のバイトを実測で見つけ、そちらも見るようにしました。
今まで動いていた選び方は何も変わりません。

同じ判定を使っていた**ステージ選択**にも同じ問題がありました。バレッタで P2 側を
操作している間、ステージのカーソルが P1 側を見てしまいます。あわせて直しています。

---

## v11.6

### 追加

**Action Timeline — Tick Data の 3 行目です。**一続きの行動を、起きた時刻で
並べます。緑色で出ます。

```
1t PreJump >  4t Air >  10t MP >  15t Hit >  30t Landing >  45t Free
```

**数字はその出来事が起きたティックで、行動の最初のティックから数えます。**
長さは引き算で出ます — 上の例なら「跳んでから 15 ティック目に当たった」
「中 P を出してから 5 ティック目に当たった」。

- **技はアクションステップと同じ名前で出ます。**通常技はボタン名 (LP〜HK)、
  必殺技はステップの一覧と同じ綴りです
- **ヒットとガードを分けます。**相手の `$140` で見ています
- **歩き・しゃがみ・ジャンプ・着地・ダッシュ・投げ**も並びます
- **連打キャンセルは 2 本として出ます。**同じ小 P でも、ゲームが技を出し直した
  ところで区切ります
- **確定した端から出ます。**行動が終わるのを待ちません
- 行動が終わって 10 ティック何もしなければ、そこで 1 本が締まります

### 直したもの

Action Timeline を作る過程で、実測を突き合わせて直したものです。

- **空中攻撃の有利フレーム**が短めに出ていました。着地を基準に測るようにしました
- **歩きが三行目に出ませんでした。**歩きの状態は `0x00` ではなく `0x04` でした
- **連続ジャンプ・連続ダッシュ**が 1 回分にしか見えませんでした
- **ダッシュ攻撃**の発生にダッシュの助走が混ざっていました
- **ダッシュ投げ**が「大 P を出してから投げた」ように見えていました
- **発生 1 フレームの投げ** (ビクトルの一回転・二回転) で技名が出ませんでした
- **キャラクターが登場している間**、まだラウンドが始まっていないのに測っていました

---

## v11.5.2

### 変更

**Tick Data (旧 Frame Data) を作り直しました。**表示が 2 段になり、フレーム
データ表と同じ数え方になっています。

```
Startup 9t  Active 2 / 2 / 2t (Anime 2 / 2 / 2t)  Recovery 38t
Total 62t  Advantage -14t  Hitstun 22t  Hitfreeze 12t
```

- **発生・持続・硬直をヒットボックスから測ります。**以前は「最初に当たるまで」
  だったので、離れて当てるほど発生が伸びていました
- **多段は段ごとに** `2 / 2 / 2` と出ます。判定が途切れない 2 段も分けて出ます
- **アニメ技を実測で見分けます。**ヒットストップ中もアニメが進む技は
  `(Anime 4t)` と出ます。技ごとの表は持っていません
- **投げ・飛び道具も測れます。**ダメージが本体に無い技 (デモンビリオンなど) も
  出るようになりました
- **ダウンと投げは起き上がりまで** — `Wakeup` として出ます
- **変身中に測定が止まっていました。**デミトリのバットスピンで何も出なかったのは
  これが原因です

---

## v11.5.0

### 追加

**REVERSAL - Action Steps。**ステップを並べて、通常技・必殺技・ジャンプ・
ダッシュを順に出せます。

つなぎ方そのものを選べます。ウェイトを手で詰める必要はありません。

- `Auto (Chain)` — チェーンがつながる最初のところ
- `Auto (Cancel)` — 必殺技キャンセルが効く最初のところ
- `Auto (Late Cancel)` — そのキャンセルの最後のところ
- `Auto (Rapid Fire)` — 連打キャンセル (小攻撃の後ろにだけ出ます)
- `Auto (Landing)` — 着地した瞬間
- `Auto (After)` — 前の技が終わったところ

数字でティック数を指定することもできます。ループもあります。

**必殺技はチートで出すのではなく、コマンドを入力して出しています。**チートで
起きていた変な現象は起きません。

タメ技のために**レバーの Hold** を付けました。Hold を入れたステップの間は方向が
入りっぱなしになります。

**Show Step Wait Ticks** (Display) — 各ステップが実際に何ティック待ったかが出ます。

```
Step.1 Act:3 / Step.2 Wait:13 Act:1 / Loop Wait:11 / Step.1 Act:3
```

**Show P2 Inputs** (Display) — 画面右端のダミーの入力アイコンを消せます。

### 変更

**Tech Throws を確率にしました。**`None` / `25%` / `50%` / `75%` / `100%` の
5 段です。抽選は **1 回の投げにつき 1 回だけ**振ります (毎フレーム振ると、抜け
入力がどれか 1 フレームに乗れば通ってしまい、50% でも実質 100% になります)。
以前オンだった設定は `100%` になります。

### 直したもの

**メニューを開くと、走行中の Action Steps を終了するようにしました。**開いても
ゲームは止まらないので裏で走り続けていて、レバーが入りっぱなしになる・編集中に
ダミーが勝手に動く・ループが前の内容のまま回り続ける、といったことが起きて
いました。閉じても勝手には回り始めません — ダミーを 1 回ガードさせるか殴ると、
編集後の新しいリストで動き始めます。

---

## v11.4.2

### 変更

**ループ再生の待ち時間を Before と After に分けました。**

親の行は `Loop Interval (Frames) : (Before/After)` で今の値を出します。右か LP で
子メニューが開き、**レバーだけで操作できます** — 左で減り、右で増え、上下で行を
移り、一番下の `Back` で戻ります。MP で 0 に戻せるのは今までどおりです。

- **After** は前の周が終わってダミーが動けるようになってから数える待ちです。
  今までの Loop Interval と同じ位置なので、**保存していた値は自動で After に
  引き継がれます。**
- **Before** は距離を戻したあと、次の周が出るまでの待ちです。

待ちが 1 か所しかないと、「距離を戻す前に間を置く」と「戻してから間を置く」を
区別できませんでした。`Reset Distance Each Loop` でワープしてから一拍おきたい、
という使い方ができます。

**メニューを閉じた直後、リピート再生が 30 フレーム止まるようにしました。**

閉じた瞬間に次の周が飛んでくると構える間がないためです。止まっている間は録画の
入力も出ません。再開は同じ録画位置からで、飛ばした分を取り戻す動きはしません。
単発の再生には影響しません。

**保存メニューの `Play again` は、ダミーが動き終わるまで選べないようにしました。**

録画は入力が途切れた時点で終わりますが、キャラクターはまだ技を出し切って
いません。そこで `Play again` を押すと、**動けないダミーに向けて再生し直す**
ことになり、状況が再現されませんでした。動いている間は灰色で `(still moving)`
と表示され、終われば押せます。

**待つのはこの 1 つだけです。**`Save to this slot` と `Record again` は
ダミーが何をしていても即座に応答します。次をすぐ録りたいときに待たされません。

あわせて、再生中に押していたボタンがそのまま選択の答えにならないように
しています。

**録画ウィザード中は `Lua Hotkey 1`（中止）だけが効くようにしました。**

`Lua 3`（ループ切替）と `Lua 4`（キャラクターセレクトへ戻る）は、再生や録画の
最中に割り込んで状態を壊すためです。中止だけは残しています。

**`Play Recording` は、右を押している間に何度も反応しないようにしました。**

右は押しっぱなしで連続入力になるため、再生の開始と停止を繰り返していました。
一度の押下で一度だけ反応します。

**キャラクターポジションの変更で、画面が一緒に動くようになりました。**

これまでカメラは自力で追いかけていたので、移動が終わっても画面だけ遅れて
追いついていました。移動そのものも少し速くなっています。

### 修正

**録画ウィザードの確認再生と `Reset Distance Each Loop` で、位置と画面が
録画したときに戻るようになりました。**

これまでは 2 人の位置は戻っても**画面のスクロール位置が戻り切らず**、さらに
画面の外に置かれたキャラクターが引き戻されることがありました。ガロンの K 投げ
のように 2 人が入れ替わって大きく動く録画で特に目立ちました。

**v11.3.x で保存した設定が正しく引き継がれるようになりました。**

移行が行われず、**選んでいたリバーサルが隣の項目にずれたり**、ループ間隔の
保存値が失われることがありました。

**複数ヒットするガードのあとでも、リバーサルとカウンターアクションの必殺技が
出るようになりました。**

小技の連打キャンセルなどで 2 ヒット目をガードしたとき、入力が積み直されず何も
出ないことがありました。

**`Knockdown Logger` をオフにしたときに、診断用の記録が完全に止まるように
なりました。**

オフでも一部が動き続けていたため、`reversal_logs` にファイルが書かれ、動作も
重くなっていました。

---

## v11.4.1

v11.4 で入れた録画ウィザードを、実際に使ってみて見つかった点をまとめて直した版です。

### 変更

- **スーパージャンプ**に改名しました。v11.4 では High Jump という名前でした。並び順とインデックスは変わらないので、設定への影響はありません。
- **保存すると録画ウィザードのスロット一覧に戻ります。**結果を1秒表示してから戻るので、続けて別のスロットも録れます。戻るときにキャラクターの距離も録画開始時に戻します。
- **スロット一覧に `Back to menu` を追加**しました。`Lua Hotkey 1` で中止したときもメニューが開き直します。
- **レバーだけで操作できるようになりました。**スロット選択も保存確認も、上下で選んで LP か右で決定します。保存確認は「保存 / 録り直し / 再生し直し」を縦に並べました。ボタンの直接操作もそのまま使えます。
- **Play Recording と Recording Wizard も右で入れます。**メニューでは右が「奥に進む」操作なので揃えました。
- **Reset Distance Each Loop** を追加しました。ループの各周の頭で、録画したときの距離に戻します。周を重ねるうちに離れていって、練習していた状況が起きなくなる問題への対処です。v11.4.1 以降に録った録画が対象です（距離は録画ファイル自身に記録されます）。
- 録画ウィザードの見出しを太くし、スロット一覧の行間と桁を揃えました。

### 修正

- **セーブステート読み込みでスクリプトが停止する問題**を修正しました。`Use Savestate Upon Recording` が有効なとき、ループ再生や再生ホットキーで落ちることがありました。
- **セーブステートは取った試合の中だけ**で有効になりました。キャラクターを選び直すと読み込まれません。全体の状態が戻るものなので、別の組み合わせのものを読むと試合ごと入れ替わってしまいます。
- **再生中は Coin での操作の入れ替えを受け付けません。**再生がダミーを動かしているところへ操作を移すと、どちらも正しく動かないためです。
- **ラウンドが始まるまで `Lua Hotkey 1` と `2` を受け付けません。**入場演出の最中に位置を動かすと配置が壊れ、メニューを開くと両キャラが止まったまま演出の途中に取り残されていました。
- 録画ウィザードを終えると、必ず操作が 1P に戻ります。2本続けて録ったときに戻らないことがありました。

---

## v11.4

v11.3.1 からの変更です。

### 新機能

**録画ウィザード**

ダミーの録画を案内付きで行えます。Recording タブの `Recording Wizard` から始めます。

スロットを選ぶと操作がダミー側に切り替わり、動かし始めたところから録画が始まります。手を止めて2秒で自動終了し、そのまま録ったものを一度再生して見せます。確認してから保存、気に入らなければ録り直し、もう一度再生、が選べます。

再生の前に両キャラを録画開始時の位置に戻すので、録ったときと同じ距離で確認できます。`Lua Hotkey 1` でいつでも中止でき、そのときは保存されません。

**ジャンプの追加**

ダミーの行動に前・N・後ろのジャンプを追加しました。`Reversal/Counter Input Motion` の一覧、バックダッシュキャンセルの次に並びます。下を入れてから上を入れる形で、前後は上を入れるときに乗せます。

**Play Recording**

Recording タブの一番上から、現在のスロットをそのまま再生できます。再生ホットキーと同じ動作で、もう一度押すと止まります。

**アケコン1本でキャラクターセレクト**

1P キャラ → ステージ → 2P キャラまで、同じスティックで完結します。

**Loop Interval (Frames) (Before/After)**

ループ再生の合間に待ちを入れられます。待ちはダミーが動ける状態になってから数え始めるので、硬直の長さに左右されず一定の間隔になります。

### 改善

- スロットに録画した日時を表示します（メニューとウィザードの両方、24時間表記）。
- 1P と 2P が入れ替わっていても、左右を自動で反転して再生します。録画時にどちら側だったかを記録し、再生時に見比べています。
- ループ再生が、ダミーが動ける状態になるのを待ってから次の周を始めます。硬直中に次の入力が来て1周目が出ない、という状態が起きなくなります。
- 録画の自動終了を「技が終わってから2秒」に変更しました。レバーがニュートラルなだけでは終わらないので、長い技の途中で切れることがなくなります。
- メニューの配色を見やすくしました。
- ダウンリバーサルの無敵が通常の挙動になりました。コマ投げにセルフディレクションが付きます。メニュー操作のカクつきを解消しました。

### 修正

- **ガードキャンセルバックダッシュが失敗する問題**を修正しました。v11.3.1 で既知の不具合として挙げていたものです。弱・中・強すべてで安定します。
- **ウィザードの録画が途中で止まる問題**を修正しました。`Use Savestate Upon Recording` が有効なとき、ループ再生が1周終わる瞬間に読み込まれるセーブステートが、進行中の録画を打ち切っていました。
- キャラクターセレクトに戻った後も再生状態が続く問題を修正しました。
- 試合が始まるまで `Lua Hotkey 1` と `2` を受け付けないようにしました。

### 注意事項

- 設定ファイルは初回起動時に自動的に移行されます。保存済みの設定は同じ内容のまま引き継がれます。
- Display タブと Etc タブの初期値を見直しました。新規インストール時のみ適用され、既存の設定には影響しません。

---

## 既知の制限

- ダッシュ間隔 / ダッシュ時間 / ダッシュ攻撃キャンセル / 攻撃ダッシュ間隔 / Jump In のトレーナーは表示フレーム基準のままです。
- Jump In Trainer に不具合があります。当てずに着地しただけでも差が記録されることがあります。
- 録画ウィザードにセーブステートは付いていません。確認再生は位置と向きを戻して行いますが、体力やゲージまでは戻りません。
- run-ahead には未対応です。有効なままだと最速行動が安定しないため、検出して画面で警告します。
