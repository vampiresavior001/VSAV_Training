# 着地予測 — 調査結果と実装案 (2026-09-18)

**問い:** `Landing` と `After` は予測できるか。
**きっかけ:** サスカッチの前ダッシュキャンセル小技を連発し、着地して次のダッシュ
攻撃を**最速で**出したい。

**結論: Landing は予測できる。ダッシュにも間に合う。** `After` は未調査。

---

## なぜ予測が要るのか

ダッシュは `F N F` と入力が続く。**最後の入力が「行動可能になった最初のティック」に
乗る**には、その分だけ**早く撃ち始める**必要がある。着地を見てから動く仕組みでは、
その助走の分だけ必ず手遅れになる。**予測は便利機能ではなく要件。**

---

## 測ったこと

プローブ `analysis/landingPredictProbe.lua`。降下中の毎ティックに
`ticks_to_landing()` の答えを記録し、実際の着地 (`$38` が 1 → 0) と突き合わせる。
ログは `analysis/landing_predict_probe_20260918_*.log`。

### 1. 物理計算は正確

```
t=165 pred=36 left=92  y=40 vy=245760 ay=-13312
t=166 pred=35 left=91  y=43 vy=232448 ay=-13312
t=167 pred=34 left=90  y=46 vy=219136 ay=-13312
```

`pred` が毎ティックちょうど 1 ずつ減る。`ay` も一定で、dword で読んでいることの
疑いも晴れた。**殴っていない側の素のジャンプは補正なしで `+0` のみ。**

### 2. 誤差の主因はヒットストップ

```
t=178 pred=23 left=79  y=70 vy=72704 ay=-13312
t=179 pred=23 left=78  y=70 vy=72704 ay=-13312   ← 全部同じ
t=180 pred=23 left=77  y=70 vy=72704 ay=-13312
```

**位置が凍るのにティックカウンタだけ進む。** `ticks_to_landing()` は**物理の時間**で
答え、待ち時間は**時計の時間**で測られる。その差がそのまま誤差になる。

`$5C` がヒットストップの**残り時間** (`tickDataVsav.lua:364`。毎ティック減り、ヒットで
`0x0B` に叩き込まれる)。実測の `-11` の山が `0x0B` = 11 と一致した。

**ただし `$5C` は「凍るはずの理由」であって「凍った証拠」ではない。** 殴った側にも
立つので、立っていても物理が動いていることがある。数えるなら **`y` と `vy` が前
ティックと同じか**を見るのが正しい。補正の精度もそちらが上だった
(`-1` の山が 330 → 2)。

### 3. キャラ別の調整は要らない

`y` / `vy` / `ay` / 床はすべて**オブジェクトから読んでいる**。係数表は使っていない。

| キャラ | 結果 (停止で補正) |
|---|---|
| Victor | `+0` のみ |
| Demitri | `+0` のみ |
| Q-Bee | `+0` が大半 + 正の誤差 |

**Q-Bee だけ荒れるのは飛べるから。** 降下を自分で変えられるので、**予測した後で
軌道が変わる**。サスカッチの跳ねと同じ現象が強く出ているだけで、**計算がキャラ別
という話ではない。**

### 4. どれだけ遅く commit すれば安全か

Q-Bee (最も厳しい条件) で、予測を**着地の何ティック前に作ったか**で分けた。

| commit の遅さ | 結果 |
|---|---|
| **1-2 ティック前** | **`+0:158` — 完全に綺麗** |
| **3-5 ティック前** | **`+0:235` `+35:2` — 99.2%** |
| 6-10 ティック前 | `+0:358` + 誤差が数種 |
| 11-20 ティック前 | 広く散る |

**→ 着地の 5 ティック前までに作った予測なら、飛べるキャラでも信用できる。**

### 5. ダッシュの助走は 4 ティック

`M.compile` に聞いた (実機不要)。**全キャラ同じ**。入力列の長さで決まるため。

| ステップ | lead |
|---|---|
| Dash Forward | **4** |
| Dash Forward Cancel | **4** |
| Dash Back | 4 |
| Attack (単体) | 0 |
| Custom DPF + HP | **5** — 窓ちょうど。余裕なし |

**→ ダッシュは窓に 1 ティックの余裕を残して収まる。成立する。**

---

## 実装案

### 凍結中はコマンド受付も減る — だから「止めて待つ」はできない

**本人の経験則 (2026-09-18):** ヒットストップ中でも**コマンド受付カウンタは
減り続ける**。測る必要はない、とのこと。

今日のログもこれと整合する。物理が凍っている間も `t` は進んでいた。この `t` は
フック `0x0221CC` が発火したティックで、**そこは入力処理ルーチンの中**である。

```
t=178 pred=23 left=79  y=70 vy=72704 ay=-13312
t=179 pred=23 left=78  y=70 vy=72704 ay=-13312   ← 物理は凍っている
t=180 pred=23 left=77  y=70 vy=72704 ay=-13312   ← が、入力処理は走っている
```

**これで案が 2 つ潰れる。**

| 案 | なぜ駄目か |
|---|---|
| 配送を止めて待つ | 窓は減り続けるので、12 ティック止めれば 10 ティックの窓は切れる |
| 凍結中も配送し続ける | 最後の入力が**着地より早く**着く。「必殺技コマンド以外は free+0。押しが free+0 に乗らなければ出ない」に反する |

### 採る案 — 捨てて撃ち直す

**未知に賭けない形にする。**

```
commit の条件
  ・ヒットストップ中は commit しない  ($5C == 0 のときだけ)
  ・ticks_to_landing() == lead  になったら撃つ

配送中に新しく凍ったら
  ・その列は捨てる
  ・解けてから予測し直して、撃ち直す
  ・着地までの残りが lead 未満なら、その回は諦める
```

**「凍っている間は commit しない」が効く理由:** 凍結中に撃てば、配送が窓を食い
ながら進む。撃たなければその問題自体が起きない。解けてから改めて測ればよい。

**諦める回が出るのは代償。** ダッシュの助走は 4 ティックなので、凍結が解けた時点で
着地まで 4 ティック以上あれば撃ち直せる。足りなければその回は出さない。**出ない
より、中途半端な入力を流すほうが悪い** — 何が起きたか分からなくなる。

### 却下した案 — 止まっている間は予定表ごと止める

当初こう提案したが**誤り**。物理と配送の関係は保たれるが、**ゲーム側のコマンド
受付を勘定に入れていなかった**。上記のとおり窓は減り続ける。

### 影響範囲

`Auto (Landing)` だけでなく、**数値 Wait も同じ問題を抱えている**はず。
`service_body` の待ち処理はすべて同じ時計を見ているため。原則を 1 か所に入れれば
全部に効く。

---

## 実機で確認した (2026-09-18)

サスカッチのダミーに 4 歩:

```
1 Auto (Fastest)  Dash : Forward Cancel
2 Auto (8)        Attack : LK
3 Auto (Landing)  Dash : Forward Cancel
4 Auto (8)        Attack : LK
```

`Show Step Wait Ticks` の読み出し:

```
Step.1 Act:5 / Step.2 Wait:8 Act:1 / Step.3 Wait:23 Act:6 / Step.4 Wait:8 Act:1
```

(`Auto (3)` と記録していたのは画面写真の読み違い。本人の指摘で訂正、2026-09-19。
`Auto (N)` の N は `auto_ticks` が測定表から解決した値で、設定値ではない。)

`Step.3 Wait:23` は 2 歩目の最後の入力から 3 歩目の押しまでで、**跳んで降りる
時間そのもの**。遅延ではない。本人の判断:「着地後のダッシュが最速になっている
ように見える」。

**プッシュブロックも通った。** 本人が PB を出した状態で試し、**着地後にきちんと
ダッシュした**。これが唯一残っていた未知のリスクだったので、これで潰れた。
実装に計測を残す案は、必要にならなかったので入れていない。

**予測より早い着地 (約 8%) も実害なし。** 逃した回も着地後にやりきるため
(本人)。窓の外の話なので、精度を上げる調査は当面不要。

## 失敗の再現条件が分かった (2026-09-19)

**発生の遅い技 (ダッシュ大 P / 大 K) を前ダッシュキャンセルから出したときだけ
`Landing` を見逃す** (本人の観察)。

**決め手は「着地後に技を空ぶらない」こと。** 単に配送が遅れているなら、着地して
から遅れたダッシュが出るはずである。出ないということは、**入力が空中で使い切られ
て消えている**ことを意味する。遅延ではなく消失。

**なぜ遅い技だけか。** 当たる瞬間が降下の遅い側へずれるため、**ヒットストップが
3 歩目の commit から配送までの窓に重なる**。小技は跳び際に当たるので、3 歩目が
commit する頃には凍結が終わっている。

凍ると起きること (いずれも既に実測済み):

| | |
|---|---|
| 物理 | 止まる (`y` / `vy` が同値のまま) |
| ティック | 進む (`t` は increment) |
| 着地 | 11 ティック後ろへずれる (`$5C` はヒットで `0x0B`) |
| 配送 | **ティック時計で走り切る** - `current_frame` だけで進み状態を見ない |

**これは上の採用案「配送中に新しく凍ったら撃ち直す」が未実装であることそのもの。**
commit 側のガードだけ入れて、配送側を入れていなかった。

### 却下した形 — 着地歩のたびに最終押しを待つ

2026-09-19 に一度実装して戻した。最終エントリで `$38` を見て、空中なら待つ、と
いう形。**凍結の有無に関係なく待つため、凍っていない大多数の回まで遅れた** -
実測 `LW:240 / Rev:24` で **1 周あたり約 10 ティック**。ダッシュキャンセルの
間合いが崩れ、攻撃がホップ中に出ず着地後に出るようになった。

**条件は「空中か」ではなく「配送中に凍ったか」。** 凍っていない回に触らない形で
なければならない。

---

## 直った (2026-09-19)

**大 P / 大 K とも正常動作を確認** (本人)。小 P は最初から最後まで無変化。

### 採った形

```
配送ごとに「凍結に遭ったか」を覚える (saw_freeze)

最終エントリが始まる瞬間だけ、遭っていた配送について:
  まだ空中で、着地がこれから来る
    → 猶予の内なら待つ
    → 猶予を超えたら頭から入れ直す (窓ごと作り直す)
```

**凍っていない配送には一切触れない。** 小 P は `FD:0` なので作動しない。以前
「空中なら待つ」だけにして `LW:240` まで膨れ、動いていた側を壊した反省。

### 実測 — 入れ直しが主役だった

```
FD:67  LS:46  AF:46  AFd:792  Pr:23  Dl:0  Hd:176  Rs:22
```

**`Hd:176 = 22 × 8`。** 猶予 8 回分を使い切ってから入れ直す、が 22 回すべて。
**保持だけで間に合った回は一度もない。**

本人の指摘 (「凍結中に 10 tick 程度経過したら再度頭から入れ直しみたいなものも
いる気がする」) が無ければ成立しなかった。当初これを保持の補助と見ていたのは
誤りで、**保持は前置き、入れ直しが本体**。

### 踏んだ罠 — 自分が出させたダッシュに反応した

最終の `forward` は v158 の規則で 2 ティック出る。**1 ティック目でダッシュが成立し、
サスカッチは足が浮く** (`$38` が 0→1、VSAV_MEMORY_NOTES.md に既出)。2 ティック目に
「空中かつ降下中」と判定して入れ直し、ダッシュ→着地→ダッシュ→着地が延々続いた。

`(_s0.tick_held or 0) == 0` を足し、**最終エントリが始まる瞬間だけ**を見るようにした。
待機中は書き込む前に return するので 0 のままで、保持も入れ直しも従来どおり動く。

**既に持っていた事実を実装時に突き合わせていなかった**のが原因。

## 全部動いた (2026-09-19)

**ジェダ大P、サスカッチ大P、サスカッチ大K の 3 つとも成立。** 直前まで「直すたびに
別が壊れる」を繰り返していたが、原因が 3 種類あったため。

### 原因は 3 つ別々だった

ティック別トレース (`scripts/land_trace.log`、着地歩の前後 90 ティックを 1 行 1
ティックで記録) で初めて分離できた。**累積カウンタでは 2 回続けて読み違えた** -
`NF` は成功側でも 100%、`HS` は両側 0 で、どちらも判別に使えなかった。
**順序を問う質問に合計で答えようとしたのが誤り。**

| 症状 | 原因 | 直し方 |
|---|---|---|
| 短い凍結が押しに重なる | ボタン無し入力は 2 ティック必要 (v158) なのに、凍結中の押しはゲームが捨てる。凍結ティックも「配送した」と数えて次へ進んでいた | **凍結中のティックを配送に数えない** |
| 凍結がニュートラルに重なる | ニュートラルには登録される押しが無い。待ってもダッシュの猶予 (10) を食うだけ | **何かを押しているエントリだけ待つ** |
| 長い凍結が押しに重なる | 待つと 1 回目のレバー方向が 13 ティック保持になり、「10 フレーム継続できる」を超過して失効 | **猶予を超えたら凍結明けに頭から入れ直す** |

### 3 機構とも必要 — 実測で確認

最終テストの 27 ブロックを機構別に数えると、きれいに分かれた。

| ブロック | 働いた機構 | 結果 |
|---|---|---|
| 64-73 | 凍結ティックを数えない規則だけ | 成立 |
| 74-80 | **着地の保持・入れ直し** (`HOLD:8 RST:1`) | 成立 |
| 81-90 | **窓切れの入れ直し** (`EXPIRED:1 REENTER:1`) | 成立 |

**どれも余分ではない。** 「着地の仕掛けは不要になるかも」という見立ては外れ。

### 途中で壊して直したもの

| 壊れ方 | 原因 | 直し方 |
|---|---|---|
| ダッシュ→着地→ダッシュの無限反復 | 最終押しは 2 ティック出る。1 ティック目でダッシュが成立して足が浮き、2 ティック目に「空中かつ降下中」と見て入れ直していた | `tick_held == 0`、始まる瞬間だけ見る |
| サスカッチの 2 回目が遅れる | 解除条件を「接地」から「動けるか」に変えた。サスカッチは**空中で押して出ている** (AF 46/46) ので前提が誤り | 即座に差し戻し |
| サスカッチ大P が壊れる | 凍結ティックを数えない規則をニュートラルにも適用した | 押しているエントリだけに限定 |

---

### ジェダは直らなかった — そして「free+0 に乗せる」では説明がつかない

**2026-09-19。ダッシュ大P > 着地ダッシュ の 2 本目が出ない。** 仕掛けは設計どおり
動いていた: `Hd:3` 待ち、`Rs:0`、`AF:0`(最終押しは地上)。**それでも出ない。**

`NF` (押した瞬間に `$05`/`$06` が非ゼロ) を測ると **`NF:2 / LS:2` = 100%**。
接地はしているが、まだ動けないティックで押していた。

**そこで解除条件を「接地」から「`$05==0 かつ $06==0`」へ変えたところ、
サスカッチの 2 回目のダッシュが遅くなった。**動いていた側が壊れたので即座に戻した。

**この 2 つは同じ物差しでは説明できない。**

| | 最終押しの状態 | 結果 |
|---|---|---|
| サスカッチ | **空中** (`AF:46/46`) | **出る** |
| ジェダ | 地上だが動けない (`NF:2/2`) | 出ない |

**サスカッチは空中で押して出ている。**「押しが free+0 に乗らなければ出ない」だけでは
この 2 例は両立しない。**何かを見落としている。** 推測で条件を足すと、そのたびに
動いている側を壊しているので、**次に触るときは先に ROM でダッシュ成立の判定そのものを
読むこと。**

**現状の扱い:** 解除は接地 (`$38`) のまま。サスカッチ (大P / 大K) は動く。
ジェダは未解決。

### 未解明として残す

`AF:46 / LS:46` (最終エントリの書き込みが全部空中) と `AFd ÷ AF ≈ 17` が、
**正常動作している事実と噛み合う説明になっていない。** 推測を書かず観察のまま残す。
精度を詰めたくなったときの入口。

---

## ガロン・フェリシアは直さない — 判断と根拠 (2026-09-19)

**症状:** 着地ダッシュが出ない。最終押しの瞬間の `$06` が `0x14`(前のダッシュの
中)で、75 回中 68 回が無駄打ち。**ダッシュでキャンセルできない着地モーション**を
持つキャラで起きる。

**「`$06` が `0x14` なら待つ」は入れれば直る見込みで、しかも安全**だと確認できた。
サスカッチは**最終押しで `14` を出すことが一度も無い** (`00` 1/1、`06` 38/39) ので、
動いている側に当たらない。数値は VSAV_MEMORY_NOTES.md。

**それでも入れない。**

1. **着地硬直のせいで、そもそも人間にも素早く出せない。**対戦相手がその戦略を
   取らないので、ダミーに再現させる価値が小さい (本人)
2. 凍結が絡まない**新しい待ち経路**が必要になり、上限の置き方も未検討
3. 2026-09-19 は**条件を足すたびに動いているものを 3 回壊している**

**「気づいていない」ではなく「分かった上で放置している」**状態にする。必要に
なったときは、標本も機序もここに揃っている。

### 計器についての注意

ティック別トレース (`scripts/land_trace.log`) は**ループで使うとブロックが
際限なく伸びる。** `trace_open` が着地歩のたびに窓を延長するため、1 ブロックに
何百回分も入る (実測 8000 行)。**解析するときは「各ブロックの最初の 1 件」では
なく全件を数えること** — これを間違えて標本を 1/25 に見誤った。

---

## 残っている宿題

### プッシュブロック — 済み

上記のとおり実機で確認済み。懸念は妥当だった (PB は攻撃側を押し返すので軌道が
変わる) が、**弾道計算が毎ティック読み直すので追随していた**。

### `After` は未調査

本人の指摘: **技の空振りなどで状況が分かれるので、ひとまとめにできない。**
「ジャンプ着地後の 8t」も**キャラごとの値**の可能性が高い。`Landing` と違って
**単一の式にはならない**見込み。

### 予測より早い着地 (全体の約 8%) — 当面やらない

`+11` / `+16` / `+35` として残る。凍結では説明できない。`$38` が落ちる時点と
`y` が床を割る時点の定義差を疑っている。ダッシュの窓 (5 ティック) の外の話で、
**逃した回も着地後にやりきるので実害なし** (本人、2026-09-18)。精度を上げたく
なったときの調査対象として残す。

---

## 現状

`master` は `v11.7.2` + 着地の修正 17 行のみ。**この調査は `scripts/` に一切
触れていない。** 測って確かめてから入れる順序を守った。
