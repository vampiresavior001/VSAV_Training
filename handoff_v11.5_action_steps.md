# 引継ぎ — VSAV Training v11.5 / Action Steps

2026-08-31 時点。次のセッションが最初に読むもの。

---


## 解決: バレッタで 2P へ操作が渡らない (2026-09-23)

**条件は「バレッタ」かつ「LP で決定」。** 本人が実機で切り分けた。

**原因** — `char_chosen` が読んでいた 4 バイトが、この組み合わせでは全部 0 になる。

| 番地 | 正体 | この組み合わせでの値 |
|---|---|---|
| `$3BD` / `$3E0` | キャラ ID (`020AC8`) | バレッタ = **0** |
| `$3AE` / `$3E1` | **決定に使ったボタンの番号** | LP = **0** |

ボタン番号は実測で MP=1、LK=3、MK=4。並びは LP MP HP LK MK HK で **LP が 0**。
キャラは正しく選ばれていて、**こちらに読める値が無かっただけ**。

**直し方** — 決定そのものを見る。選択画面では攻撃ボタンが決定なので、`$394`
(入力のボタン側) の押し始めを latch する。マスクは `$77` で、Start と Coin は
数えない。選択画面を離れたら落とす。従来の 2 バイトの判定も残してある
(ID が入るのは 1 フレーム早い)。

**外した見立て 3 つ** — いずれも測って否定した。`$3BC` / `$3E3` は逆アセンブル上
それらしいが終始 0。`$3E1` の「セッション差」は誤読で、正体はボタン番号。`$3B4`
(このツールが書く無効化フラグ) は 396 行すべて 0 で無関係。

**教訓** — この件では 3 回、測る前に直そうとして全部外した。ROM に「それらしい形」
があっても、**実機で値を見るまで決めない**。
## 0. 場所 — 移行は済んでいる

2026-09-02 に移行完了。**リポジトリはここにあります。**

```
リポジトリ   C:\fightcaVSAV-Debug\emulator\fbneo   .git あり
旧フォルダ   C:\fightcade\emulator\fbneo           退避用に中身を残してあるだけ
```

旧フォルダにも同じ `.git` が残っていますが、**もう触りません。**
両方に commit すると分岐して合流できなくなります。作業はこちらだけで。

移行前に何があったかは、この 2 つの commit に書いてあります。

```
e93cb77  移行先フォルダの作業を取り込む(ダッシュキャンセル、Auto のオフバイワン、
         メニューゲート)。タグ v301-dash-cancel-step
1435fc0  この引継ぎ資料
```

`guardCancel.lua` は取り込み時に行頭の余分な半角スペースを 83 行落としています
(元の HEAD では 0 行)。編集ツールの混入で、コードは変わっていません。
---

## 1. これは何か

VSAV (CPS2, 970519 Japan) の FBNeo 用トレーニングツール。Lua。

**Action Steps** = ダミーの多段行動をステップで定義する機能。ガードやダウンを
トリガに「ジャンプ → 空中ダッシュ → 攻撃」のような並びを走らせる。

```
scripts/actionSequenceEditor.lua   メニュー。描画と編集だけ
scripts/actionSequenceRunner.lua   コンパイルとスケジュール
scripts/guardCancel.lua            310 KB。アーム、ティックフック、実測値の表
scripts/controller.lua             make_input_sequence(モーション → 入力列)
analysis/test_*.lua                オフラインテスト 8 本
VSAV_MEMORY_NOTES.md               ゲーム側の解析結果。事実だけ
```

### 動かし方

```bash
cd C:/fightcaVSAV-Debug/emulator/fbneo/scripts && lua5.1 ../analysis/test_editor_rows.lua
```

テストは 8 本。**必ず `scripts/` から**走らせる(相対パスで controller.lua や
guardCancel.lua をソースごと読むため)。

```
test_editor_rows      画面に出る文字列を 1 文字ずつ照合
test_editor_ops       追加・並べ替え・削除・旧形式の移行
test_runner_compile   Auto の解決先、先頭ニュートラル、ボタン展開
test_seq_hold         Hold で方向が保持されるか
test_gc_frequency     Guard Action Frequency の発生率
test_gc_leftover      機会が独立しているか
test_random_sources   メニューの表記と実装の一致、乱数の種
test_menu_gate        Reversal Action Steps の行が 0xB のときだけ出るか
```

**実機テストは FBNeo の完全再起動が必要。**Lua の差し替えは再起動でしか効かない。

---

## 2. 守ること(利用者の指示)

```
返答もコミットメッセージも日本語。ソースコメントだけ英語
推測で直さない。分からなければアセンブラ(ROM)に戻る
実装前にログや既存テストで検証し、予測を報告してからテストを依頼する
目視確認は最小限。判定はコードにさせ、ユーザには「1 回普通に使う」だけ頼む
渡す前に自分で批判的レビューを通す
手を入れる前に既存スクリプトを洗う(車輪の再開発防止)
正しさと簡単さなら簡単さ
必殺技以外は free+0。押しが乗らなければ「遅い」ではなく「出ない」
120f のような固定フレーム数を入れない
版を渡すたびに git tag を打つ
再テストを頼む前に reversal_logs を archive する(消さない)
```

### 特に効いた 2 つ

**テストに自分の前提を書くと、前提が誤りのときテスト全通過のまま壊れる。**
Guard Action Frequency で実際に起きた。動作テストに加えて**ソース側の契約**も
照合すること(`test_gc_leftover.lua` の `[7]`、`test_random_sources.lua`)。

**スクリーンショットの数字は測定値として読む。**「測ってください」と頼んだ値が
既に送られた画面に写っていたことがある。依頼の前に全行読む。

---

## 3. この表について — 一般化を 4 回して 4 回とも外した

`guardCancel.lua` の `AIR_DASH_TICKS`(空中ダッシュの実測値)。

```
「空中の攻撃は 0 固定」      レイレイの 5 で崩れた
「踏切はジャンプの性質」      ザベルの交差 4 で崩れた
「同じ向き同士だけ遅い」      Q-Bee の後→前 6 で崩れた
「和がキャラごとに一定」      Q-Bee の垂直→前 0 で崩れた
```

**組ごとに測って入れる以外にない。**きれいな規則が見えても入れないこと。

現在の値(4 キャラ 18 通り、すべて実測済み):

```
              前→前  後→後  後→前  前→後  垂直→前 垂直→後   (ダッシュ / 攻撃)
ザベル        8 / 0   8 / 0   4 / 4   4 / 4   4 / 4   4 / 4
Q-Bee         6 / 0    -      6 / 0    -      4 / 0    -
レイレイ      9 / 5   9 / 5   6 / 8   6 / 8   6 / 8   6 / 8
ジェダ        7 / 6    -      4 / 9    -      4 / 9    -
```

「-」は後ろ空中ダッシュを持たないキャラ。**ダッシュの入力自体が 4 ティック要る**
ので、Wait を 0〜4 のどれにしても最後のタップは +4。4 より下は測れない。

---

## 4. 落とし穴(踏んだもの)

### ガード行動の出力経路は 2 本ある

```
アーム経路      arm_edge / hs_arm_edge / BLK.edge が立ち、直接送る
リアクティブ経路 counter.sequence を関数の終盤で書く。アームを一切見ない
```

リアクティブ経路を止められるのは `should_perform` の早期 return だけ。
**片方だけ塞いでも止まらない。**Guard Action Frequency を 4 回直して 4 回とも
外した原因がこれ。

### シーケンスの 1 歩目と 2 歩目以降は別機構

```
1 歩目      アーム経路。free-1 の署名で置く。入力リストしか受け取らない
2 歩目以降  service_body。ティック時計で置き、レコードに属性を付けられる
```

**新しくステップの属性を足すときは、必ず両方に配線が要るか確認する。**
これで 3 回踏んだ(ダッシュ攻撃の Auto、free+1 の落ち込み、1 歩目の Hold)。

### 0x02211A への registerexec は重複登録できない

後勝ちで、先のものが黙って捨てられる。ティック単位の処理は
`guardCancel.lua` の既存フックから呼ぶこと。詳細は `VSAV_MEMORY_NOTES.md` の
「2. ティック単位で動く方法」。

### `python - <<'PY'` は バックスラッシュを壊す

`"\n"` や `'\\'` が実 newline になる。**Lua の文字列を含む編集は Edit ツールか、
スクリプトをファイルに書いてから実行**すること。このセッションで 4 回踏んだ。

---

## Hold + ループ + 方向で始まる 1 歩目 (2026-09-19)

**症状:** `Loop Wait : Auto (Landing)` のループで、**二度目のダッシュがいつの間にか
出なくなる。** コードの退行に見えたが違った。**設定**だった。

**原因:** 2 歩目の `Hold` が `forward` を握り続け、ループが 1 歩目のダッシュへ戻る
とき、その方向が**既に倒れている**。`$126` は 1 ティックの押しエッジなので、
**倒れたままの方向には立たない** - 1 回目のタップが存在しないことになり、2 回目
だけが入って「ただの前入力」になる。

```
2 歩目の Hold で forward を握ったまま
  → ループ再開。1 歩目のダッシュは forward から始まる
  → エッジが立たず 1 回目のタップが無効
  → ダッシュにならない
```

**直し方は設定側。** 2 歩目の `Hold : No`。

### なぜ 1 歩目に先頭ニュートラルが無いのか

`M.compile` は方向で始まる列にニュートラルを挟むが、**`i > 1` に限っている。**

```lua
if i > 1 and not _posture and opens_with_direction(inputs) then
	table.insert(inputs, 1, {})
end
```

理由はソースに書いてある - 1 歩目は**アームが置く**もので、起き上がりやガードの
瞬間に対して実測で合わせ込まれており、エントリを足すと全部 1 ティックずれる。

**この免除はループでは根拠を失う。** 同じファイルにこう書いてある。

> the arm only places the pass it was triggered for. **From pass two on, step one
> is delivered by the tick walker like any other step**

2 周目以降の 1 歩目はアームが置かない。**それでも先頭ニュートラルは付かない。**

### 離しはアーム側にある

「ガード後の最速バクステ」は解決済みだが、**入力列ではなくアームに入っている。**

```lua
local NEEDS_RELEASE = {
	["back dash"] = true, ["back dash cancel"] = true,
}
```

> The release is what turns the next back into a press edge. 方向を**既に握って
> いるダッシュだけ**が前に離しを受け取る。前ダッシュで離すとガードが 5 ティック
> 落ちるだけで何も買わない

**controller.lua のダッシュ定義には先頭ニュートラルが無い。** あるのはタップ間の
ニュートラルだけで、先頭を持っているのは**スーパージャンプ** (しゃがみが down を
握っているため、同じ理屈)。

```lua
"back dash"    → { {"back"},    {}, {"back"} }
"forward dash" → { {"forward"}, {}, {"forward"} }
"super jump"   → { {}, {"down"}, {"up"} }      -- 先頭にある
```

`actionSequenceRunner.lua` のコメントが「back dash ... carry a leading neutral」と
書いているのは**不正確**。バックダッシュが持つのは中間のニュートラル。

### 直していない

**実害が小さいと判断** (本人、2026-09-19)。`Hold` を外せば済む。

直すなら「ループ再開時だけ 1 歩目に離しを入れる」形になるが、**1 歩目の lead が
1 増えるため `Auto (Landing)` の予測に効く。** 触るならテストを先に書くこと。

### 教訓

**動かなくなったとき、まず設定を見る。** この件は v11.7.3 でも再現し、そこで
「コードの退行ではない」と分かった。`scripts/` を古い版へ戻す A/B が効いた。


## 5. このセッションで入れたもの(タグ)

```
seq-menu-breadcrumb     見出しを経路に。Start を Wait へ戻す
seq-list-wait           一覧も詳細と同じ語に。1 歩目の Auto (Fastest) を出す
seq-auto-free0          Auto の押しが free+1 に落ちていたのを修正
gc-freq-*  (4 本)       Guard Action Frequency。原因は残りステップの持ち越し
rng-seed-and-labels     乱数の種が未設定。メニュー表記と実装の 35/65 ずれ
seq-hold-charge         Hold 行。タメ技が組めるように
seq-hold-movement       Hold を移動系へ拡大。Order を Wait と同じ操作に
seq-ppp-and-hold-label  PPP / KKK が一度も押されていなかった
seq-air-dash*  (5 本)   Air Dash を分類として追加、実測値 18 通り
action-steps-layout     一覧を Action Steps に。印を 1 つにして列を揃える
v11.5.0-beta            ログのスタンプ
```

`git tag -l` で全部出ます。どこからでも戻れます。

---

## 6. 残件

```
Auto (Chain) / Auto (Cancel)   チェーンと必殺技キャンセル。ROM を読むところから
Counter / Guard トリガへの展開   見出しはトリガから引くので足すだけ
キャラ別必殺技レジストリ          charMoves.lua の移植
Show GC Frequency Counter      Display タブ、既定オフ。不要なら外せる
```

`Auto (Chain)` の手順は空中判定を当てたときと同じ。ROM の該当ハンドラを読む →
オフラインで再生 → 実機 1 回。**推測で入れると、この表と同じ轍を踏みます。**

---

## 7. ベータ版パッケージ

```
VSAV_Training_v11.5.0-beta.zip     426 KB。fbneo フォルダに解凍するだけ
scripts_v11.5.0-beta/              その中身
```

除外したもの:

```
training_settings.json     使用中の設定。ロガー ON のまま。v11.3.3 では実際に混入した
reversal_logs/             204 MB
reversal_logs_archive/      12 MB
macro/last_recording.mis / wizard_temp.mis / */test   作業用の録画
*.fs / *.sav
analysis/                  テスト・解析
```

診断は既定で全部オフ(`knockdown_logger_enable` / `display_gc_freq_counter` が
`false`)。ログを書く経路はすべて `is_enabled()` の後ろ。

**パッケージ本体に対して**テスト 7 本と全 Lua の構文チェックを通してあります。

---

## Tick Data の残件 — 直し忘れではなく判断 (2026-09-07)

四つとも既知で、この形のままにすると決めたもの。**蒸し返す前にここを読むこと。**

**超必の暗転を含む** — `$105` は暗転中も立っているので、ミッドナイトブリスは
`Recovery 312` / `Total 345` (表は 17 / 57) と出る。超必すべてに効く。
**そのままでよい**: この読み出しで一番使うのは Advantage で、そちらは暗転の
影響を受けない。

**飛び道具の Active と Recovery** — 本体が硬直明けした時点で測定が締まるので、
画面端まで飛ぶぶんは入らない。**今の仕様でよい**: Startup は表と合っており、
それが欲しい数字。

**チェーンは 1 つの流れとして出る** — 技ごとに分けるには接触の検出をエッジ以外
にする必要がある (`$05` が `0x02` に入るエッジは、相手が既にやられ中だと立た
ない。詳細は VSAV_MEMORY_NOTES.md)。ガード時の `$158` に相当するヒット時の
信号は未特定。**そのままでよい**: 最後の技だけ出すほうが実態から遠い。

**4096 ステップ付近は未検証** — ハンドオフの `KNOWN_UNVERIFIED.md` から未消化。
手で 4096 歩を作るのは現実的でないので、ダミーデータを流し込む形になる。
→ 2026-09-08 に着手。`analysis/gen_max_steps.py` と下記の計測を参照。

---

## reviewfix3 の取り込み (2026-09-08) — v11.5.3

外部レビュー由来の状態安全パッチ。中身は 5 ファイル、うち 2 つは**表に出る
挙動が変わる**ので、戻したくなったときのためにここに理由を残す。

**旧形式の移行は Save のときだけになった** — キャラ別になる前の (`steps` が
上にある) データを、以前はエディタを開いた瞬間に「そのとき出ているダミー」の
下へ移していた。見るために開いただけ、保存せず閉じた、でも設定は書き換わって
いる。しかも旧形式は誰のものだったか分からないので、移した先が間違っていても
気づけない。開くのは読むだけ (`read_saved`)、移すのは Save だけにした。
`actionSequenceRunner.lua` の `M.schedule` は旧形式を今も nil で返す — つまり
旧形式のシーケンスは、一度エディタで Save するまで動かない。

**ループはリストが読めなくなったら止まる** — 読み直しが nil を返したとき、
以前は手元の周を回し続けていた (「一過性だから」)。だが nil になるのは実際に
は「ダミーが変わった」「保存が消えた」で、そのまま回すとエディタがもう見せて
いない入力を出し続ける。止めて、次のガード機会で武装し直す — メニューを閉じた
あとの挙動 (2026-09-06 のユーザ指示) と同じ形。

残り 3 つは表に出ない安全側の修正。保存先のキャラを開いた時点で固定 / 編集中に
キャラが変わったら保存せず閉じる / ステートロードでエディタも閉じる (これまでは
`show_menu` だけ false になり、エディタは開いたまま残っていた)。

`local editor_character_id` を nil にする経路は `close()` だけ。`M.abort` は
`close_requested` も降ろす — 残すと次に開いた瞬間、触っていない下書きに対して
「閉じますか」が立ち上がり、捨てるものが無いのでその場で閉じてしまう。

テストは `test_editor_ops.lua` の [12] [13]、`test_runner_compile.lua` の
ループの節。4 つの保証それぞれを壊して落ちることを確認済み。**エミュレータ上で
しか見られない分**は下記が残っている。

1. 旧形式を開いて保存せず閉じる → `training_settings.json` が変わらないこと
2. 旧形式を Save → 開いたときのキャラの下にだけ入ること
3. 編集中にダミーを変える → 保存されずに閉じること
4. 編集中にステートロード → 保存されずに閉じること
5. ループ中に保存を無効にする → 前のリストを回し続けないこと
6. 戻して次のガード機会 → 普通に回ること

レビュー側が付けてきた `-reviewfix3` という版名は使わず `v11.5.3` にした。
ログの版番号は「古いバイトコードが動いていないか」を見るためのものなので、
v11.5.2 と区別が付けば足りる。

レンダリング負荷 (`root_items()` が毎フレーム全ステップを作る件) は今回
手つかず。カーソルと無効化の扱いに触るため、別件。

---

## 4096 ステップの負荷 (2026-09-08)

`analysis/gen_max_steps.py` で `training_settings.json` に直接書き込む。ある
ステップを繰り返すだけなので、ゲーム中の挙動は元のステップのまま、**数だけ**が
変わる。実ファイルは `analysis/` に退避される (gitignore 済み、配布物にも入ら
ない)。**FBNeo を閉じてから走らせること** — 終了時に設定を書き戻すので上書き
される。

```
python analysis/gen_max_steps.py show
python analysis/gen_max_steps.py fill --char 1 --count 4096 --template 2
python analysis/gen_max_steps.py restore
```

デスクトップの Lua 5.1 で測った 1 回あたりの時間 (`gui.text` は空関数なので
**描画そのものは入っていない。実機はこれより重い**)。

| steps | guiRegister | registerBefore | 合計/フレーム |
|------:|------------:|---------------:|-------------:|
| 16 | 0.08 ms | 0.15 ms | 0.2 ms |
| 256 | 1.8 ms | 1.8 ms | 3.6 ms |
| 1024 | 7.2 ms | 7.2 ms | 14 ms |
| 4096 | 35 ms | 35 ms | 70 ms |

**きれいに線形** — `root_items()` が毎フレーム全ステップを作るため。60fps の
予算 16.7ms に対し、1024 で使い切り、4096 では 4 倍。エディタを**開いている
間だけ**の話で、閉じていれば 0。

1 回だけの費用は問題にならない。compile 7〜18ms (以後キャッシュ)、dkjson は
679KB を decode 72ms / encode 38ms。壊れるものは無く、遅くなるだけ。

直すなら `root_items()` を画面に映る行だけ作る形にする。カーソル位置と
無効化の扱いに触るので、安全パッチとは分けること。

### 長いリストのスクロール (同日)

4096 歩を積んだあと、最初に出たのがこれ。1 行 4 フレームでは端まで 4 分半、
**積めるが歩けない**リストだった。押しっぱなしの長さ (`state_time`) で 1 回の
移動量が変わる。

| 押している長さ | 1 回あたり |
|---:|---:|
| 〜30f | 1 行 |
| 30f〜 | 2 行 |
| 60f〜 | 8 行 |
| 100f〜 | `max(16, 行数 / 50)` |

最上段だけ**リストの長さに比例**させてある。どんな長さでも 50 回ほどの繰り返し
で渡り切るので、4096 歩と 400 歩がだいたい同じ時間になる。**32 行未満はギアを
付けない** — 一画面のリストで折り返しを取り上げるだけになるため。

**狙うのはタップ、掴むのは移動。** 一度 120 / 200 / 260 まで押し出したことが
あるが、それは「掴むとページ送りになって狙えない」への手当てで、原因は下記の
`state_time` の方だった。直したうえで、最初の速さを残すよう指示 (ユーザ,
2026-09-08)。1 回押せば必ず 1 行なので、狙いはそれで足りる。

### `state_time` は押していない時間も数える

**このスクロールで一度踏んだ罠。** `playerObject.lua` の `state_time` は「今の
状態が何フレーム続いているか」で、**押していない状態も数える**。誰も触っていない
方向は起動からずっと同じ状態なので、何万フレームにもなる。

最初の `held_time` は 2 人ぶんの `state_time` の**大きい方**を、掴んでいるか
どうかを見ずに読んでいた。P2 の触られていない Down が何時間もの長押しとして
読まれ、リストを開いた瞬間から最上段のギアに入る - 1 行ずつ動く機能が丸ごと
消えていた (ユーザ, 2026-09-08)。**掴んでいるプレイヤーの時計だけを読むこと。**

閾値を上げる修正を先に入れてしまったが、症状はそれでは消えなかった。数字を
いじる前に、その数字が何と比べられているかを見ること。上げた閾値は原因が
分かったあとで元に戻した。

走っている最中は端で**止まる** (折り返さない)。一押しの折り返しはそのまま:
1 行目から上を一回で最終行、が長いリストで Save に届く近道になっている。

ギアが効くのはルートのリストだけ。他の画面は数行しかない。

**別途見つけたもの (未修正)**: 12 行の窓の最終行が説明文 (`y = 168`) と重なる
ことがある。ギャップの数で決まる — 実測して確認した。

- 400 歩 (`+ Add Step` あり、ギャップ 3 つ): 最終行 `y = 165`、**3px しか離れて
  いない**
- 4096 歩 (`+ Add Step` は上限で消える、ギャップ 2 つ): 最終行 `y = 159`、重ならない

つまり**上限に達していない長いリストでだけ**起きる。12 行を超えていれば 16 歩
上限のころからある。今回の依頼の外なので触っていない。

---

## Action Route — 技に至る道筋 (2026-09-08)

Tick Data は**1 つの技**を測る。起点は `$105` が立つ瞬間で、そこへ至る道筋は窓の
外にある。ジャンプ中段が「ジャンプしてから何 tick で相手に触れたか」は Tick Data
の 2 行では表せない (ユーザ, 2026-09-08)。

Tick Data のトグルの中で、3 行目として出る。

```
Prejump 2t >  Rise 22t >  Startup 5t >  Hit @30t >  Land 45t >  Recover 96t >  Free @170t
```

区間は差分、**接触と終わりだけ起点からの累計** (`@`)。`Hit @30t` が欲しかった数字。
**ジャンプとダッシュだけ**が対象 — 歩きは道筋を持たず、一歩ごとに行が書き換わる
だけになる (ユーザ判断)。

### 節目と信号 (すべて実測、`analysis/routeProbe.lua`)

| 節目 | 信号 |
|---|---|
| ジャンプの起点 | `$06` が `0x06` になる |
| ダッシュの起点 | `$06` が `0x14` になる |
| 離陸 | `$38` 0 → 非 0 |
| 空中ダッシュ | ジャンプの途中で `$06` が `0x14` に。上昇とダッシュを両方書く |
| 技 | `$105` の立ち上がり。ダッシュ中は**セルポインタが自分のスクリプトを離れた**とき |
| 判定 | セル + `0x0A` の箱 |
| 接触 | 投げ → ブロッククロック → `$05` のエッジ → **P2 の `$5C` の増加** |
| 着地 | `$38` 非 0 → 0 |
| 終点 | `$05` と `$06` がともに 0 |

**ダッシュが始まったティックに動いたものは、何であれダッシュのもの。**ダッシュは
自分で `$105` を立て (実測 flag +0)、それを技と読むと技が起点に乗ってダッシュの
区間が消える。空中ダッシュも同じ形なので、判定は「route の最初のティックか」では
なく「ダッシュが始まったティックか」で行う。

**技は最初の 1 つだけ。**`$10B` はヒットでも動くので、追いかけると錨が最後のヒット
まで歩く。空中チェーンの 2 発目以降は別の問題。

### 実測で分かった限界 — 蒸し返す前にここを読むこと

**ヒットストップ中に次が当たる場合は取れない。**`$5C` は当たるたびに `0x0B` に
戻るが、まだ 0 でないところへ重ねると増加にならない。トレースでは 0 回しか
観測していないので、起きるかどうかも未確認。

**ガード時の `$5C` は未測定。**トレースにガードのサンプルが 1 つも無い
(`$158` が一度も動いていない)。だからブロッククロックを先に見る順序にしてある。
`$5C` を先に見ると、ガードがヒットと出る可能性がある。

**表の数字とは単位が違う。**ジャンプ／ダッシュのページは明示的に「単位はフレーム」。
実測の予備動作は 7 本中 5 本が 4t で、表は 3。turbo 3 で 3 フレーム : 4 tick なので
**換算すれば合う**が、技のフレームデータ表 (tick と一致) と同じようには比べられない。

**中立に戻らないと道筋は伸び続ける。**空中チェーンを続けたトレースでは 1 本が
170t になった。着地で締める案もあるが、着地硬直まで見たいという依頼なので中立まで
待つ形にしてある。

### Tick Data への波及

`$5C` の増加は、**チェーンの 2 発目以降でも接触を拾える**。Tick Data が「チェーンを
技ごとに分けられない」と判断したときの理由がまさに接触のエッジだったので、条件が
変わった。**ただし有利不利は別で、`defender_free` は `$05` が 0 に戻るのを見て
いるためチェーン中は取れない。**接触が取れる = 分割できる、ではない。着手する前に
ここを読むこと。

### 技の開始の信号は 3 回変わった (2026-09-09 に決着)

1. `$106`+`$102`+`$101` — ダッシュ中に**一度も動かない**。何も起きなかった
2. `$10B`+`$1B7` — ダッシュ攻撃 16 本で毎回動いたので採用したが、**16 本とも
   当てていた**。空振りでは動かず、当てると判定の 2 ティック後に動く
3. **セルポインタ `$1C`** — ステッパは `+0x18` ずつしか進めないので、それ以外の
   変化は外からの差し替え = 別の技。**空振りでも当てても同じ**

`analysis/replay_route.lua` で実測ログを流し直せる。空振りのダッシュ小 P 3 本が
`Dash 7t > Startup 2t` / `Dash 5t > Startup 5t` / `Dash 9t > Startup 3t` に
分かれることを確認済み。**画面に出す前に机上で確かめられる**ので、次に測り方を
変えるときもここを通すこと。

---

## ジャンプ攻撃の有利は着地基準 (2026-09-09)

**着地モーションは地上通常技でキャンセルできる。**そこを硬直として数えると、
実際より不利に出る。着地込みでマイナスでも、そこをキャンセルして相手の技を
潰せてしまう (ユーザ)。

なので **`Advantage` の攻撃側の基準を、空中通常技に限り「着地した瞬間」に
する**。技が終わったかどうか (`$105`) ではなく `$38` が 0 に戻ったティック。

### 取り込んだパッチから変えた 2 点

ユーザから `reviewfix5-landing-advantage` のパッチが来て、設計はそのまま採った。
変えたのは 2 つ。

**1. 判定を `$06 == 0x0A` から `0x06 または 0x0A` に広げた。**パッチは自身の
レビュー文書で「FBNeo で確認が要る」と書いていた部分で、**手元のログで確認したら
外れていた**。空中接触 9 回のうち `0x0A` は 1 回だけで、8 回は `0x06` (ジャンプ)。
元の判定だと 9 回中 1 回しか効かない。

**2. 定数をアダプタ側に置いた。**パッチは `tickData.lua` に `0x0A` を直接書いて
いたが、あのモジュールはゲームを知らない純粋な状態機械という約束。アダプタが
`p1.air_normal` という真偽値にして渡す。

落とさなかったもの: **接触した瞬間にだけ latch する**という設計。地上で当てた
あとにジャンプしても、その接触は地上のもの。パッチの要点で、テストにもした。

### 承知の上で残すもの

**3 行目の `Free at` は着地基準ではない。**道筋は「中立に戻るまで」を測るので、
着地モーションも入る。1 行目の `Advantage` と 3 行目の `Free at` は別の質問に
答えている。混乱するようなら 3 行目にも着地の印を足す余地はある。

**空中必殺技は従来どおり。**`0x0E` / `0x10` / `0x12` の着地硬直は本物で、
キャンセルできない。

---

## 道筋は時計になった (2026-09-09)

区間の連なり (`Prejump 3t > Jump 26t > Startup 4t`) をやめて、**出来事とその時刻**
にした。区間の長さは引き算で読む。

```
1t PreJump >  5t Jump >  30t MP >  35t Hit >  53t Landing >  81t Free
1t Dash >  8t LP >  20t Free
1t PreJump >  4t Jump >  30t Dash >  44t HP >  60t Landing >  68t Free
```

**理由が 2 つ。**発生は 1 行目に出ているので繰り返していただけ (ユーザ指摘)。
そして区間モデルは**1 回のジャンプで 2 つ技を出した場合に 1 つしか選べなかった**。
時計なら点が 2 つ増えるだけ。

起点は 1。予備動作の 1 ティック目が `1t`。

### 名前はどこから来るか

どちらも既にツリーの中にあった。

**通常技はボタン**。`$102` が強度 (0/2/4 = 弱中強)、`$101` が 0 なら P、非 0 なら K。
連打キャンセルの解析で判っていた組で、実測でも 6 通りすべて出ている。

**必殺技は `charMoves.lua`**。`$106` から名前を引く表がキャラ全員分あり
(コメントの `PL1 (MO) FF8506` がこのバイト)、`globals.char_moves` として毎フレーム
再構築されている。

### 技が始まった瞬間をどう見るか — 2 通りある

**普通は名前の変化。**表示するものと判定するものを同じにしてある。技の中では
変わらず、技が変われば変わり、**ヒットでは変わらない** (`$10B` が壊れた理由)。
アニメのループでも離陸でも変わらないので、セルポインタが必要とした 3 つの除外が
要らない。

**ダッシュ中だけは名前が変わらない。**`$06` は `0x14` のままで、強度のバイトも
動かない (ダッシュ小 P は前後とも 00/00)。ここだけはセルポインタが自分の
スクリプトを離れた瞬間を技の始まりとし、名前はボタンから取る。除外は「ダッシュに
入った / 空中判定が変わった / 道筋の 1 ティック目」の 3 つ。

**そして「判定が一度も出なければ技ではない」。**アニメはループもするので、
セルの飛びだけでは足りない。デミトリのダッシュがそれで、表では 攻撃前 が `-`
(ダッシュから技が出せない) にもかかわらず `1t Dash > 13t HK` と出ていた
(ユーザ報告 2026-09-09)。名前は強度のバイトが直前の HK を保持していたもの。

判定の有無は**最後に聞く**。判定は技の開始より後に出るもので、その差が発生であり、
見せたい数字そのものだから。アニメで見つけたものだけを検査する — 名前で見つけた
ものは自分でそう言っている。ダッシュコマ投げは名前の側 (状態が 0x14 を抜ける) を
通るので引っかからない。投げに判定は無いので、これで正しい。

### 承知の上で残すもの

**同じボタンを続けて出すと 1 つに見える。**小 P → 小 P の連打キャンセルは鍵が
同じなので 1 回。追及するが優先度は下げる、というユーザ判断 (2026-09-09)。

**着地後の技も並ぶ。**道筋は中立に戻るまでなので、着地してから出した技も点として
入る。

### 接触は 1 技につき 1 回 (2026-09-09)

多段技が行を埋めていた。ダッシュ ES デモンクレイドルで
`132t Hit > 136t Hit > 140t Hit > ... > 160t Hit` と 9 個並び、ほかが読めなく
なった (ユーザ報告)。**欲しいのは「何を何ティックで当てたか」であって、当たり
続けた回数ではない。**回数は 1 行目の Active の並びに出ている。

1 技につき 1 回。**1 道筋につき 1 回ではない** — 技が変われば次の接触もまた出る。
投げも同じで、投げた瞬間だけが出て、そのあとの経過は出ない。

### 投げの最中は技を出さない (同日)

**投げのアニメは投げ。**そして投げはボタンで入るので、そのアニメを技として名づけ
ると「空ジャンプして投げただけ」が
`47t Throw > 51t HP > 104t HP` と出る (ユーザ報告)。掴んだことは行に出ているし、
離すまで他は始められない。相手が投げられ状態 (`$05 = 0x06`) のあいだは技の点を
打たない。

接触も 1 技 1 回のままなので、投げのダメージが入る瞬間の判定も出ない。**投げは
「いつ掴んだか」の 1 点だけ**になる。

**「掴んでいるあいだ」では足りなかった。**蹴り投げは掴んだ相手を先に離すので、
投げ手がまだアニメを続けている最中に `$05` が `0x06` を抜ける。そこから先が技と
読まれ、`158t Throw > 293t MK > 293t Hit` と出た (ユーザ報告)。**一度投げたら、
その道筋ではもう技の点を打たない**形にした。投げの残りは投げの一部。

### 判定はその技のものでなければ数えない (同日)

「判定が一度も出なければ技ではない」は、道筋のどこかに判定があれば通ってしまって
いた。ダッシュ序盤の幽霊が、40 ティック後に出た本物の必殺技の判定で生き延び、
`1t Dash > 13t ?P > 129t Dash DP` のように出ていた (`?P` は強度のバイトが
想定外の値だった印)。**判定はそのとき出ている技に印を付ける**形に変えた。

### 隙間は 15 ティックまで許す (2026-09-09)

ザベルのダッシュコマ投げは、ダッシュから抜ける途中で一度中立を通る。最初の中立で
締めると投げが行から落ち、**ダッシュが止まっただけ**に見えていた (ユーザ報告)。
中立になっても 15 ティックは道筋を開けたままにし、その間に何か起きれば続ける。
**終点は最初に中立になったティックのまま**なので、待ったぶんは数字に足されない。

**`GAP_TICKS = 15` はこのプロジェクトが普段は入れないティック数の定数。**その名前で
指示があったこと、そして「この間は前の行動の続き」と言っている RAM が無いこと
(どちらの場合も状態はただの中立) が理由。変えるならこの 1 行。

**副作用: 15 ティック以内に続けて出した行動は 1 本にまとまる。**実測ログを流し直す
と、ジャンプの着地からすぐダッシュした 2 本が
`1t PreJump > 5t Jump > 44t Landing > 72t Dash > 77t LP > 90t Free` の 1 本になる。
道筋の本数は 35 → 21 に減った。狙いどおりだが、承知して使うこと。

### 必殺技も道筋を始められる (2026-09-09)

ジャンプとダッシュだけでなく、`$06` が `0x0E` / `0x10` / `0x12` になったときも
道筋が始まる。必殺技はそれ自体が 1 つの行動なので、ジャンプやダッシュを経由する
必要がない。この場合は開始のラベルを書かない — 技の名前が同じティックに乗るので、
そちらの方がよく言い表している。

```
1t Soul Fist >  14t Hit >  41t Free
```

### 離陸の印は `Air` (2026-09-09)

`Jump` と書いていたが、この出来事の意味は「足が地面を離れた」で、ジャンプは
その一つの手段にすぎない。**サスカッチの地上ダッシュは表で 浮遊 タイプ** (`空通`
= 空中通常技が出せる) で、1 ティック目から空中判定なので
`1t Dash > 1t Jump` と出ていた (ユーザ報告)。`Air` なら原因を決めつけずに事実を
言うだけで、`PreJump` の後に来ても読める。

```
1t PreJump >  4t Air >  30t Dash >  44t HP >  60t Landing >  68t Free
```

### どの行動でも道筋が始まる (2026-09-09)

ジャンプ・ダッシュ・必殺技だけだったのを、**行動なら何でも**に広げた。`$06` が
`0x06` ジャンプ / `0x0A` 通常技 / `0x0E`・`0x10`・`0x12` 必殺技・ES・EX /
`0x14` ダッシュ / `0x16` ダークフォース になったとき。

外したもの: `0x00` 待機 と `0x04` 自由になる直前 は他の行動の尻尾、`0x02` やられ
と `0x0C` ガードは**やられた方**、`0x08` は着地の局面でしか出なかった。

**歩きはそもそも `$06` を動かさない** (`0x00` のまま) ので、一歩ごとに行が
書き換わることはない。ジャンプとダッシュに絞った当初の理由は、実は成り立って
いなかったことになる。

立ち通常技だけでも道筋になり、チェーンは隙間の許容で 1 本に並ぶ。

```
1t MP >  7t Hit >  31t Free
1t LP >  6t Hit >  11t LK >  16t Hit >  41t Free
```

### ヒットとガードは `$140` で分ける (同日)

ブロッククロック `$158` は**どのトレースでも一度も動かなかった**ので、ガードが
ヒットストップ側に落ちて `Hit` と出ていた (ユーザ報告)。`$140` は相手の復帰の
種別で、ガード中は `0x02` か `0x12`、ヒット硬直中は `0x00` か `0x04`
(ROM の `0x02395A` が 地上ガード で 2 を書く)。

**接触したかどうか**と**それが何だったか**を分けた。`$158` は前者の合図としては
残してある — 他が動かないティックに動くことがあるため。名前は `$140` が決める。

### 歩きとしゃがみはレバーから読む (2026-09-09)

**`$06` は歩いてもしゃがんでも `0x00` のまま**なので、状態からは分からない。
向き補正後のレバー `$122` を、**自由かつ地上のあいだだけ**読む — 技の最中に前を
握っていても歩きではない。

```
向き ($0B) 0    前 = 0x02   後 = 0x01
向き ($0B) 1    前 = 0x01   後 = 0x02
共通            下 = 0x04
```

下が優先。しゃがみ前はしゃがみ。`Walk` / `Back` / `Crouch` の 3 つ。

**歩いているあいだは道筋を締めない。**`$05` も `$06` も 0 なので、そのままだと
15 ティックで自分の道筋を締めてしまう。「行動できる」ではなく「立ち止まって
いる」で締める。

```
1t Walk >  21t MP >  27t Hit >  51t Free
1t Crouch >  11t Walk >  31t Free
```

`analysis/replay_route.lua` ではレバーを再現できない (probe が記録していない)。
歩きを机上で確かめたいなら probe に `$122` と `$0B` を足すこと。

### 1 行目はチェーンを 1 つとして測ったまま (同日)

3 行目が技ごとに並ぶようになっても、1 行目は変えない。**チェーンは硬直を
キャンセルして繋ぐものなので、技ごとの持続や硬直を出す意味がない** (ユーザ判断
2026-09-09)。1 行目のチェーン全体の有利と、3 行目の技ごとの時刻で足りている。

### 規則どうしがぶつかっていた (2026-09-09)

3 つ報告が来て、2 つは自分で入れた規則の衝突だった。**片方ずつ足したときに、
もう片方への影響を見ていなかった。**

**投げが消える。**「接触は 1 技 1 回」と「投げの最中は技を出さない」を別々に
入れた結果、小技をガードさせてからの投げで、投げ自身の技の点が抑えられ →
`contact_done` が下りず → 掴みも出ない、という連鎖になっていた。**掴みは「同じ技が
また当たった」ではない**ので、1 技 1 回の対象から外した。

**投げから始まる道筋が無い。**掴みは相手側 (`$05 = 0x06`) で見えるが、投げ手の
状態が行動になるのを待っていたので、立ちからの投げが道筋を開けなかった。掴みでも
開けるようにした。

**チェーンの初段だけ Hit になる。**`$140` は `$05` がやられに入るのと同じ
ティックに書かれるとは限らない。最後の接触を 1 ティックだけ保留し、ガードだと
分かったら直す。**当たったものが後からガードに変わることはない**ので、この直しは
取りこぼしを拾うだけで、正しいものを壊さない。

### 歩きとしゃがみ — 番地を間違えていた (2026-09-09)

**`$122` はボタンだった。**トレース 859 行すべて 00 で、歩いても何も出ないのは
当然だった。`$06` が動いているのでは、という当初の見立ても外れ。

**`inputHistory.lua` が地図を持っていた。**しかも実測付きで、隣の `$123` を使わない
理由まで書いてある (片方の向きで毎フレーム左右が入れ替わる。変化率 20.3% 対 4.2%、
再現精度 83% 対 99.9%)。**手を入れる前に既存スクリプトを洗う、を守れていなかった。**

```
+0x122  ボタン
+0x125  レバー: bit0 前, bit1 後, bit2 下
```

ビットは**向きに対する相対**なので、前後だけ知りたいこちらは向きを見る必要が無い。
`inputHistory` は `registerBefore` で読んでいるので、同じ時点で値がある。

## `After` の数値化は保留 — 直し忘れではなく判断 (2026-09-20)

`Wait` の選択画面は、ダッシュの後だけ `Fastest (11)` と**解決した値**を出す。
それ以外は `After` としか出ず、押すまで何ティック待つのか分からない。
**「通常技の後だけでも数を出せないか」を調べ、保留にした** (本人の判断)。

### 静的な予測は届かない

`M.auto_ticks_for` が数を返すのは**ダッシュ絡みの 3 つだけ**。地上ダッシュの後、
空中ダッシュの後、ジャンプから空中ダッシュへ。それ以外は `nil` を返し、
`After` は**数ではなく状態を見て**動く。実行に数は要らない - 要るのは表示だけ。

通常技に限っても 2 つとも塞がっている。

- **技の長さの表がツールのどこにも無い。** 作るなら 16 キャラ × 6 ボタン ×
  3 姿勢 = 約 288 件
- **1 件では足りない。** `After` はダミーが自由になるまで待つので、**当たれば
  ダミーのヒットストップぶん延びる** (実測で `Hitfreeze 24t` を観測)。
  空振り・ガード・ヒットで別の値になる

ゲームから読む案 (④ と同じ発想) も、**編集画面の時点でその技は再生されていない**
ため、キャラごとの技のセルスクリプト先頭アドレスが要る。① より重い。

### 実測値なら既に取れている

`log_wait` が各ステップの待ちを記録し、`Show Step Wait Ticks` が
`Step.2 Wait:13 Act:1` として出している。**ただし表示専用で、ループ再開のたびに
捨てられる** (`M.wait_log = {}`)。

(キャラ, 前のステップ, このステップ) で保持すれば編集画面に出せる。この形なら
**ヒットストップの分岐が自動的に解け**、通常技に限る必要も無くなる - 1 回走らせた
後なら必殺技でも投げでも数が出る。

### それでも保留にした理由

**`Fastest (11)` と `After (11)` の違いが利用者に伝わらない。** 前者は測ってある
定数、後者は「前回こうなった」。括弧の中を見ても区別がつかず、**数字が出ている
以上「その数で動く」と読まれる。** 実際には状況で変わる。

> 利用者にわかりやすくできる気がしない (本人、2026-09-20)

**誤解させる情報は出さないほうがまし**という判断。蒸し返すなら、まず
**表示の区別のつけ方**から決めること。数を出す手段は上記のとおり既にある。
## 着地待ちの引き継ぎ更新 (2026-09-20、Codex)

下の「未解決」は古い調査記録。ユーザ提供の Claude Code ログでは、空中の
`timing_missed` が `toLand=6` で開き、`lead=3` の予測より早く出していたと確定。
予測が返るあいだ期限を抑える修正後、モリガン・サスカッチ・跳ばない並びの
①②③はユーザが実機確認済み。その修正は作業ツリーにあり、維持している。

追加症状はダウン後のダッシュ失敗で Landing ループが止まること。
`scripts/reversal_logs_archive/2026-09-20_before_grounded_landing_fix/kd_c05_s02.json`
の f3536..3706 で、P2 は地上なのに
`y=2621440 (40), vy=-135168, ay=-24576, floor=40` が残っている。
f3537 に通常技へ入り、f3560 に `$05/$06/$38/$5C` がすべて 0 へ戻るが、
物理予測はこの地上の残留値に対して **1 を返し続ける**。予測が nil でなければ
期限を抑える条件が、地上の硬直明けまで塞いでいた。

**地上の期限救済の変更は撤回済み。** Landing の期限抑制に
地上判定を加え、同時に `air_seen` の local 宣言を cancel より前へ移して arm でも
初期化する変更を試した。オフライン35本は通ったが、ユーザからモリガンの
ダッシュ中K連発が壊れたと報告を受けた。
実装と追加テストを今回の作業開始時点へ戻した。元からあった Claude の
「予測が nil でなければ期限を抑える」修正・計器は残している。

その後、ユーザが **After で試していた** と確認し、Landing に戻したら正しく動くと
報告した。最初の変更が連発を壊したと確定したわけではない。直前6件は land_gate
が0件で、設定も -1 だった。Landingへ変更後の6件では38回すべてが
landing_ready / lead=3 / toLand=3。kd_c05_s07 のメモリでもダッシュ→空中MKを9回確認。
ただしこの時点では地上の期限救済を再投入していなかった。続きは下記。

### After が空中で地上ダッシュを出す問題 (同日)

ユーザ指摘：「After は操作可能になるまで待つ機能のはず」。実装の dummy_free は
空中なら空中攻撃の受付を見ており、地上ダッシュにも同じ判定を使っていた。
コンパイル結果に既存の GROUND_DASH 分類を残し、`step.auto` の枝だけで地上を
要求する修正を追加。着地後も元の `$05/$06 == 0` を待つ。空中ダッシュと地上
ダッシュは入力文字列が同じなので、motion から区別してはいけない。

Landing の分岐・共有の dummy_free・期限は変更なし。既存35本と
`analysis/test_after_ground_dash.lua` が通過。地上ダッシュ5種について通常のステップと
ループの両方、空中攻撃・空中ダッシュ、Landingの先行入力を確認。
Afterの挙動と「次の行動で待ち条件を判断する」仕様はユーザが実機確認・了承済み。
動作確認済みLanding版のソースと設定は
`local_checkpoints/20260920_landing_confirmed/` に保存した。

### 立ち中Kが漏れた後は、地上の硬直明けでLanding待ちを救済 (同日)

ユーザ了承：「ダッシュ中Kが失敗して立ち中Kになったら、その技が終わり、地上で
行動可能になった時点で次のステップへ進む」。Landingの期限判定に
`$38 == 0` の場合も救済する条件を追加した。既存の busy→free 判定は残し、
立ち中Kの途中や、直前の入力がまだ技になっていない瞬間には先へ進まない。
空中の着地予測、Afterの分岐、air_seenの扱いは変更しない。

`test_landing_ground_recovery.lua` で実測の残留物理値を本物の予測関数へ渡した。
修正前はステップ内・ループ両方で硬直明けに止まり、修正後はそのティックで次の
入力を開始する。モリガンとサスカッチの空中の早出し防止・凍結待ち・予測での
先行入力、跳ばない並びの救済も検証。計37本通過。
**ユーザが実機で「救済されるようになりました」と確認済み。**
Afterの修正、通常のLanding連発、立ち中Kが漏れた後の地上復帰による救済について、
それぞれのユーザ確認を得た。この件は解決済みとして扱う。

修正直前のAfter・Landing確認済みソースと設定は
`local_checkpoints/20260920_after_and_landing_confirmed/` に保存した。

## キャラ選択に戻ると Action Pattern のループが回る (2026-09-21、2 段階で解決、実機確認済み)

症状: 試合からキャラ選択へ戻っても、Guard Action 0xC のパターンが
再生され続けた。

### 1 段階目 (match_live) — 不十分だった

ループの補充 (loop_refill) は「キューが空なら次の周を積む」だけで、今が
試合中かを聞いていなかった。actionSequenceRunner.lua に match_live() を
足し、globals.match_running (変身中も落ちない「本当に試合中」の 1 定義) が
false のあいだは補充も走行中の周の配送も止めた。

**実機で止まらなかった。** 理由は届き方の前提。service は guardCancel の
0x02211A フックから呼ばれ、その命令が選択画面で実行される保証は無い。
さらに globals.dummy は選択画面で更新されない (マスタースクリプトが
ミラーリングの枝で早期リターンするため、最終試合フレームの stale オブ
ジェクトのまま)。ランナー側のゲートが走らない场景でも、loop_sched と
loop_which は生き残り、次の試合で勝手に再開し得た。

### 2 段階目 (シーン 2 の毎フレームキャンセル) — 解決

マスタースクリプトの registerbefore 冒頭で、シーン 2 (0xFF8009 == 2 =
キャラ選択。ミラーリングが同じ信号を使うので実機で動作保証済み) の
あいだ毎フレーム:

- actionSequenceRunnerModule.cancel() (ループ状態ごと落とす)
- player_objects[2].pending_input_sequence = nil (cancel が届かない
  配信スロットの半分)

フレーム側が先に走るので、そのフレームのティックフックは空の状態から
始まる。savestate ロードの後片付けと同じ形。

### 残すもの

- ランナー側の match_live ゲートも残す (dummy が生きている移行窓の
  防御)。match_running が無い環境では「生きている」扱い = テスト互換。
- M.loop_alive() を追加 (診断が 生/死 を聞けるようにしただけ)。
- テスト [11] を test_pattern_run.lua に。ランナー側の契約に加え、
  マスタースクリプトの「シーン 2 の門の中で cancel している」「配信
  スロットも空にしている」をソース契約で固定。既存 28 本 + ルート 10 本
  通過。**ユーザが実機で確認済み (2026-09-21)。**


## Dummy タブへの改名と Position 行の近道 (2026-09-21、実機確認済み)

**タブ名**: Player タブは Dummy タブに改名した。中身は Position/Pose/
Wakeup/Tech Throws/Guard/Guard Action Type/Action Patterns とすべて
ダミーの挙動で、Player は誤称だった (ユーザ、2026-09-21)。コメント、
テストの名指し、設計ドキュメント、RELEASE_NOTES の該当行も同じく
直した。過去の RELEASE_NOTES もユーザ指示で直してある (履歴より
表記の一致を優先)。

**Position 行の近道**: 行は左/右で値を直接変え、配置は値が変わった
ときだけ適用される (position.lua の mode ~= last_applied)。だから
同じ位置に戻すには別の項目を挟んで 2 回変えるしかなかった。

    LP = 現在の設定値で再配置 (値は変えない、メニュー開いたまま)
    HP = 再配置 + メニューを閉じる (トグルの正式な閉じ経路)
    MP = 既定値 (Off) へのリセット (現状維持)
    左/右 = 配置の選択 (従来どおり)
    Off のとき LP/HP は何もしない

実装は position.lua の reapply() (Off・スライド走行中・ラウンド未検出
では false) と menu.lua の HP ディスパッチ (新規。MP/LK と同じ形)。
LP はリスト行に未割り当て、HP はメニュー全体で未使用だったので衝突
しなかった。行の凡例と説明文も更新済み。

テスト: test_position_row.lua (reapply のゲート順序、壁配置でのスライド
駆動、menu 配線のソース契約、タブ名と行の所在)。既存 29 本 + ルート
10 本、合計 39 本通過。**ユーザが実機で確認済み。**


## 起き上がりのダッシュが向きの反転で消える (2026-09-20、実機確認済み)症状: Guard Action 12 (`counter_attack_stick_dummy = forward dash`、button2 あり)
の起き上がりリバーサルで、**ダウン中に相手と逆向きだったダウン**のとき
ダッシュが出ない。設定のボタンが単独で通常技になる (free+2..+3 に 0x0A) か、
何も出ない。ボタンなし設定なら常に「無し」。反対向きでなかったダウンでは
常に出る。

測定 (`analysis/read_wakeup_dash.py`、kd_c05_s01..s16 と kd_c0A_s19..s37、
kd_c0F_s08..s11 の 35 ウェイクアップ。ログは
`reversal_logs_archive/v11.5.3/`):

```
defer の解決時と press の配送ティックで向きが食い違う → 失敗 15/15
   (0x0A が 11、無し 4。dash_facing の fii で判定)
食い違わない                           → ダッシュ 18/18
残り 2 件の「無し」は free+1 で再ヒット (st=2, air=255)。
   ユーザの攻撃が起き上がりを食ったもので、ツールの失敗ではない。
```

反転が free の後に来ても、defer より前に済んでいれば成功する
(kd_c05_s04: $b は +0 で反転、press の捕获時にすでに新向きだったので成功)。
反転は行 (rows) では free のフレームに現れるが、マークでは 1 ティック早く
見えることがある (フレーム境界とリワインドの違い)。配送側は各ティックで
比較するので、どちらの見え方でも正しく扱える。

### 原因

- **タップ 1 は壊れない。** entry 1 は各ティックで `entry_to_bits` が解決するので、
  そのティックの `$b` に対して正しい。補正後は「前」になる。
- **最後の押しだけが反転に跨る。** free-1 の署名が生ビットを捕获して
  (`press_defer`)、free+0/+1 へ配送する。0x022194 のスワップは**配送ティックの
  `$b`** で効くので、反転後には捕获済みの生ビットが逆方向を意味する。
  1 ダッシュの 2 タップが「前/後」に分かれ、ダッシュにならない。
- ボタンは別経路で入るので、ダッシュが無いままボタンだけが通常技になる。

### 直し (guardCancel.lua、作業ツリー)

- 両 defer 箇所 (署名と Q-Bee の nosig) で `fast_press_fref = facing_for_input()`
  を保存する。
- `press_now` の配送で、参照が変わっていれば `swap_facing_bits()` でレバー
  bit0/1 を入れ替えてから assert する。`press_now` のマークは実際に assert した
  値を出す。
- `swap_facing_bits` は算術実装 (`bor8` と同じ理由)。0 / 3 / down / up は不変で、
  down+forward のような組合せも bit0/1 だけが入れ替わる。
- 触っていないもの: タップ、prefix、ダッシュキャンセル (`fast_press_rev`)、
  Hold (`fast_press_mlev`)、即時押し (`_off == 0`)。キャンセルと Hold も
  捕获してから配送する構造は同じだが、**反転を跨る実測がない**ので入れていない。
  起き上がりで dashc を使って失敗する報告が来たら、同じ形で直すこと。

### 確認 (2026-09-20)

実機で再起動のうえ確認、ユーザ報告「なおりました」。修正後バッチ
(`reversal_logs_archive/v11.7.6-wip/`、スタンプ v11.7.6-wip) の
**35 ウェイクアップが 35/35 すべてダッシュ (0x14)。0x0A と説明の付かない
「無し」は 0 件。** 修正前バッチ (同日の v11.5.3 スタンプ分) は
0x0A が 11、無しが 4 残っていて、版を混ぜずに版ごとに数えないと
成功率が平均化されて見えることに注意。

再ヒット (free+1 で st=2, air=255) は相手の攻撃しだいなので対象外。
同じ家族として、ガード／ヒット硬直のリバーサルで gc_delay の途中に
すれ違いが起きたときも、以前は押しが逆に出ていたはずで、同じ仕組みで
正しい向きに直る (こちらは実測なしの推定)。判定はコード側で:

```bash
cd C:/fightcaVSAV-Debug/emulator/fbneo/analysis; $env:PYTHONIOENCODING='utf-8'; python read_wakeup_dash.py
```

`first=14` 以外の行が出なければ、全ウェイクアップでダッシュが出ている。
(マークを印字するのは失敗側だけなので、成功は行が出ない。)

オフライン: `test_wakeup_facing.lua` を新規。既存 28 本 + ルート 10 本、
合計 38 本すべて通過。

## モリガンのダッシュ連発 — 未解決 (2026-09-20、以下は更新前の調査記録)

**症状.** `Dash Forward (Hold)` → `Attack Forward + MK`、`Loop Wait : Auto (Landing)`。
**MK をガードされた次の周だけダッシュが出ず、立ち中 K になる。** 空振りなら成立する。
サスカッチのダッシュ HP 連発でも同じ形が出る。

```
成功  212t MK > 231t Landing > 234t Crouch > 235t Walk > 237t Dash
失敗   42t MK >  49t Guard >  72t Landing > 81t Crouch > 82t MK
                  ↑ ガードされた           ↑ ダッシュが無い
```

失敗周は `Landing` から次の行動まで **9 ティック**空く。着地硬直が丸ごと再生されて
いる = **ダッシュが着地をキャンセルできていない。**

### 測って分かっていること

**配送中に凍結は起きていない。** ダッシュのステップを配るあいだ `$5C` は全ティック 0。
凍結は**その前に当たった MK のもの**で、配り始める頃には明けている。

```
lg 123  e1  seq_land=1  saw_freeze=0  air=1  $5C=0  toLand=6
lg 127  e3  seq_land=1  saw_freeze=0  air=1  $5C=0  toLand=2   <- 最後の押しがここ
```

**最後のタップが着地の 2 ティック前、空中で出ている。** 着地ガード
(`guardCancel.lua`、`_s0.seq_land and _s0.saw_freeze and _i == #_s0.sequence`) は
`saw_freeze` を要求するので**一度も発動しない。**

### 見立て

**`saw_freeze` は代理の条件。** 本当に聞きたいのは「そのまま出すと着地より早く
着弾するか」。ここでは**スケジュールをずらした凍結が前のステップの接触によるもの**
なので、「配送中の凍結」しか見ない `saw_freeze` では拾えない。

**正しい問いは「アンカー以降に凍結があったか」**であって「配送中に凍結があったか」
ではない。

### 試して外した 4 手 — 同じ道を通らないこと

| 手 | 結果 |
|---|---|
| `saw_freeze` の代わりに `ticks_to_landing() > 0` を直接聞く | **サスカッチが壊れた** (`LW:240` 再発)。着地ぴったりを狙った押しも「まだ落下中」なので全部待たせてしまう |
| 最初の凍結ティックで `$5C > 残り窓` なら入れ直す | 着地ステップを着地ガードから奪う。モリガンは直らず、**サスカッチが壊れた** |
| 入れ直しの条件に `saw_freeze` を足して広げる | 入れ直しは発動したが**出ない。**入れ直した後の配送は成功周と同形なのに歩きになる |
| `loop_refill` を凍結中は走らせない | 凍結は refill より**後**に始まるので、ゲートを素通りする |

上の 2 番目に `not _s0.seq_land` の除外を足したところ、**元からある入れ直しガードまで
止めてしまいサスカッチが壊れた。** 除外は今日足したガードにだけ必要なもので、元の
ガードに付けてはいけない。

### 次にやるなら

1. **ステップが配り始めた時刻と、スケジュールが想定した時刻の差を実測する。**
   推測せず、ずれの大きさを数字で出す
2. そのずれが直前の接触の凍結ぶんと一致するか確かめる
3. 一致するなら、**アンカー以降の凍結**を `saw_freeze` とは別の印として持たせる
4. **サスカッチとモリガンの両方**で測る。片方だけで判断しない

**計器は残してある** (`dash_facing` / `seq_frz` / `seq_restart` / `face_hold`、
Knockdown Logger が ON のときだけ記録)。読み出しは `analysis/read_dash_facing.py`。
ログは `scripts/reversal_logs_archive/2026-09-20_morrigan_run1..5` と
`2026-09-20_dash_facing_run1..3`。

### この日の教訓

**同じ場所で 7 回外した。** うち 2 回は自分が入れたものが原因で、1 回は動いていた
ものを壊した。原因は 3 つとも同じ - **測る前に直した**こと。

読み違いも 3 件あった。**P1 の `$06` (`0xFF8406`) を P2 だと思って読んだ**、
**`$06 = 0x04` を歩きだと思った** (実際は「自由になる直前」で、`0x00` の直前にしか
出ない)、**`$122` を読んでレバーのつもりだった** (68000 はビッグエンディアンなので
ボタン。レバーは `$123`)。**番地とエンディアンを確かめてから結論を出すこと。**
## Auto (Chain) が空中で出ない (2026-09-24、直し済み・実機未確認)

`chain_ready` (`actionSequenceRunner.lua`) が `$06 == 0x0A` を条件にしていた。
地上の通常技は `0x0274CE` が `$04..$07` へ `02 00 0A 00` を書くのでそれで合うが、
**空中では `$06` がジャンプの `0x06` のまま**になる。実測 13 件のうち 12 件が
`0x06`、`0x0A` は 1 件だけ (`VSAV_MEMORY_NOTES` の「空中で当てた瞬間の `$06` は
0x06 が多数派」、2026-09-09)。

**`$06` は ROM の条件ではない。** `0x028E42` が見るのは `$39` / `$1B2` /
`$12E` (ラッチされたボタン語) / セルのフラグで、`$06` も `$38` も見ていない。
ランナー側が「通常技が出ているか」の代わりに置いたもの。

直しは `0x0A` と `0x06` の両方を通す。接触 `$39` が上にあるので、攻撃が出て
いなければそもそも成立せず、広げても緩くならない。
`analysis/test_chain_air.lua` が地上・空中・広げすぎの 3 方向を見る。
**実機で空中チェーンが出ることを確認済み (2026-09-24)。**

**`cancel_ready` に同じ行があった** (本人の指摘、同日)。キャンセルの許可は
`$167` / `$168` と `$119` で、ROM はここでも `$06` を見ていない。同じように
両方を通した。Auto Cancel / Late Cancel とも同じテストで見ている。

**2 度目の修正: `$06` は条件ごと外した (同日)。** `0x06` を足しただけでは
足りなかった。ダッシュ攻撃からのキャンセルが `?act14` で止まり、`$06` の実測値が
4 種類そろった。

| 値 | 何 | 実測 |
|---|---|---:|
| `0x06` | ジャンプ | 空中接触 13 件中 12 |
| `0x14` | ダッシュ | 地上接触 12 件中 7 |
| `0x00` | — | 地上接触 12 件中 5 |
| `0x0A` | 通常技 | 空中接触 13 件中 1 |

**`0x0A` はどこでも少数派。**接触ティックの `$06` は「そのキャラがその時
やっていたこと」であって、技の種類ではない。ROM が見ていない値で門番をしたのが
最初から間違いだった。接触 `$39` とキャンセル窓 `$167`/`$168`/`$119` が別に
効くので、外しても緩くならない。

**`Show Step Wait Ticks` に理由を出すようにした。**ゲートが最後まで待っていた
条件が `?hit` / `?inhib` / `?win9` のように付く。締切で出たときだけ付き、
成立したステップには何も付かない。これが無ければ `?act14` は見つからなかった。

**`rapid_ready` の `$06 == 0x0A` は残す (本人の判断、同日)。**連打キャンセルは
**通常技キャンセル通常技**なので、「今出ているのが素の通常技か」はその機能自体の
条件になる。

**聞かれる瞬間が違うことも効いている。**chain と cancel は接触を要求するので
**接触ティック**で評価され、そこでの `$06` は「そのキャラがその時やっていたこと」
(ジャンプ / ダッシュ / 0x00) になる。`rapid_ready` は接触を取らず、**その技自身の
セル窓**で評価される — `0x0274CE` が `0x0A` を書いた区間の中。上の実測は接触
ティックのものなので、そのままは当てはまらない。

**ただしセル窓を通しての `$06` は誰も測っていない。**確認済みではなく「反証が
無い」状態。**類推で外さないこと。**

## 空中ガードの反撃表示 — 提案が通った段階 (2026-09-24)

本人の指定は**案 A の読み方 (b)**。可否を断定せず、窓の長さを出す。

```
Air Guard   free 6t   land 11t   Adv +4t   Air 5t / Ground 0t
```

**追加要求: 速すぎてガード硬直中に押してしまった場合も分かること。**
硬直中の押しは通らないので、「反撃できなかった」のか「早すぎた」のかが
区別できる必要がある。

着手前に測ること (どれも未確認):

1. 空中ガード中の `$140`。地上ガードは `0x02` で、ROM のコメントも「地上ガード」
   (`0x02395A`)。メモの「ガード中は `0x02` か `0x12`」の `0x12` が空中かは未確認
2. 空中ガードで `$158` (GC 受付 14) が入るか。入れているのは地上ガードの経路
   (`0x023960`)。入るなら空中ガードキャンセルが存在する
3. 空中ガード硬直は着地前に明けるか
4. `tickData.lua` の `p2_became_free` (`$05`) が空中ガードでも立つか
## キャラ選択の「選んだか」— latch を外して振り出しに戻した (2026-09-24)

v11.7.12 で入れた「攻撃ボタンの押下エッジで latch する」回避策を**外した**。
**3 回続けて別の不具合を生んだ。**

| 回 | 誤り | 症状 |
|---|---|---|
| 1 | latch を落とすのが `char_chosen` の中。呼ぶのはシーン 2 の中だけなので**到達不能** | 2 回目のキャラ選択で 2P が操作不可 |
| 2 | latch を 2P にも効かせた。ミラーが P1 の**押しっぱなしの決定ボタン**を 2P へ OR する | 2P が選んでいないのに「選んだ」。押しの長さ次第で出たり出なかったり |
| 3 | 「押した」だけで立つので、**受付が始まる前の押し**でも立つ | 1P が選んでいないのにミラーが走り、**1 本のスティックで両方のカーソルが動く** |

3 回目はカーソル (`base + 0x03`) がバレッタのときだけに絞ったが、**受付前は
カーソルバイトも 0** なので同じ値になり、すり抜ける。

**共通の誤りは「ボタンが押された」を「このプレイヤーが決定した」の代わりに
使ったこと。**ゲーム自身の「決定した」信号は見つかっていない。`$380..$3FF` は
4 セッション走査済みで該当なし。

### 今どうなっているか

`char_chosen` は `$3BD` と `$3E1` を読むだけ (v11.7.12 以前と同じ)。
**バレッタを LP で決定した場合だけ答えられない**が、外し方は安全側 —
ミラーが止まらないだけで、暴走はしない。

### 解決 — `$04` で答える (同日、測定から)

決定の押下エッジで両プレイヤーブロック 0x400 バイトを控え、10 フレーム後の差分を
取った。**バレッタ + LP で動いたのは `$04` (02→04) と `$05` (06→00) だけ**。続けて
`$04`/`$05` を変化のたびに記録し、6 回の選択画面で局面の表が取れた
(`VSAV_MEMORY_NOTES` 冒頭)。

- バレッタ + LP でも `$04` が `04` になる → 元の不具合が直る
- 受付前の押しでは `$04` は `00` のまま → 3 回目の不具合が起きない
- `$3BD` は前の試合の値が残る → `$3BD` で答えていたこと自体が誤り

**`char_chosen` は `$04 ~= 0` だけ。状態を持たない。**
「押した」ではなく「ゲームが決定を受け付けた」を読むので、上の 3 回の不具合は
どれも構造的に起きない。

**教訓: ここは 4 回推測して 4 回外した** (09-23 の 3 仮説 + 今日の latch)。
測ったら 1 回で決まった。
