# PB 採点の再設計 (保留) — 設計メモ

2026-09-21 時点。**実装はしていない。** 次に PB 練習の採点をしたくなった
とき、この 1 枚から始める。既存の Show PB Stats を置き換える案。

---

## 何が問題か — 現状の Show PB Stats は強さに結びつかない

現状の定義 (playerObject.lua:217-235、hud.lua:288-300):

```
Total  = 攻撃が P1 に触れた回数 (0xFF8405 == 2 の立ち上がり。ブロックでも被弾でも)
Pass   = P1 のテックが成立した回数
Fail   = Total - Pass
```

**問題 1 — 毎回押し続けるほど数字が上がる。** 分母が「触れた回数」なので、
必要かどうかに関係なく全タッチで押せば押すほど Pass% が上がる。PB を
押さえるという選択 (ただガードして反撃を待つ) は全部 Fail に落ちる。
**無理にでもボタンを押したほうが成績が良くなるが、それは強さに結びつかない**
(ユーザ、2026-09-21)。

**問題 2 — 被弾と普通ガードが同じ Fail。** 「押したのに出なかった」と
「ガードすら漏れて食らった」は重さが全然違うのに、現状は同じ数え方。

**問題 3 — フレームベース。** playerObject.lua:195-201 に作者自身の
既知課題がある: 「rawStateService.lua のティックが真実で、そちらで作るべき」。

**怪しい信号 (実装時に突き合わせること)。** 現行の Pass は
`p1_tech_hit = readword(0xFF8800 + 0x1B0)` (dummyState.lua:739) —
**P2 側オブジェクトの $1B0** を読んでいる。P1 自身の成立フラグは
`$171` (0xFF8471、dummyState.lua:745 に `p1_is_tech_hit` として既存)。
どちらが正の成功信号か、実装時に実測で確定してから使うこと。

---

## 思想 — 上手な PB の分解 (ユーザ、2026-09-21)

1. **狙った時に 100% 出せる**
2. **出す必要がないときは出さない**
3. **攻撃をくらったら大失敗**

現状の指標は (2) と (3) を測れない。(1) すら、押さなかったタッチを
Fail に落とすので正しく測れていない。

**追加の仮説 (ユーザ、2026-09-21):** **なるべく遅く開始し、かつ必要な
押下数を確保して成立させるほど、被弾率が下がる。** 開始が遅いほど窓に
残るティックが減り、必要な押下数 (6 で実用上 100%、8 で確定) を詰めるのが
難しくなる。遅い開始 × 少ない押下数 × 高い成功率 が最上位の技術で、
この 3 つの数字を並べれば検証できる。

---

## 指標 — 1 タッチを 3 択に分解する

1 触れ = 1 試行。結果は次の 3 択:

| 結果 | 意味 | 判定 |
|---|---|---|
| **PB成立** | 押して成立した | P1 の `$171` (0xFF8471) = 0x10、`$184` (0xFF8584) |
| **ガードのみ** | 触れたが PB は出していない (合法。PB は任意) | `$05`=2 の区間で `$140` (0xFF8540) が 0x02/0x12 |
| **被弾 (HIT)** | **大失敗**。ガードすら漏れた | 同じ窓で `$140` が 0x00/0x04 |

ヒット/ガードの分岐に `$140` を使う根拠は VSAV_MEMORY_NOTES と道筋の
実装 (0x02395A が地上ガードに 2 を書く、0x02393A が空中に 0x12)。

### 表示する 5 つの数字 (2 行)

```
PB 3/4 (75%)   Block 5   HIT 1
開始 +7t   押下 6.3   精度 3/3
```

| 指標 | 定義 | 分母 | 対応する思想 |
|---|---|---|---|
| **結果分布** | 全タッチを PB / Block / HIT の 3 択で数える | 全タッチ | 全体像 |
| **被弾 (HIT)** | 独立計上、赤で表示。**目標は 0** | — | 大失敗 |
| **実行精度** | PB成立 ÷ **押下ありタッチ** | 窓内に押下 ($170 ≥ 1) があったタッチのみ | (1) 狙った時に 100% |
| **平均開始遅延** | 窓が開いたティック ($1AB が開く) から**最初の押下**までのティック数 | 押下ありタッチのみ | 仮説の「遅く」 |
| **平均押下数** | 窓あたりの `$170`。成功窓は成立時点で凍結した値 (0x0275E0 が以後の押しを拒む)、失敗窓は窓が閉じた時点の値 | 押下ありタッチのみ | 仮説の「押下数を確保」 |

- **選択率**は分布から読める (PB ÷ (PB+Block+HIT))。行を増やさない。
  「出しすぎ/出しなさすぎ」はこの数字を見てユーザが判断する。
- **無理押しへの耐性。** 実行精度の分母は「押した機会」なので、押せば
  押すほど分母が増えるだけで有利にならない。成立後の押しは
  0x0275E0 が拒むので数えもしない。
- **何が測れないか。** 「出す必要がなかった」はメモリに存在しない
  (意図はゲームが知らない)。ツールが測れるのは結果の分布と実行精度
  まで。リバーサル検証のマーク方式 (ホットキーで正解を入力) まで行くと
  重いので、ここでは採らない。

---

## 信号と観測点 (実装時に使う番地)

| 信号 | P1 | P2 | 意味 |
|---|---|---|---|
| `$05` | 0xFF8405 | 0xFF8805 | 触れている (== 2)。タッチの区切り |
| `$1AB` | 0xFF84AB | 0xFF89AB | PB の受付窓。0x023966 で 14、0x02249C で減衰、~12 ティック実用 |
| `$170` | 0xFF8470 | 0xFF8970 | 窓内の押下数。0x027606 で計数。8 で確定、6 で実用上 100% (0x02760E とその下の表) |
| `$171` | 0xFF8471 | 0xFF8971 | 成立で 0x10 (0x027632) |
| `$184` | 0xFF8584 | 0xFF8984 | 成立フラグ。立ったら 0x0275E0 が以後の押しを拒む |
| `$140` | 0xFF8540 | 0xFF8940 | ヒット/ガードの種別。ガード 0x02/0x12、ヒット 0x00/0x04 |

- ティックは `0xFF8081`。既存のティックストリーム: guardCancel の
  0x0221CC フック (inputHistory が生入力の退避に使っているもの) か、
  rawStateService の ticker。
- フレームベースのままである現状の問題を、ここでティックベースに
  作り直して解消する (作者の TODO を消化する)。

---

## 未決事項 (実装前に実測で確定すること)

1. **開始遅延 0 の境界。** 窓が開いたのと同じティックの押下は
   $126 エッジとブロックエッジが重なり得る。数えられるかは実測で見る。
2. **`$170` の窓リセット。** 窓ごとに 0 に戻る前提で平均を取る。
   0x023966 が窓と一緒に何を書くか、実測で確定してから。
3. **`$171` vs `$1B0`。** 上記「怪しい信号」。正の成功信号を 1 つ決める。
4. **結果の確定タイミング。** ガード/被弾は `$05` が 0 に戻るまで
   待たないと確定しない (道筋の「判定は最後に聞く」と同じ考慮)。
   遅延と確実性のトレードをどう切るか。
5. **表示文言と置き場所。** Show PB Stats の置き換えで合意済み。
   Trainer タブ、2 行。gui.text は ASCII のみ (日本語は PNG)。
6. **リセット。** 平均は**累計**、トグル OFF でクリア
   (master スクリプトの display_pb_stats リセットと同じ形)。
   移動平均 (「今の調子」) は将来の拡張として残す。

---

## 保留の理由と再開条件

2026-09-21、設計合意の時点で保留。既存の Show PB Stats が存在し、
練習の第一目的 (自分の PB の成否を見る) は今の機能でも一応回る。
本設計は「強さに結びつく指標」への作り直しなので、実装すると既存の
表示と計測を置き換える破壊的変更になる。次に PB 練習を本気で改善したく
なったとき、この 1 枚から始めること。