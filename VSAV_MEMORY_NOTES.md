# VSAV 解析メモ ― アドレスと時間の扱い

Vampire Savior (CPS2, 970519 Japan) をトレーニングモードから調べた結果のうち、
**確定しているものだけ**をまとめたものです。経緯は省いて事実だけ載せています。

P1 のオブジェクトは `0xFF8400`、P2 は `0xFF8800`。以下 `$xx` は
オブジェクト先頭からのオフセットで、P2 なら `0xFF8800 + $xx`。

---

## 1. 時間の単位

**ゲームは 1 ティックごとに入力を読み、状態を進めます。**表示フレームとは別物です。

```
ターボ3   3 表示フレーム = 4 ティック
```

攻略 wiki の「ターボ3 = ノーマル × 0.75」と同じことを逆から言った関係です。
**攻略 wiki のノーマルスピードの F 表記は、そのままティック数として読めます。**

`0xFF8081` が 1 ティックごとに +1 されるカウンタ(ROM `0x008E10`)。
これが解析の時計になります。ラン・アヘッドが有効だと巻き戻るので、
逆行を検出したい場合はこの値の差分を見ます。

### 押しと行動

```
ティック X で押しのエッジが立つ  ->  行動はティック X+1
```

373 スパンで例外なし。`$126` = `~$124 & $122`(新しく押されたビット)。

---

## 2. ティック単位で動く方法

**これが中核です。**Lua から見える「フレーム」ではゲームの時間を追えません。

### CPU のアドレスにフックを掛ける

FBNeo の Lua には**命令アドレスにフックを掛ける関数**があります。

```lua
memory.registerexec(0x02211A, function() ... end)
```

**68000 がそのアドレスを実行するたびに呼ばれます。**フレームのコールバック
(`registerBefore` / `guiRegister`)は表示フレームに 1 回しか回りませんが、
これは**ゲームロジックが進むたび、つまり 1 ティックに 1 回**回ります。

> **同じ番地に 2 つ登録できません。後勝ちで置き換わります。**
> `0x02211A` には `guardCancel.lua` のフックが既にあります。別ファイルから同じ
> 番地に `registerexec` すると、**読み込み順で後のものだけが残り、先のものは
> 黙って捨てられます**。エラーも警告も出ません。
>
> これで測定を 2 回空振りしました。しかも「両方生きている」と誤判定しています
> —— 根拠にしたのは guardCancel 側の診断が出ていることだけで、それは
> **置き換えられた側から見える姿と区別がつきません**。
>
> **ティック単位で何かしたいときは、既存のフックから呼んでください。**
> `guardCancel.lua` の `0x02211A` フック先頭が入口です(`air_probe_tick` の例)。
> 呼ぶ側をグローバル関数にしておくと、guardCancel の upvalue 上限(Lua 5.1 は
> 60、既に上限ちょうどの関数があります)に触れずに済みます。

**ここが出発点です。**トレーニングモードの時間の単位を「表示フレーム」から
「ティック」に変えられるのは、この 1 点があるからです。

使っているフック:

```
0x02211A   入力の消費。ここに置いた処理が、その回に読まれる入力を決める
0x0221CC   トレース採取
0x02760E   プッシュブロックの回数比較(レジスタを書き換える例)
```

**このルーチンはオブジェクトごとに回ります。**どちらのプレイヤーの回かは
`A6` で分かるので、必ず確認してください。

```lua
if memory.getregister("m68000.a6") ~= 0xFF8800 then return end   -- P2 以外は無視
```

レジスタは読むだけでなく**書き換えられます。**プッシュブロックの必要回数を
変える処理は、ゲームの比較命令の直前で `D0` を書き換えています。

```lua
memory.registerexec(0x02760E, function()
  if memory.getregister("m68000.a6") ~= 0xFF8400 then return end
  if memory.getregister("m68000.d0") < min_inputs then
    memory.setregister("m68000.d0", 0x00000000)
  end
end)
```

`memory.registerwrite(addr, len, fn)` もあり、**特定アドレスへの書き込み**で
発火します。1 表示フレームに複数回起きる変化を捉えるのに使えます。

### 経過ティックを数える

フックは「今このティック」を教えてくれますが、**何ティック経ったか**は別に要ります。

```
0xFF8081   1 ティックごとに +1 されるカウンタ ( ROM 0x008E10 )
```

これを基準にすれば、待ち時間も間隔もすべてティックで書けます。
差分は `(now - then) % 256` で取ります。

**ラン・アヘッドが有効だとこの値は巻き戻ります。**前のティックから 1 増えて
いない場合は、エミュレータが状態を復元して同じティックを再実行しています。
**「経過時間」をフレーム番号や Lua 側のカウンタで持つと、この再実行で
狂います。**このカウンタで持てばゲームと一緒に巻き戻るので狂いません。

### ラン・アヘッドは切ってもらう

**ラン・アヘッドが有効だと、最速行動が安定して出せません。**

エミュレータが同じティックを何度も実行するため、こちらのフックも同じ回数
呼ばれます。**「このティックで押す」という判断が、1 回のはずが複数回起きる**
わけです。押しエッジは 1 ティックにしか立たないものなので、
再実行された回のどれで書き込みが確定するかによって、狙ったティックから
ずれます。**free+0 に置いたつもりのものが free+1 に落ちれば、非必殺技は
出ません。**

実測では、ラン・アヘッド 1 フレームでティック対の **24.4% が逆行**し、
深さは 1〜2 でした。4 回に 1 回ずれる時計の上では、1 ティックの精度は出せません。

そのため画面に警告を出しています。**FBNeo の Run-ahead を Disabled にして、
エミュレータを再起動してもらう**必要があります。

### 有効かどうかの判定方法

**FBNeo は Run-ahead の設定を Lua に見せません。**そこで設定を訊くのではなく、
**挙動から判定します。**

`0xFF8081` は 1 ティックに 1 回 +1 されるので、**前の値から +1 以外の変化を
したら、それは再実行です。**

```lua
local _step = (lg - last_lg) % 256
if _step ~= 1 then
  -- 巻き戻り。深さは (last_lg - lg) % 256
end
```

**1 回の逆行では警告を出しません。**セーブステートの読み込みでも同じことが
起きるためです。**継続的な割合**を条件にしています。

```
逆行が 20 回以上、かつ全ティックの 5% 以上
```

この 2 つを満たしたときだけ画面上部に赤帯を出し、逆行率と最大の深さを
表示します。

---

## 3. 入力をメモリに書く

狙ったティックに入力を置くには `joypad.set()` では届きません。

### なぜパッド経由では届かないか

入力の取り込みは 2 段階あります。

```
0x008EB2   ステージ 1   メインループの末尾
0x014E7A   ステージ 2   move.w D2, ($b94,A5)   ここで $B94 が埋まる
0x02211A   消費         ゲームロジックが読む
```

**ステージ 1 はメインループの末尾**にあります。FBNeo のパッドに値を置いても、
ゲームがそれを見るのは**次のティック**です。しかも Lua の `joypad.set()` は
表示フレームに 1 回しか回りません。

つまり「今のティックで判断して、今のティックの入力にする」ことがパッドでは
できません。**判断とその結果の入力が同じティックに入らない**わけです。

### 書く場所と書き方

**消費の直前(`0x02211A`)にフックを置き、そこで直接書きます。**
ゲームがこれから読むコピーに書き込むので、その場で効きます。

```
0xFF8B94   ボタン    ( P1 は 0xFF8794 )
0xFF8B95   レバー
```

守ることが 2 つあります。

**1. 両方のバイトを毎回書く。**レバーのバイト(`0xFF8B95`)だけを書いても
**入りません。**実測では、保持中にレバーだけ書いたフレームは `$123` が `0x00`
のままでした。届いたのは `0xFF8B94` も書いたフレームだけです。ボタンが無い
ときも 0 を OR して両方書きます。仕上げにゲーム自身と同じ形
(`move.w`、`0x014E7A` と同じ)でワード書き込みをもう一度行います。

**2. レバーは OR ではなく代入。**保持中の値に OR すると、残っていた下と
新しい後ろが合わさって**下後ろ**になります。回転系の認識(`0x02A1B4`)は
4 方向と `cmpi` で完全一致を取るので、斜めは弾かれます。エントリが持っている
ビットこそが押すべきものなので、そのまま代入します。

### レバーのビット

**向きによって前後が入れ替わります。**`$0B` が向きです。

```
向き 0    前 = 0x02   後 = 0x01
向き 1    前 = 0x01   後 = 0x02
共通      下 = 0x04   上 = 0x08
```

ゲーム側も `0x022194` で `$122` の bit0 と bit1 を交換しています
(交換表は `0x0221C8`)。**方向は注入する瞬間の向きで解決してください。**
モーションの途中で振り向くキャラがいるので、キューに積む時点でビットに
変換すると左右が逆になります。

### 左右の補正は 2 系統あり、読むバイトが違う — ROM で確認 (2026-09-20)

注入した 1 ワードは、**2 つのフィールドへ、別々のバイトで補正されます。**

```
02218E: tst.b  ($b,A6)          ; $122 は「向き $b」だけで判断
022192: beq    $221aa
022194: move.w ($122,A6), D0    ; bit0/bit1 を交換 (表は 0x0221C8)
```

```
0221CC: move.b ($b,A6), D0      ; $12a は条件が違う
0221D0: tst.b  ($38,A6)  bne    ; 空中なら $b
0221D6: tst.b  ($115,A6) bne
0221DC: move.b ($120,A6), D0    ; 地上ならこちら
0221E4: move.w ($12a,A6), D0    ; bit0/bit1 を交換
```

| フィールド | 何に使われるか | 補正に使うバイト |
|---|---|---|
| `$122` / `$126` | 生の方向 (ダッシュ・歩き) | **`$b`** だけ |
| `$12a` / `$1ac` / `$1ae` | コマンド認識 (必殺技) | 地上は **`$120`**、空中と `$115` では `$b` |

**`$120` は同じティックの中で後から更新されます。**

```
02211A  move.w ($394,A6),($122,A6)   ; 注入するのはここ
022130  bsr $22160                   ; $120 が更新されるのはここ
022134  bsr $2218e                   ; 補正は新しい $120 を使う
```

したがって `$120` をメモリから読むと **1 ティック古い値**になります。
`$b` にはこの更新がないので、**`$122` 向けには素直に読んで構いません。**

**`$b` と `$120` は、すれ違いの最中に食い違います。**`$120` は X 座標の差から
出ますが(±0x16 のデッドゾーンあり)、`$b` は**キャラが実際に振り向いた
ティック**にしか変わらず、ダッシュ中のキャラは振り向きません。

実測 (2026-09-20、すれ違い 3 回、同じ形)。`used` はツールが使った値。

```
lg 18   $b=0  $120=1  used=0   注入 L2
lg 19   $b=0  $120=1  used=0   注入 L2
lg 21   $b=0  $120=1  used=1   注入 L1   ← 3 つが全部バラバラ
lg 22   $b=1  $120=1  used=1   注入 L1   ← $b が遅れて追いつく
```

**1 つのダッシュの 2 タップが、画面上で逆向きとして出ています。** ゲーム側の
基準は動いていないので、「同じ方向を 2 回」が成立しません。

**ダッシュの走る向きは、成立した瞬間の向きで決まります。**振り向く前に入れた
ダッシュは、最後まで逆へ走ります。後から補正できないので、**バックダッシュで
代用することもできません** — `$b` がコマンドの途中で反転すると、前半バック
ダッシュ・後半前ダッシュになり、どちらの技にもなりません。

**読み戻すときは `$123`。**`$122` はワードの**上位 = ボタン**です (68000 は
ビッグエンディアン)。レバーは `$123`。最初の測定は `$122` を読んで全行 `0x00`
になりました。

### パッド経由の入力との共存

`joypad.set()` は同じ入力を 1 ティック遅れで届け続けますが、害はありません。
**ゲームは先に注入した値を見て、その後にパッドの値を見る**ので、
「押した → 押しっぱなし」と読まれ、**エッジは注入したティックに立ちます。**

### 押しエッジ

```
$126 = ~$124 & $122      新しく押されたビット
```

**握りっぱなしの方向は、もう一度入れてもエッジになりません。**
ダミーがガードのためにレバーを後ろに握っている状態で「後ろ」を入れても
`$126` は立ちません。**一度中立に戻してから入れ直す**必要があります。
後ろダッシュがそのままでは出ないのはこれが理由です。

### タメ技は「レバーを書き続ける」だけで出ます — 実測で確認

`h_charge` / `v_charge` というトークンが `make_input_sequence` にありますが、
**このコードベースでは完全に死んでいます。** `entry_to_bits` の `BUTTON_BITS` に
無いので `or 0` でボタン 0 になり、実際のビットになりません。解釈していたコードは
3rd Strike 由来のもので `controller.lua` にコメントアウトされたまま残っています。

タメに専用の仕組みは要りませんでした。**レバーのビットを毎ティック書き続ければ、
人間が握っているのと同じ**です。ゲーム側のタメカウンタはそれを見ています。

確認した並び(ビシャモン、EX 居合斬り = 前タメ後ろ→前 + KKK):

```
3  0 Ticks           Crouch Back (Hold)          ← ここから下後ろを握る
4  Auto (Recovered)  Attack Down Back (Hold) + LP
5  Auto (Recovered)  Attack Down Back (Hold) + LP
6  Auto (Recovered)  Attack Down Back (Hold) + LP
7  Auto (Recovered)  Attack Down Back (Hold) + MP
8  9 Ticks           Attack Forward (Hold) + KKK ← 前に倒して解放
```

**通常技 4 発ぶんの保持でタメは足ります。**ティック数を数える必要はなく、
「前のステップが握り続ける」だけで成立します。これは「120f とかは入れちゃダメ」
という決め事とも一致します。

保持でボタンは落とします。握ったままだと次の技に押しエッジが立たず、
タメの解放も**方向の変化**であってボタンの変化ではないためです。

### `EXP` / `EXK` は展開しないとボタンになりません

`make_input_sequence` の複数ボタン展開に無い名前は、**文字列のまま**エントリに
入ります。`entry_to_bits` は `BUTTON_BITS` を引いて `or 0` を取るので、6 ボタン
以外の名前は**ボタン 0**になります。方向だけのエントリが出来上がり、技は出ません。

`PPP` (`EXP`) と `KKK` (`EXK`) はこれで一度も押されていませんでした。新しい
ボタン名を足すときは、`LP+LK` などと同じ場所で 6 ボタンに展開すること。

---

## 4. 主要オフセット

| オフセット | 内容 |
|---|---|
| `$04`–`$07` | 状態ワード。`move.l` でまとめて書かれる |
| `$05` | マスタ状態。`0x00` 自由 / `0x02` やられ・ガード |
| `$06` | 行動種別(下表) |
| `$0B` | 向き |
| `$20` | 現在のアニメセルの残りティック |
| `$21` | 現在のセルのフラグ。bit0 が連打キャンセル可、bit6 が最終セル |
| `$1C` | 現在のセルへのポインタ |
| `$38` | 空中判定(非 0 で空中) |
| `$59` | 食らった技の強さ 0 弱 / 1 中 / 2 強 |
| `$105` | 攻撃フラグ |
| `$122` `$123` | 入力(向き補正後) |
| `$126` | 新しく押されたビット |
| `$12A` | コマンド認識が読む入力 |
| `$140` | 復帰の種別。`0x00`/`0x04` ヒット硬直 / `0x02` 地上ガード / `0x0A` 起き上がり。**ガード中は `0x02` か `0x12`**、ヒット硬直中は `0x00` か `0x04` で、この 2 つで硬直の種別を分けられます |
| `$158` | ガードキャンセルの受付(14 から毎ティック減算)。ガード成立で `0x0E` が入ります |
| `$164` | ガードの押し戻しタイマー |
| `$170` | プッシュブロックの押した回数 |
| `$171` | プッシュブロック成立時 `0x10` |
| `$184` | 成立フラグ。非 0 の間はプッシュブロック判定を拒否 |
| `$1AB` | プッシュブロックの受付窓(14 から毎ティック減算) |
| `$1A7` | 起き上がりの経過カウンタ |
| `$382` | キャラ ID |
| `$394` | 生のレバー + ボタン |

### ジャンプとダッシュの道筋 — 実測 (2026-09-08, `analysis/routeProbe.lua`)

監視バイトが変わったティックだけを記録したトレースから。**変化した瞬間だけ**の
記録なので、間のティックは同じ値が続いている。

| 節目 | 信号 | 実測 |
|---|---|---|
| ジャンプの起点 | `$06` が `0x06` になる | |
| 離陸 | `$38` 0 → 1 | 起点から 2〜4t (表の予備動作 3 と整合) |
| ダッシュの起点 | `$06` が `0x14` になる | 同時に `$105` も立つ (ザベル) |
| 技の開始 | `$105` 0 → 非 0。ダッシュ中は `$10B` の変化 | |
| 判定 | セル + `0x0A` の箱 id | |
| 接触 | **P2 の `$5C` (ヒットストップ) の増加** | 下記 |
| 着地 | `$38` 1 → 0 | |
| 終点 | `$05` と `$06` がともに 0 | 着地から 8t (攻撃なしジャンプ) |

実測した 3 本のジャンプ攻撃 (起点からの経過):

```
離陸 +3   攻撃 +26  判定 +31  ヒット +32  着地 +74
離陸 +2   攻撃 +24  判定 +29  ヒット +30  着地 +74
離陸 +6   攻撃 +31  判定 +35  ヒット +36  着地 +54
```

**`$06` は空中でも `0x06` のまま。**予備動作と浮遊時間を分けるのは `$38` の方で、
`$06` ではない。上の表の `0x08` (空中) は、このトレースでは**着地の局面**と
ラウンド開始の演出中にしか現れなかった。Route はこの値に依存しない。

**起点を「中立を出た瞬間」にしてはいけない。**前の行動の尻尾で `$06` が `0x04`
(自由になる直前) を経由することがあり、そこから数えると予備動作が 6t に伸びる。
`0x06` になった瞬間から数えること。

**空中攻撃中に `$06` が `0x0A` (通常技) になることがある。**空中判定に `$06` を
使わない理由。

### 空中で当てた瞬間の `$06` は 0x06 が多数派

ジャンプ攻撃の有利を着地基準で測るには「空中の通常技か」を判定する必要がある。
**実測した 9 回の空中接触の内訳** (2026-09-09):

| 接触した瞬間の `$06` | 回数 |
|---|---:|
| `0x06` (ジャンプ) | 12 |
| `0x0A` (通常技) | 1 |

**`0x0A` だけを見ると 13 回中 1 回しか拾えません。**空中攻撃は多くの場合ジャンプ
状態のまま当たります。判定は `0x06` と `0x0A` の両方を通すこと。

**空中ダッシュ攻撃も同じ `0x06` です。**ザベルで 1 本通しで測ったところ、
空中ダッシュ中は `0x14` (ダッシュ) ですが、そこから攻撃を出した瞬間に `0x06` へ
戻り、接触ティックも `0x06` でした。`0x14` を見張る必要はありません。

同じ実測で `$04` は全件 `0x02`、`$105` は全件 `0x01`、`$07` は `$06` に対応して
`0x06` / `0x02` / `0x04` でした。

必殺技 (`0x0E`) / ES (`0x10`) / EX (`0x12`) は除く — 空中必殺技の着地硬直は
キャンセルできないので、着地基準にすると実際より有利に出る。

この 9 件の内訳と、抽出条件・適用範囲・確定度を書き出したものが
`analysis/ISSUE-CONTACT-ATTACKER-STATE-001_provisional.md` です。ROM 解析側の
`ROM-AIR-NORMAL-TRANSITION-001` (`0x0274CE` が `$04..$07` に `02 00 0A 00` を書く)
と食い違うのはここで、**ROM の書込み自体は否定していません。**否定しているのは
「接触ティックで `$06` を見れば `0x0A`」という派生の判定条件のほうです。
唯一 `0x0A` だった 1 件は、同じジャンプの 3 段目の空中通常技でした。何が
`0x06` と `0x0A` を分けているかは未解決です。

参考: 同じ測定での地上接触は `$06` が `0x00` 5 回、`0x14` (ダッシュ) 7 回。

### やられ中の相手への接触は `$5C` で取れる

**`$05` が `0x02` に入るエッジは、相手が既にやられ中だと二度と立たない** (下記)。
その穴を埋める信号が見つかった。**P2 のヒットストップ `$5C` は、やられ中に当てても
当たるたびに `0x0B` へ跳ね上がる。**毎ティック減って 0 になり、次のヒットでまた
`0x0B` に戻る。ガードの `$158` と同じ「増加を見る」形で拾える。

エアチェーンのトレースで **5 回**、`$05` が `0x02` のまま `$5C` が 0 → `0x0B` に
跳ねるのを確認 (2026-09-08 実測)。

```
$5C 00->0B  box=23  10B 33->3C   ← $05 は 02 のまま
$5C 00->0B  box=23  10B 3C->45
$5C 00->0B  box=23  10B 54->5D
$5C 00->0B  box=23  10B 5D->66
$5C 00->0B  box=28  10B 78->84
```

**Tick Data の「チェーンを技ごとに分けられない」もこれで塞がる可能性がある。**
分けられなかった理由が接触のエッジだったので (下記)、そこを `$5C` に替えれば
条件が変わる。別件として扱うこと — 既存の測定に手が入る。

### `$10B` はヒットでも動く

技の開始で動くだけではない。**ヒットした瞬間にも動く** (33 → 3C など)。ダッシュの
助走を切るのに使うときは**最初の 1 回だけ**を採ること。ジャンプ攻撃では `$105` の
立ち上がりが技の開始と同じティックなので、`$10B` は要らない。

### P1 のレバーは `$125`。`$122` はボタン

`inputHistory.lua` が既に持っていた地図 (実測付き)。**歩きとしゃがみを `$122` から
読もうとして、859 行すべて 00 だった** (2026-09-09)。

```
+0x122  ボタン: bit0 LP, bit1 MP, bit2 HP, bit4 LK, bit5 MK, bit6 HK
+0x125  レバー: bit0 前, bit1 後, bit2 下, bit3 上
+0x00B  向き
```

### `0x04` は硬直中に始まり、`$05` の解除より長く続くことがある

復帰の瞬間を 10 件測った結果 (2026-09-10、ザベル vs CPU モリガン):

- 9 件は `$05` が `0x02` を抜けたティックで `$06=0x00`
- 1 件は**その時点で既に `$06=0x04`**。相手が歩き続けたため `0x00` に戻るまで
  **245 ティック**かかった
- 10 件中 6 件は、**まだやられ中 (`$05=0x02`) のうちに `$06` が `0x04` になっていた**

有利フレームの通常経路 (`tickData.lua` の `p2_became_free`) は `$05` の辺だけを見て
いるので影響を受けません。`$06` も見ているのは `p2_can_act` で、**こちらはノック
ダウンと投げの起き上がりにしか使われていません。**その経路の実測は無い (上のログに
ノックダウンが 1 件も含まれていない) ため、**`0x04` を足す変更は保留にしました。**
`0x04` の意味は「自由になる直前」なので、起き上がりに足すとリバーサルが 1 ティック
早く出る方向に効きます。

### レバーの向き

⚠ **前後の向きは未確定です。**この地図どおりに三行目へ Walk / Back と出したところ、
**画面上で前後が逆でした** (2026-09-10 実測)。上下と下は合っています。三行目は
どちらも Walk と表示することにしたので実害はありませんが、**向きを知る必要がある
用途にこの bit0/bit1 の対応をそのまま持ち込まないでください。**`inputHistory.lua`
も同じバイトを向きで入れ替えて表示しているので、直すときは両方を測り直すこと。

**`$123` は使わない。**同じ並びを持っているが、片方の向きのときに左右のビットが
毎フレーム入れ替わる。実測で、横を握っているあいだの変化率が `$123` は 20.3%、
`$125` は 4.2%。再現精度は 83% 対 99.9%。

**`$125` のビットは向きに対する相対**なので、前後を知りたいだけなら向きは要らない
(表示のために絶対の左右へ直すときだけ `$0B` で入れ替える)。

どちらも `registerBefore` の時点で読めている — `inputHistory` がそこで読んで
いる。

### `$06` の値

| 値 | 意味 |
|---|---|
| `0x00` | 待機 / **着弾フリーズ**(impact freeze)。`$05` が非 0 のときの `0x00` は「硬直中で、かつ着弾フリーズが走っている」で、この間に積んだ入力は捨てられます |
| `0x02` | やられ・投げられ |
| `0x04` | **歩き**、および自由になる直前。2026-09-10 の実測では `0x04` の 580 ティック中 569 でレバーが前か後ろに倒れており、残る 11 だけが前の行動の尻尾。**歩きを見分けるにはこの値とレバー ($125) の両方が要ります** |
| `0x06` | ジャンプ |
| `0x08` | 空中 |
| `0x0A` | 通常技 |
| `0x0C` | ガード |
| `0x0E` | 必殺技 |
| `0x10` | ES |
| `0x12` | EX |
| `0x14` | ダッシュ |
| `0x16` | ダークフォース |
| `0x1A` | DF 停止 |

### キャラ ID (`$382`)

```
0x00 バレッタ   0x01 デミトリ   0x02 ガロン     0x03 ビクトル
0x04 ザベル     0x05 モリガン   0x06 アナカリス 0x07 フェリシア
0x08 ビシャモン 0x09 オルバス    0x0A サスカッチ 0x0B ザベル(第2)
0x0C Q-Bee      0x0D レイレイ   0x0E リリス     0x0F ジェダ
0x12 ダークガロン               0x18 オボロ
```

---

## 5. 「動ける 1 ティック前」を知る

やられ・ガード・ダウンから復帰する **1 ティック前**に、ゲームが必ずこれを書きます。

```
024F0A: 2D7C 0202 0400 0004   move.l #$2020400, ($4,A6)
```

つまり `$04=02 $05=02 $06=04 $07=00`。**この直後のティックでキャラは動けます。**
14 キャラ中 13 キャラで 250 回中 249 回。

Q-Bee だけは別のハンドラ(`0x04A6FA`)を通り、この署名を出さずに
`0x04A7A0` から抜けます。個別扱いが必要です。

この署名が出る条件は 1 つ手前にあります。

```
024F02: 4A2E 0021             tst.b  ($21,A6)      ; 最終セルか
024F06: 6A00 3068             bpl    $27f70        ; 違えば $20 を減らすだけ
```

### `$21` を「フレーム」で読んではいけない

投げ抜け(`$140 = 0x0E`)の武装は `$21 == 0x40` を条件にしています。実測すると
**この値はちょうど 1 ティックしか出ません**。ザベルの投げ抜け 7 本、成功も失敗も
1 バイトの違いもなく同一でした。

```
$21 = 0x00 ×19,  0x40 ×1,  0xFF ×16      ← 7 本すべて同じ
```

違ったのは**読む側の時計**だけです。`service_held_reversal()` は**表示フレームごと**に
しか走らず、turbo 3 では 1 フレーム ≒ 1.33 ティックなので、この 1 ティックを
**4 回に 1 回ほど踏み外します**。7 本中 4 本が武装し、3 本は一度も武装しませんでした。

**一般則: 1 ティックしか出ない値は、必ずティックフック(`0x02211A`)で読むこと。**
フレーム側で読んでいる限り、条件が正しくても確率的に落ちます。キャラ固有に見える
症状の原因がこれであることがあります。

同じ形が `0x02` 系の起き上がり予備経路(`_known == nil` のときの `$21 == 0x40`)にも
残っています。こちらは `$1a7` の表に無いキャラだけが通る道です。

`$21` は最終セルの持続が尽きたときに `0x027F88` で `0xFF` になります。

### 行動が出せる最速ティック

```
必殺技コマンド(波動・昇龍・半回転・1 回転…)   押しは free-1、行動は free+0
それ以外(ダッシュ・通常技・投げ・ジャンプ・DF)  押しは free+0、行動は free+1
```

**非必殺技は 1 ティックずれると「遅れて出る」のではなく「出ません」。**
ダッシュの 2 回目のタップを free+1 に置くと `$06` が `0x14` にならず、
代わりに通常技が出ます。

### 自分の技の硬直明けは `$05==0 かつ $06==0` のティックが free+0 — 実測で確定

やられ・ガードからの復帰には free-1 の署名(`$04=02 $05=02 $06=04 $07=00`)が
ありますが、**自分の技を振り終わった硬直明けには署名がありません**。
`$05` は技を出している間もゼロのままなので、変化するのは `$06` だけです
(通常技中は `0x0A`、`0x0274CE` で設定)。

`$06` が `0x0A → 0x00` になったティックを 0x02211A の直前で読み、その同じ
ティックに入力を書けば **free+0 に乗ります**。1 つ前ではありません。

確認方法: Aulbath のダッシュ MP → しゃがみ LP は**目押しのリンク**で、
1 ティックずれると落ちます。`$06==0` のティックに押しを乗せたところ
つながるようになりました。free+1 では落ちていたので、境界はここで確定です。

> 押しが `lead` ティック分だけ後ろにずれていたのが原因でした。数値待ちは
> リストを `wait - lead` から早出しして名指しのティックに乗せますが、状態
> 判定(Auto)は先が読めないので早出しできません。**Auto のステップでは、
> 最終エントリより前にあるものがそのまま free からの遅れになります。**
> `analysis/test_runner_compile.lua` がこの遅れを全アクションについて
> 表にして assert します。

---

## 6. 起き上がり

`$1A7` が起き上がり中に 1 ティックずつ増えます(`0x024E84`、`$194` と `$15D` で門番)。
終端値はアニメーションの長さで、**アニメ 44 の持続合計 + 2**(9 キャラ中 8 キャラで一致)。

終端値はキャラと `$07` で決まります。実測値:

```
キャラ         $07=2/4   $07=6
ガロン           29        31
ビクトル         30        30
ザベル           22        38
モリガン         27        47
アナカリス       27        46
ビシャモン       23        30
オルバス         28        33
サスカッチ       23        47
レイレイ         29        30
リリス           27        47
ジェダ           24        39
```

Q-Bee は `$1A7` ではなく **`$0A` の符号**で決まります(0 → 15、負 → 40、正 → 47)。
デミトリはアニメーションで終わらず着地判定を待つため、`$1A7` では計れません。

---

## 7. ガードとガードキャンセル

```
02395A: move.b #$2, ($140,A6)    ; 地上ガード
023960: move.b #$e, ($158,A6)    ; ガードキャンセル受付 14
023966: move.b #$e, ($1ab,A6)    ; プッシュブロック受付 14
022492: subq.b #1, ($158,A6)     ; 毎ティック
02249C: subq.b #1, ($1ab,A6)     ; 毎ティック
```

`$158` は**ガードキャンセルの窓であって、硬直の長さではありません。**

### ガードとヒットは `$140` と `$158` で見分けられる — ただし 1 ティック遅れることがある

2026-09-10 の実測 (ザベル、CPU 相手のガード 10 件):

| | 相手 `$140` | 相手 `$158` |
|---|---|---|
| ガードさせた | `0x02` | `0x0E` から毎ティック減算 |
| ヒットさせた | `0x00` のまま | `0x00` のまま |

**10 件のうち 3 件は、接触したティックではまだ両方 `0x00` で、次のティックで
設定されました。**残る 7 件は接触ティックで既に入っています。2 ティック以上
遅れた例はありません。ログ上でその 3 件は連番のティックなので、取りこぼしに
よる見かけ上の遅れではありません。

**接触したティックだけを見て「ガードでない」と決めてはいけません。**
`actionRoute` が Hit/Guard の表示を 1 ティック保留してから確定させているのは
これが理由です。

### ⚠ `$158` を `== 14` で見てはいけません

接触で 14 が入り、**1 ティックに 1 ずつ**減ります。一方こちらの処理は
**表示フレームに 1 回**＝約 1.6 ティックごとなので、**14 そのものは 10 回に
4 回ほど見逃します**。

見逃すと「窓が開き直った」ことに気づけません。複数ヒットのガードで
**2 ヒット目のリバーサルが出なくなる**のはこれが原因でした。

### ガード行動を出す経路は 2 本ある — 片方だけ塞いでも止まらない

`guardCancelCheck` から出力へ至る道は独立に 2 本あります。

```
アーム経路      arm_edge / hs_arm_edge / BLK.edge のどれかが立ち、
                queue_input_sequence で直接送る

リアクティブ経路 counter.sequence = ga_sequence(...) を関数の終盤で書き、
                その下の queue が送る。アームは一切見ない
```

**リアクティブ経路を止められるのは `should_perform` の早期 return だけ**です。

```lua
if not should_perform and not _pb_guard then return {} end
```

Guard Action Frequency を 4 回直して 4 回とも外したのはこれが理由でした。
アーム側だけを見て「アームが立っていない間はゲートを開けてよい」と判断すると、
リアクティブ経路が完全に素通しになり、**`None` にしても毎回出ます**。

確定した仕様:

```
閉じ方   機会の全フレームで閉じる    リアクティブ経路の唯一の門だから
引き方   機会につき 1 回だけ引く     ガードした 1 発 = $158 の立ち上がり
```

`BLK.episode` は 2 か所で解除されます。`$158` の立ち上がり(**新しい 1 発 =
新しい機会**)と、`$140` がブロック硬直の値を外れたとき(**同じ 1 発の中**)。
**後者を機会と数えてはいけません** — 1 発の中で何度も引くことになり、
`1-(1-p)^武装回数` でほぼ 100% になります。

### シーケンスの 2 歩目以降は、機会が終わっても消えません

`actionSequenceRunner` の `pending` に積まれ、`NO TIMEOUT ANYWHERE HERE` の
とおり時間切れがありません。消えるのは guard action が sequence でなくなった
とき、試合が始まっていないとき、ステートロードの 3 つだけでした。

結果、**ガード硬直で出せなかった残りが、次のガードで出ます**。画面上は
新しいガード行動と区別が付きません。抽選に外れた機会でも残りが出るので、
頻度を変えても見た目が変わらない。当たったときは `M.arm` が `pending` を
丸ごと置き換えるので、**拒否側でも `cancel()` を呼んで対称にします**。

**カウンタは再セット以外で増えないので、「前回より大きい」で判定します。**
13 を見ても 12 を見ても再セットと分かります。

```lua
local _s158 = memory.readbyte(0xFF8958)
if _s158 > BLK.prev158 then ... end     -- == 14 ではなく
```

同じ罠は `$1AB`(プッシュブロックの窓)や `$164` にもあり得ます。**毎ティック
減る値を表示フレームから特定の値で捕まえようとしない**のが原則です。

### 着弾フリーズ中に積んだ入力は捨てられます

`$05` が非 0(硬直中)で `$06` が `0x00` の間が着弾フリーズです。この間に
リバーサル入力を積んでも、**同じ表示フレームの後半にある破棄処理で消えます**。

したがって**フリーズ中は武装しない**のが正しく、あわせて**フリーズ中は
エピソード(武装済みフラグ)も下ろしておく**必要があります。下ろさないと、
フリーズ明けに積み直す経路へ入れません。

(v11.4.2 の差分より。受領文書は
`analysis/VSAV_TRAINING_v11.4.2_CHANGES.md`)

### 硬直の長さを決めているもの

ガード硬直に**カウンタは存在しません**。ディスパッチャは 0x024A22 で、
`$07` をサブステップとする語テーブルで飛びます。毎ティック走るのは
`$07 = 2` の枝で、`bsr $27DE4` が真を返した瞬間にガードが終わります。

```
027DEA: move.b ($59,A6), D0     ; 攻撃側の食らいデータ(0x0764EA+5)が決める反応タイプ
027DF0: lea    ($285de,PC), A0  ; タイプ別スクリプトの語オフセット表(符号付き、自己相対)
027DF8: lea    (A0,D0.w), A0
027DFC: move.w ($164,A6), D0    ; カーソル
027E00: move.b (A0,D0.w), D0    ; 1ティック 1バイト
027E04: bmi    $27e2a           ; 負 = 終端 → ガード終了
027E06: addq.w #1, ($164,A6)
027E22: add.w  D0, ($10,A6)     ; 負でなければ、そのティックの X 押し戻し量
```

**押し戻しの距離と硬直の長さは同一のデータです。**バイトが尽きたら硬直が
明けます。表は 0x0285DE。**データ領域なので MAME のリスティングでは読めず**
(CPS2 は命令フェッチしか復号しない)、実機から読む必要があります。

| `$59` | 長さ | 押し戻し |
|---|---|---|
| 0 (弱) | 11 | `04 04 04 04 03 03 03 02 01 01 01` |
| 1 (中) | 16 | `03 04 04 04 05 06 06 05 04 03 02 01 01 01 01 01` |
| 2 (強) | 20 | `04 05 06 07 08 08 08 07 06 05 04 03 02 01 01 01 01 01 01 01` |
| 3 | 24 | `0a` ×8 から `01` まで |
| 4 | 48 | 全部 `00` (押し戻し無し) |
| 7 | 20 | `0c 0e 10 10 10 10 08 08 …` (頭が重い) |
| 8 | 16 | `04 03 02 01` のあと `00` ×12 |
| 9 | 36 | |

### `$158` を硬直の基準に使ってはいけない理由

`$158` は接触時に 14 が入り、0x022492 が**毎ティック**減らします。対して
`$164` は**キャラが実際に動いたティックしか進みません**。つまりヒットストップ
中は `$158` だけが減り、スクリプトは止まったままです。

デミトリのゼロデモンは `$164` を **12 ティック 0 のまま**保持します。その間に
`$158` は 14 から 2 まで燃えてしまうので、`$158` を基準にした予測は free を
追い越します。保存した 710 本のガードを再生すると、強のガード 229 本のうち
**111 本 (48%)** で予測が free 以降に落ちていました。

**ガードされたゼロデモンの硬直は 20 ティックで、普通の強と同じです。**
硬直が短かったのではなく、基準にしていた時計が別物だった、というだけです。

残りティックは `長さ − $164` で毎ティック正確に取れます。

### 近接ガード(影縫い)

後ろを入れたときにガードポーズを取って動けなくなるかどうかは、ゲーム自身が
決めています。入口は 0x02766A / 0x027688、返り値 D0 = 1 で成立。二段構えです。

**前半 0x0276AC — オブジェクト走査。** `A5+0x1400` (= 0xFF9400) から 0x100
刻みで 32 個。生きていて(`(A4)==1`, `$01!=0`, `$a2==0`)、自分の持ち物でなく
(`$70` が自分と違う)、現セル `$1c` の `$17` の bit7 が落ちているオブジェクトが
一つでもあれば成立します。**距離を見ません** — 飛び道具が画面のどこにあっても
後ろに下がれないのはこれです。

**後半 0x0276DE — 相手プレイヤー。** `$154` が非0なら無条件成立。`$105` が
0 なら不成立。そうでなければ `$8c` の技ごとの判定枠テーブルを引きます。

```
027712: movea.l ($8c,A4), A1     ; 判定枠テーブル
027716: move.b ($a,A0), D0       ; セルの枠 index
02771C: ext.w / lsl.w #5         ; *32
027724: move.w (A1), D0          ; 枠のオフセット(向きで符号反転)
02772E: add.w  ($10,A4), D0      ; 枠の X
027732: sub.w  ($10,A6), D0      ; 自分との距離
02773A: move.w ($4,A1), D1 / addi.w #$80, D1
027742: cmp.w  D1, D0 / bhi 次のセル / 成立
027754: lea ($18,A0), A0         ; 残りのセルを先読みする
```

**技ごとに影縫い距離が決まる**のはこの枠データです。その外では後ろを入れても
ガードポーズにならず、ただ歩きます。通常技は持続が短いので歩きが見えません
が、持続の長い必殺技(デミトリのデモンクレイドル等)でははっきり歩きます。

`$ac` (0x0276F8) は **無条件成立ではありません**。`$38` による却下を飛ばして
範囲判定の途中に入るだけです。

---

## 8. プッシュブロック

**約 12 ティックの窓の中で、押した回数**を数える仕様です。

```
0275D8: tst.b  ($1ab,A6)          ; 窓が閉じたら拒否
0275E0: tst.b  ($184,A6) / bne    ; 既に成立していたら拒否
0275E8: cmpi.w #$202, ($4,A6)     ; $04$05 が 02 02
0275F0: cmpi.b #$2, ($140,A6)     ; 地上ガード
0275FE: bsr    $274ba             ; $126 & $77 = このティックの押し
027606: addq.b #1, ($170,A6)      ; 回数を積む
02760E: cmpi.b #$8, D0 / bcc      ; 8 回以上は無条件で成立
027616: lea    ($28d50,PC), A0    ; 8 未満は回数別の確率表
02761E: jsr    $14e8a.l           ; 乱数
027632: move.b #$1, ($184,A6)     ; 成立
```

* 押しは **6 ボタンのどれでもよく、1 ティックにつき 1 回**(`$126 & $77`)。
* 成立した瞬間に `$184` が立ち、**以降の押しは数えられません。**
  `$170` が途中で止まるのは取りこぼしではなく成立です。
* 8 未満の確率表(`0x28D50`)は思ったより甘く、3 回で成立することが普通にあります。

### 成立すると硬直の時計が変わる

**ガード硬直とは別経路になります。**7 章のディスパッチャ(0x024A22)は `$07` で
枝を選びますが、成立するとその `$07` が書き換わります。

```
024A66: tst.b  ($184,A6)          ; 成立していたら
024A6C: move.b #$6, ($7,A6)       ;   サブステップを 6 へ
024A72: move.b #$a, ($26,A6)      ;   $26 = 10
...
024AFC: subq.b #1, ($26,A6)       ; $07 = 6 の枝は毎ティックこれ
024B00: bne    $24b22
024B02: move.l #$2020400, ($4,A6) ; 通常ガードと同じ free 署名
```

**ガードスクリプト(`長さ − $164`)は捨てられます。**成立後の残りティック数は
**`$26` そのもの**です。10 から始まるので、free の 9 ティック前を狙うなら
`$26 <= 9` で掛かります。

プッシュブロック後の最速行動を出すには、`$07 == 6` を見て時計を `$26` に
切り替える必要があります。ここを取り違えると「プッシュブロックは出るが
最速行動が出ない」になります。

---

## 9. コマンド認識

* 入口は `0x029F4A`。読むのは **`$12A`**。
* `andi.w #$f` でボタンは無視。`cmp.w` の完全一致(bit7 が立っている場合を除く)。
* **毎ティック比較され、中立のティックで進行がクリアされます**(`clr.b (A4)`)。
* ステップのタイムアウト表は `0x02A55A`、値は 14〜19 ティック。
  **完成したコマンドはこの長さだけ保持されます。**

### 連打キャンセルはチェーンとは別経路 (`0x028FB0`)

通常技ハンドラ `0x028E42` は、**接触 `$39` とチェーン禁止 `$1B2` を見る前に**
`0x028FB0` を呼び、成立したら `0x028ED4` へ飛んで**その 2 つを飛び越します**。
つまり連打キャンセルは**空振りでも成立し、`$1B2` も見ません**。

`0x028FB0` が見るもの(これで全部):

```
tst.b  ($38,A6)   bne 失敗      空中は不可
move.b ($111,A6) / cmp.b ($110,A6) / bne 失敗
btst   #0, ($21,A6)  beq 失敗    現在のセルが受け付けるか
move.w ($126,A6) andi.w #$7700   攻撃ボタンの押しエッジがあるか
bit = ($102>>1)+8, $101 が非 0 なら +4
btst   bit, D0    beq 失敗       同じボタンか
st     ($11a,A6)                 成立。D2/D3 に強度と系統を返す
```

ビット位置は `8 LP / 9 MP / 10 HP / 12 LK / 13 MK / 14 HK`。
**強度は見ていません。**`$102`/`$101` は「どのボタンなら通るか」を作るためだけに
読まれ、可否を決めるのは `$21` の bit0 です。小攻撃だけが連打できるのは
アニメーションデータがそうなっているからで、コード上の規則ではありません。

`$110`/`$111` はダークフォースの対。`0x027008` が発動時に両方へ 1 を書き、
`0x022526` がタイマー切れで `$110` だけを消します。

`$11A` は成立フラグ。新しい通常技の開始 (`0x0274E8` / `0x027A12`) で消えます。

**`$11A` はヒット処理まで生き残ります。**`0x0188EE` と `0x01896E` が読んでいて、
前者はヒットストップ表の行を `+0x60` ずらし、後者は `$59` (食らった技の強さ) に
1 を書きます。**連打キャンセルで出した技は、相手にとって別の強さの技になります。**

連打キャンセルの成立側 `0x028ED4` は `$11A` を消しません (消すのは新規開始の側)。
チェーンの成立側 `0x028ED0` が立てるのは `$119` で、そのあと同じ `0x028ED4` へ
落ちます。**`$119` がチェーン、`$11A` が連打**の対です。


### `$3E1` は「決定した」の旗 — バレッタで実測 (2026-09-19)

`$3BD` はキャラ ID のコピー (`020AC8: move.b ($382,A6), ($3bd,A6)`) で、**バレッタが
`0x00`** なので「バレッタを選んだ」と「まだ選んでいない」が同じ値になる。

**`$3E1` はキャラに依存しない。** 決定の 1 フレーム後に立つ。

```
f=2116  p1_382=00 ... 3E1=04/00     バレッタで決定。$3BD は 00 のまま
f=1980  p1_382=01 ... 3E1=04/00     デミトリで決定。同じ 04
```

**P1 は 04、P2 は 03** (`analysis/select_probe.log`、2026-09-19)。2026-09-12 の
測定では P1 が `01` だったので**セッションで変わる**。値そのものではなく
**非ゼロかどうか**だけを見ること。

**2026-09-12 の測定にはバレッタで決定した記録が無く**、`char_chosen` が
`$3BD or $3E1` を見る形は**未検証の仮定のまま入っていた**。今回それが正しいことが
確かめられた。

### 押しが通る状態は「自由」ではない — `$06` 別の実測 (2026-09-19)

着地歩の最終押しの瞬間の `$06` と、直後にダッシュが成立したかを 477 回分。

| 押した瞬間の `$06` | 成立 | 意味 |
|---|---|---|
| `0x00` 自由 | **94 / 95** | |
| `0x06` ジャンプ・着地 | **282 / 283** | **自由でなくても通る** |
| `0x02` やられ | 0 / 24 | 通らない |
| `0x14` ダッシュ | **7 / 75** | **ほぼ通らない** |

**`$06 == 0` を「押しが通る条件」に使ってはいけない。** `0x06` は非ゼロだが
282/283 で通る。2026-09-19 に解除条件を「`$05`/`$06` が両方 0」に変えて
サスカッチのダッシュを遅らせたのは、この数字を知らずに推測したため。

**通らないのは `0x14`(前のダッシュの中)。** キャラ差が出る:

| キャラ | 最終押しの `$06` |
|---|---|
| サスカッチ (`0A`) | `00` 1/1、`06` 38/39。**`14` で押すことが一度も無い** |
| `02` / `07` (ガロン・フェリシアとみられる) | `14` で 75 回中 68 回が無駄打ち |

**ダッシュでキャンセルできる着地モーション (サスカッチ) と、できないもの
(ガロン・フェリシア) の差**と一致する。ただし `$06` はその可否そのものではなく
**代理指標**。可否を直接読むなら `air_ready` が見ているようなセルのフラグ
(`cel + 1` のビット) を探すことになる。

### コマンド受付の猶予 — 外部資料 (本人提供、2026-09-19)

**実測ではなく資料由来。**このファイルの他の項目と出所が違うので分けて書く。
1 フレーム = 1 ティック (ノーマル換算、本人が根拠ありと明言)。

**必殺技コマンドは操作間の猶予が乱数。**

| 猶予 | 確率 |
|---|---|
| 10F | 16/32 |
| 11F | 4/32 |
| 12F | 4/32 |
| 13F | 3/32 |
| 14F | 3/32 |
| 15F | 2/32 |

**ダッシュは乱数ではなく固定。ノーマル 10F、ターボ 8F。**

```
N → 前 (10F 継続できる) → ニュートラル (10F の猶予) → 前 で成立
最速  前→N→前 の 3F      最長  21F 目
```

ダッシュは必殺技ではないので扱いが特殊で、**1 回目のレバー方向がニュートラルから
10F しか受け付けない**。「数秒間右に歩く → N → 右」でダッシュが出ないのはこれ。

**この数字が効く場面。** 着地予測で撃った配送がヒットストップに遭うと、凍結中も
受付カウンタは減る (本人の経験則)。入力列 `{} {forward} {} {forward}` の 3 番目が
ニュートラル区間にあたり、**既に 1 ティック使っているので伸ばせるのは 9 ティック
(ターボで 7)**。**実測のヒットストップは 11 ティック (`$5C` = `0x0B`) なので、
ニュートラル区間に丸ごと重なればダッシュコマンドは切れる。**最終押しを着地まで
保持するだけでは足りず、頭から入れ直す必要がある場合がある。

### サスカッチのダッシュは足が浮く — `$38` はダッシュの結果

ダッシュが始まったティックで `$38` が 0 → 1 になります (2026-09-11、3 本とも)。

```
ダッシュ開始 t= 27  $38 は 0 -> 1
ダッシュ開始 t=249  $38 は 0 -> 1
ダッシュ開始 t=167  $38 は 0 -> 1
```

**同じティックに 2 つ起きるので、並べる順番は「ダッシュ → 空中」です。**
足を浮かせているのはダッシュのほうで、`$38` はその結果です。

### 発生 1 フレームの投げは、技の開始と掴みが同じティック

ビクトルの一回転投げ (Mega Spike) と二回転投げ (Gerdenheim 3、EX なので `$06` は
`0x12`) の実測 (2026-09-11):

```
t=171  自 $06=0E $105=01  相手 $05=06    ← 同じ行
t=179  自 $06=0E $105=01  相手 $05=06    ← 同じ行
t=109  自 $06=12 $105=01  相手 $05=06    ← 同じ行
```

サスカッチのビッグブランチは技の開始が 1 ティック先行します (`t=163` 開始、
`t=164` 掴み) が、**ビクトルの投げは同じティック**です。「掴んでいる間は技を
読まない」という規則を**今のティック**で判定すると、投げた技が自分の投げに
消されます。**前のティックで判定すること。**

### 投げの形 (実測、サスカッチのビッグブランチ 3 回)

2026-09-11 に初めて本物の投げがログに入りました。それまでの 7 セッションには
1 件もありませんでした。

```
t=163  自 $05=00 $06=0E $105=01     ビッグブランチ開始 (必殺技なので 0x0E)
t=164  相手 $05=06                  掴んだ
t=239  相手 $05=02                  離した。やられへ移る
       自 $06 は 0x0E のまま         ← 離したあとも投げの動作は続いている
t= 19  自 $06=04                    自由になる直前
t= 20  自 $06=00                    動ける。ここから追撃が入る
t= 27  自 $06=14 $38=01             ダッシュ (サスカッチなので空中)
t= 36  自 $105=01                   ダッシュ攻撃
```

**投げた側の `$06` は投げの間ずっと `0x0E` で、相手を離しても戻りません。**
「投げが終わった」と言えるのは **相手が `0x06` を抜け、かつ自分が `$05`/`$06`
とも 0 になった**ときです。片方だけではまだ投げの中にいます。

⚠ **相手の `$05` が 1 ティックだけ `0x06` になる記録は投げではありません。**
2026-09-10 までのログに 9 件あり、すべてラウンドの切り替わりでした (直後に自分の
`$06` が 02 04 06 08 0A と規則的に進む)。本物の投げは 50 ティック以上続きます。

### `$1B8` は「攻撃を始めた回数」— 同じ技の 2 本目はここで分かる

技の名前もボタンも変わらない連打キャンセルで、**新しい発生が始まったこと**を
言える値です。ROM が 1 を足す場所:

```
0274D6 / 02751A   通常技の開始 (2 経路)
027818 / 027870 / 0278A6 / 0278DA / 027A00
028F18            チェーンと連打キャンセルが合流する尾
```

`0x028F18` があるので、**ゲームが受理した連打だけが数を動かします。**押しても
受理されなかった入力では動きません。

⚠ **`0x028F18` は空中では実行されません。**手前の `0x028F0E` が `$38` を見て、
非 0 なら `0x028F34` (rts) へ飛びます。

```
028F08: move.b #$2, ($7,A6)
028F0E: tst.b  ($38,A6)
028F12: bne    $28f34        ; 空中ならここで終わり
028F14: clr.b  ($7,A6)
028F18: addq.w #1, ($1b8,A6)
```

**空中のチェーンでは `$1B8` は増えません。**サスカッチのダッシュは 1 ティック目
から空中なので、ダッシュ小 P → 中 P も増えません。連打キャンセル自体は
`0x028FB0` が `$38` を見て地上限定なので、そちらへの影響はありません。

空中でも `0x028ED4` は通るため、**`$102` と `$101` は必ず新しい技のものに
書き換わります。**空中の 2 本目はこちらで見分けます。

意味は `0x00D348` が決めています。`$1B6` (当たった数) を `$1B8` で割って表を引き、
そのあと両方を `clr.w` します — ラウンド終了時の命中率です。つまり `$1B8` は
**その ラウンドで出した攻撃の本数**で、フラグではありません。

**自然な多段技とアニメーションのループでは動きません。**どちらも開始ルーチンへ
入り直さないためで、アニメーションの巻き戻りを見るより確実です。

#### 実測 (2026-09-10、ザベルの立ち小 P)

普通に 2 回当てたあと、連打キャンセルを 3 回。`$1B8` は 8 回増え、**増えた回数と
技を出した回数がちょうど一致**しました。余計な加算はありません。

| # | tick | `$11A` | |
|---:|---:|---|---|
| 1 | 663 | 00 | 単発 |
| 2 | 847 | 00 | 単発 |
| 3 | 987 | 00 | 連打 1 本目 |
| 4 | 1003 | **FF** | 連打 2 本目 (16t 後) |
| 5 | 1113 | 00 | 連打 1 本目 |
| 6 | 1129 | **FF** | 連打 2 本目 (16t 後) |
| 7 | 1220 | 00 | 連打 1 本目 |
| 8 | 1237 | **FF** | 連打 2 本目 (17t 後) |

`$119` はセッション中ずっと `00` でした (チェーンはしていないので当然)。
**`$11A` が立つのは連打として受理された 3 回だけ**で、`$1B8` の加算と同じティック
です。

このログを `analysis/replay_route.lua` で三行目に流し直すと、単発は
`1t LP > 5t Hit`、連打は `1t LP > 5t Hit > 17t LP > 21t Hit` になりました。

**ヒットストップ中は連打キャンセルの判定自体が走りません。**`0x022552` が `$5C`
を最初に見て、非 0 なら 1 減らして `0x0225D0` へ飛び、`0x0225C4` の `$06`
ディスパッチを丸ごと飛ばします。

チェーンがヒットストップ中の入力で成立するのは、`0x028E76` が読むのが `$12E`
(ラッチされたボタンワード) だからです。連打キャンセルは `$126` しか読まず、
`$126` は 1 ティックの押しエッジ (`0x0221B4`、`$122 AND NOT $124`) なので、
**ヒットストップ中に押した分は捨てられます。**
(実測: ザベルのしゃがみ小 P。空振りでは出るがヒットでは出ない、という形で出た)

チェーン側の次技マスクは `0x028F36` の分岐表:

```
小 → $6600 (MP HP MK HK)、$101 == 0 なら +bit12 (LK)
中 → $4400 (HP HK)、      $101 == 0 なら +bit13 (MK)
大 → 0、                  $101 == 0 なら $4000 (HK)
```

順序は LP < LK < MP < MK < HP < HK。

---

## 10. アニメーション

```
0x0BCF7A   キャラ別のアニメーションテーブル(long)
```

そこから**ワード単位のオフセット**でアニメ ID を引きます。セルは **24 バイト**。

```
+0x00  持続ティック
+0x01  フラグ  bit0 = このセルで連打キャンセルを受け付ける
                bit6 = 最終セル(st ($21,A6))
                bit7 = +0x18 のポインタへジャンプ
+0x18  ジャンプ先
```

ステッパは `0x027F70`。`$20` を毎ティック減らし、0 になったら次のセルを読みます。

---

## 11. ダッシュ

`$06 = 0x14` がダッシュ状態そのものです。攻撃が出たかどうかは `$105`(攻撃フラグ)
で見ます。

**`$06` がダッシュ攻撃中も `0x14` のまま、とは限りません。**ザベルで 16 本
測った結果 (2026-09-08、`analysis/dashProbe.lua`)、判定 id `0x17` の技は
`0x14` のまま、`0x1E` の技は攻撃が始まるティックに `0x06` へ抜けます。
**同じティックに抜ける**ので、「今ダッシュ状態か」で境目を判定することは
できません。

```
027982: move.l #$2001400, ($4,A6)   ; ダッシュ状態へ
```

* ダッシュは **タップ → 中立 → タップ**。2 回目のタップは **free+0** でなければ成立しません。
* 1 回目のタップは位置に余裕があります。実測で **free−10 まで**成立します。
* **中断は逆方向。**認識するタイミングは 1F のみですが、押しっぱなしで問題ありません
  (攻略 wiki サスカッチ)。
* **デミトリは中断不可**(攻略 wiki 基本動作)。
* ザベルはダッシュが攻撃フラグを立てるため、ダッシュ攻撃の差分が常に 0 になります。
* 同じ理由で、Tick Data のダッシュ攻撃はダッシュの助走を発生に含んで出ていました
  (2026-09-08 報告)。`$105` の区間がダッシュから始まっているためです。**まだ箱が
  出ていないあいだに限り**、`$06 = 0x14` かつ技 id (`$106`+`$102`+`$101`) が
  変わったところへ測定の起点を移すようにしました。箱が出たあとは動かさないので、
  チェーンを割ることはありません (割ると接触が拾えなくなる。下記)。
* **技 id は `$106`+`$102`+`$101` では足りません。**それで組んだ最初の版は何も
  変わりませんでした (2026-09-08 実測)。ダッシュ状態の中で技ごとに動くと**実測**
  されているのは `$10B` と `$1B7` の 2 つ (上の滑空チェーンの表)。今は 5 バイト
  全部を並べて、どれかが動いたら別の技として扱っています。
  ザベルのダッシュでも同じかは未確認 — 確かめるには `analysis/dashProbe.lua`。
* **`$10B` は「技が始まった」という意味ではありません。**16 本のダッシュ攻撃では
  攻撃が始まるティックに 3 ずつ増えたので、そう読んで実装しましたが、**後の実測で
  崩れました** (2026-09-09)。ザベルのダッシュ小 P では:
  * **空振りだと一度も動きません。**
  * **当てると、判定が出た 2 ティック後**に動きます (ヒットのリアクション)。

  意味は「アニメーションスクリプトの新しい項目に入った」で、それが技の開始に当たる
  技と、ヒットのリアクションに当たる技があります。**技の開始の合図には使えません。**
  16 本の観測が一致していたのは、**16 本とも当てていた**ためと考えられます。
  `$1B7` は動かない回があり、`$102`/`$101` は技によっては動きます。`$106` はこの
  状態では一度も動きませんでした。

* **技の開始はセルポインタが自分のスクリプトを離れたティックです** (2026-09-09、
  空振りのダッシュ小 P 3 本で実測)。アニメーションのステッパ `0x027F70` は

  ```
  027F70: subq.b  #1, ($20,A6)     残りティックを 1 減らす
  027F74: bne     $27f9e           0 でなければ何もしない
  027F96: lea     ($18,A0), A0     通常の進行 = 次のセル = +0x18 ちょうど
  027F8E: movea.l ($18,A0), A0     ループ = セルに書かれたアドレスへ
  ```

  **セルレコードは 0x18 バイト固定**で、ステッパが自力で動かすときは必ず `+0x18`。
  それ以外の `$1C` の変化は、ステッパの外が差し替えたということ = 別の技。実測:

  ```
  +0  cel=00166E0E            ダッシュのアニメ
  +3  cel=00166E26  +0x18     同じアニメが 1 コマ
  +5  cel=00166E3E  +0x18
  +7  cel=00167E6C  +0x102e   <<< 技の開始
  +9  cel=00167E84  +0x18     判定 (box=16)
  ```

  3 本とも同じ形で、**空振りでも当てても同じように出ます**。`$10B` が持っていた
  弱点がありません。

* **体の状態が変わるとアニメも差し替わる。**実測 (ザベルの空中ダッシュ攻撃、
  2026-09-09) では、技の前に 3 回セルが飛んでいます。

  ```
  +0   cel +0x244    ジャンプ開始 ($06 が 0x06 へ)
  +2   cel +0x1e0    離陸 ($38 が 1 へ)
  +30  cel +0x1ad0   空中ダッシュ ($06 が 0x14 へ)
  +43  cel +0x13b6   ← ここが技
  ```

  **技の開始として使えるのは最後の 1 つだけ。**前の 3 つを除かないと、技マークが
  起点で使い切られて何も測れません。除くのは「ダッシュに入ったティック」「`$38`
  が変わったティック (離陸と着地の両方)」「道筋の 1 ティック目」。

* **着地モーションの中でもアニメは変わります** (`$06` が 0x06 → 0x08 → 0x00 と
  歩く)。足が着いたあとの飛びは、そのジャンプの技ではありません。

* **probe は 4 ティックに 1 回取りこぼします。**`emu.registerbefore` は描画
  フレームごとで、ツール本体は `rawStateService` の ticker (turbo の 4 つ目の
  描かれないティックも見る) で回っています。ログでは `+0x18` が 2 つ重なって
  `+0x30` の 1 回に見えることがあり、そのままだと飛びと誤読します。
  `analysis/replay_route.lua` は取りこぼしを埋め直してから流します。

* **ループを技の開始と誤読しうる。**上の `movea.l ($18,A0), A0` はアニメの先頭へ
  戻るもので、これも `+0x18` 以外の変化になります。実測した 3 本は 19〜23t と
  短くループが起きていません。**長いダッシュや長いジャンプでは未確認。**使う側で
  「まだ箱が出ていない間の最初の 1 回だけ」に絞ってあります。

* **技が自分の判定より後に始まることはない。**`$10B` を使う場所には「まだ箱が
  出ていない」条件を必ず付けること。付けないと、当てたときに錨がヒットまで歩き、
  発生が 1 ティックと出ます。
* **1 本だけ、技が出たあとにもう一度 `$10B` が動いています** (run 12、`$1B7` も
  02 → 03)。追いかけると発生が 1 になるので、**最初の 1 回だけ**を境目として
  採ります。
* 実測した発生は、判定 id `0x1E` の技で 3〜5t、`0x17` の技で 5〜7t。フラグの
  立ち上がりから数えると 10〜16t で、差がダッシュの助走ぶんです。

### 滑空(空中ダッシュ)からの連携中、オブジェクトは静止している

ジェダの滑空 → 前+HK → LK → LP を、1 ティック 1 行で 5 本記録した結果
(`airChainProbe.lua`)。

```
滑空成立の直後から最後の技まで、状態はこう固定されます

$05 = 0     $06 = 0x14 (ダッシュ状態)   $07 = 6
$38 = 1     $105 = 1                    $140 = 0
```

* **`$06` は `0x14` のまま変わりません。**滑空は「ダッシュ状態」で、そこから出る技は
  ダッシュ攻撃だからです(§11 と同じ話)。
* **`$105` は滑空が立てた 1 のまま**で、技ごとに落ちません。ザベルのダッシュと同型。
* **技と技の間で、`$000..$3FF` に変化するバイトはありません。**動くのは座標、
  アニメーションカウンタ、そして注入した入力の写し(`$122/$124/$126/$12A/$1AC/$396`)
  だけです。

意味のある値は 2 つ見つかりましたが、どちらも**区間内で不変**なので「出せる」の
印にはなりません。

| 番地 | 内容 |
|---|---|
| `$1B7` | チェーン段数。1 技目の区間で 1、2 技目で 2 |
| `$10B` | 技 id。区間ごとに 17 / 23 |

### ダッシュ攻撃の「最速」は、測る原点で 1 ティック違う

同じ瞬間が、経路によって 12 と 11 の 2 通りに表されます。

```
アーム経路 (Reversal - Specified + Delay: Auto)
    kd_press_base() = 1        ダッシュは SPECIAL_MOTION に無い
  + DASH_AUTO_TICKS = 12       モリガン前ダッシュ
  → press_defer pc=13          ログの実測値と一致

シーケンスの 2 歩目以降
    11                         ダッシュ自身の入力が終わった時点が起点
```

**runner の待ちは 1 ティック後ろから数える**ので、表の値から 1 引いたものが同じ
瞬間になります。実測(モリガン、12 本)で 1 ティックの誤差もなく一致:

```
P@197 P@198   ダッシュのタップ
s@209 (MK)    198 から +11。手書きの +11t と同一の位置
```

`actionSequenceRunner.lua` の `M.compile` が、**シーケンスの 2 歩目以降で直前が
ダッシュの `Auto`** をこの値に解決します。表は読むだけで、`guardCancel.lua` 側
(`dash_attack_ticks()` / `kd_delay_ticks()` / `kd_press_offset()`)は**一切変更して
いません**。`Reversal - Specified` も、シーケンスの 1 歩目も、12 のままです。

> **アームの `Delay: Auto` へ融合する実装は失敗しました。**ログ上、機構は設計
> どおり動いています(`arm_edge len=3` / `press_defer pc=13` / `press_now` で
> `forward+MK` が入る)のに技が出ない。**原点が違うものを同じ数で置いたため**です。

### ⚠ シーケンスの 1 歩目と 2 歩目以降は、別の機構が出しています

これで 3 回踏みました。新しくステップの属性を足すときは、**必ず両方に配線が
要るか確認**すること。

```
1 歩目      guardCancel のアーム経路。free-1 の署名でトリガに合わせて置く。
            受け取るのは「入力リスト」だけ
2 歩目以降  actionSequenceRunner の service_body。ティック時計で置き、
            配信レコードに属性を付けられる
```

踏んだ 3 件:

| 症状 | 原因 |
|---|---|
| ダッシュ攻撃の `Auto` が 1 ティックずれる | 原点が 1 違う。表の値から 1 を引いて解決 |
| 2 歩目以降の押しが free+1 に落ちる | 先頭ニュートラルの `lead` を `Auto` が引けない |
| **1 歩目の `Hold` が効かない** | **アーム経路は入力リストしか受け取らない** |

3 件目は `M.arm` が hold を預け、ティックウォーカーが「何も配信していない最初の
ティック」で一度だけ受け取る形にしました。**アームの時点で押し付けてはいけません** —
リバーサル本体を配信している最中に方向を主張すると、free-1 の機構と入力語
(`0xFF8B94`)を取り合います。

### 空中で技が出せる条件 — ROM から確定

**推測をやめてコードを読んだ結果、実測から当てた規則は「意味が違う」ものでした。**

#### 攻撃の入口 `0x0274BA`

```
0274BA: tst.b   ($15d,A5)        ; 全体の禁止フラグ
0274BE: bne     $274ca
0274C0: move.b  ($126,A6), D0    ; 押しエッジ(上位バイト = ボタン)
0274C4: andi.w  #$77, D0         ; 0x77 = 6 ボタン
0274C8: rts                      ; 非 0 なら攻撃入力あり
0274CE: move.l  #$2000a00, ($4,A6)   ; $06 = 0x0A = 通常技へ
```

#### 空中/滑空からの条件 `0x0277BE`

```
0277D2: tst.w   ($44,A6)         ; 落下速度
0277D6: bmi     $274ba           ; 上昇中なら高さ不問で攻撃判定へ
0277DA: move.w  ($14,A6), D0
0277DE: sub.w   ($3a,A6), D0     ; 高さ = Y − 地面
0277E2: move.b  ($382,A6), D1    ; キャラ id
0277EA: move.w  (A0,D1.w), D1    ; キャラ別の最低高度(表 $be1ba / $be1fa)
0277EE: cmp.w   D1, D0
0277F0: bcc     $274ba           ; 高さが足りれば出せる
0277F4: moveq   #$0, D0          ; 足りなければ出せない
```

**つまり滑空から技を出す条件は「高さ」です。**`$115` が立っていれば別の表(`$be1fa`)を使います。

#### 同じ技は 1 回だけ — `$1a6`

```
0277F8: bsr     $274ba           ; 攻撃入力あり?
0277FE: bsr     $2759a           ; どのボタンか
027808: btst    D1, ($1a6,A6)    ; この滞空で既に使ったか
02780C: bne     $277f4           ; 使用済み → 出せない
02780E: moveq   #$1, D0
```

**`$1a6` は「この滞空で使った技」のビットマスク**で、ボタンのビット配置と同じです。
実測(ジェダ 滑空→HK→LK→LP)と完全に一致:

```
HK  bit6  → $1a6 = 0x40
LK  bit4  → $1a6 = 0x50
LP  bit0  → $1a6 = 0x51
```

チェーンコンボではなく**「1 滞空につき各ボタン 1 回、アニメーションと高さが許す限り」**。
ヒットさせるとヒットストップで落下が遅くなり、その分だけ回数が増えます。

#### 投げ判定の id は RAM に無い

攻撃判定はアニメーションデータで、ステッパが毎ティック オブジェクトへ写すので
読めます(`セル + 0x0A`、0 なら判定なし)。**投げ判定は違います。**id は
キャラ自身のスクリプト内の即値です。

```
040776 の直前: 7068              moveq  #$68, D0     ; 投げ判定 id
040776:        4EB9 0002 947E    jsr    $2947e.l
```

したがって「このティックに投げ判定が出ているか」を示す RAM は存在せず、
検査そのものを見るしかありません。

```
0x029406   投げの間合い判定。A6 = 投げる側、D0 = 判定 id
           0x02947E が bsr で入り、キャラ側から直接呼ばれる経路もある
0x029450   その内部。cps2-hitboxes.lua はここにブレークポイントを置いている
           (ただし registerexec の行はコメントアウトされていて未登録)
0x029638   遠距離投げ
```

判定内容は、相手 (A1 = `($32,A6)`) の `$4` が `0x200`/`0x204`、`$143`/`$38`/
`$147`/`$11E` がいずれも 0、`$94` に食らい判定があること。そのうえで自分の
`$8C` テーブルの投げ判定と、相手の押し判定 (`$97`/`$90`) を `0x19172` で重ねます。

> **この検査を投げの持続の測定に使うことはできません。**`0x029406` を
> registerexec でフックして P1 のティックを数えたところ、ミッドナイトブリスで
> 1 ティックしか取れませんでした(表の持続は 29)。**掴んだ瞬間に 1 回走るだけ**
> で、判定が出ているあいだ毎ティック呼ばれてはいません。取れるのは「掴んだ
> tick」であって持続ではない、ということです(2026-09-07 に実測して確認)。
>
> 同じ測定で `Startup 267` / `Total 323` (表は 12 / 57) も出ました。超必の
> 暗転と変身演出のあいだ `$105` が立ちっぱなしで、表が数えていない時間を
> 含みます。**超必を通常技と同じ土俵で測るには、暗転を除く手段が別途要ります。**

### チェーン中の 2 発目は「やられに入った」では拾えない

`$05` が `0x02` に**入る**エッジは、相手が既にやられ中だと二度と立ちません。
チェーンの 2 発目以降は相手が `0x02` のままなので、接触の検出にこのエッジを
使うと**当たっているのに空振り扱い**になります。

これは Tick Data で実際に踏みました。技 ID (`$106` + `$102` + `$101`) が
変わったら測り直す、という実装を入れたところ、チェーンの 2 発目が独立した
測定になり、その測定からは接触が見えず `whiff` で終わりました
(2026-09-07 実測、しゃがみ小P → しゃがみ小K)。

**チェーンを技ごとに分けて測るなら、接触の検出をエッジ以外にする必要が
あります。**ガード時は `$158` (ブロッククロック) の増加で拾えていますが、
ヒット時に相当するものは未特定です。

### `$21` はセル番号ではなくビットマスク

```
022BBC: tst.b   ($21,A6)
022BC0: bmi     $22be2           ; 負 = アニメーション終了
022BC2: btst    #$0, ($21,A6)
022BC8: bne     $22bce
022BCA: bra     $27f70           ; bit0 が無ければ何もできない
022BD6: btst    #$1, ($21,A6)
022BDC: bne     $22bf6           ; bit1 → $27122 = 方向キーの判定(移動)
```

**bit1 は移動の許可であって、攻撃ではありません。**実測から当てた「bit1 の次の
ティック」は**タイミングがたまたま合っていただけ**で、だから「正しく直す」と壊れました。
攻撃判定(`$274ba`)は `$21` のテストより手前で呼ばれます。

#### ティック内の順序

```
0x02211A  ツールのフック(入力注入)
0x0221B4  $126 = 押しエッジ = $122 AND NOT $124
0x0222AC  $04 で状態ディスパッチ → $05 → $06 → キャラ別
$27f70    アニメーション更新($21 はこの後)
```

**フックで読んだ `$21` は、そのティックの判定が使う値そのもの**です。

#### 状態ディスパッチの解き方

```
$04  0x0222C0 + word[0x0222C0 + $04]        0x02 → 0x02239E
$05  0x02245A + word[0x02245A + $05]        0x00 → 0x022552
$06  0x0225EE + word[0x0225EE + $06]        0x14 → 0x023244 (ダッシュ)
                                            0x0A → 0x022FC0 (通常技)
キャラ別  long[0xBD37A + $382*4]             ジェダ(0x0F) → 0x0525C4
$07  0x0525D0 + word[0x0525D0 + $07]        0x06 → 0x052860
$135 0x05286C + word[0x05286C + $135]       0x04 → 0x0528E8 → jsr $277be
```

> **データテーブルは listing から読めません。**`mame_code/` は復号後のオペコード
> 列なので、データ領域の hex は壊れています(`0xBD37A` が `0x808A08C9` に見える)。
> **ROM の zip から直接読むこと。**`vsav.zip` の `vm3e.03d`(0x000000)/`vm3e.04d`
> (0x080000)/`vm3.05a`(0x100000)… で、**ワード内のバイト順が逆**(`ROM_LOAD16_WORD_SWAP`)。
> 入れ替えて読むと 18/18 が妥当なコードアドレスになります。

### 空中の Auto は ROM どおりの判定で最速に乗っている

`actionSequenceRunner.lua` の `dummy_free()` は、上の 2 条件をそのまま実装しています
(アニメーションフレームの許可ビット + `$1a6` の未使用)。**`$21` は読みません。**

実測(ジェダ 滑空→HK→LK→LP、9 本):

```
HK → LK   25   手で詰めた最速と一致
LK → LP   24   同じ
```

**`$21` を使った 3 つの実装はすべて失敗しました。**`$21` はこのフラグの写しで、
フレーム本体と 1 ティックずれています。合わせようとするほど壊れました。
写しではなく `($1c,A6)` を読めばラッチも要りません。

### 空中ダッシュ — ジャンプ入力から何ティックか

実測(ザベル、前ジャンプ直後の最速空中ダッシュ)。
**ダッシュの最後のタップが着地するティック**で数えます。

```
+8   出る
+7   出ない
```

つまり踏切のせいで **+8 より早くは成立しません**。これはキャラごとに違う可能性が
あるので、他キャラは同じやり方(シーケンスの Wait を 1 ずつ下げて出なくなる点を探す)
で測ってください。

**測った値は `guardCancel.lua` の `AIR_DASH_TICKS` に 1 行足します。**
`DASH_AUTO_TICKS` の隣にあり、同じ規則で動きます — **行の無いキャラには数字を
出さず、`Auto` は状態判定のまま**。他人の踏切を借りません。

**空中ダッシュを持つ 4 キャラ、18 通りすべて実測済みです。**
`guardCancel.lua` の `AIR_DASH_TICKS` に、ジャンプの向きとダッシュの向きの
「組」ごとに入っています。

```
                ダッシュ / 攻撃
              前→前  後→後  後→前  前→後  垂直→前 垂直→後
ザベル        8 / 0   8 / 0   4 / 4   4 / 4   4 / 4   4 / 4
Q-Bee         6 / 0    -      6 / 0    -      4 / 0    -
レイレイ      9 / 5   9 / 5   6 / 8   6 / 8   6 / 8   6 / 8
ジェダ        7 / 6    -      4 / 9    -      4 / 9    -
```

「-」は後ろ空中ダッシュを持たないキャラ。**キャラごとでも、ダッシュの向きごとでも
なく、組ごとに違います。**

### 「和がキャラごとに一定」は反証済み

`dash + atk` がキャラごとに一定になる、という観察が 3 キャラで一致しました。

```
レイレイ   14    6 通りすべて
ジェダ     13    3 通りすべて
ザベル      8    測った 2 通り
```

**Q-Bee が反証しました。**垂直→前は `dash 4 / atk 0` で和が 4、前→前は
`dash 6 / atk 0` で和が 6。一定ではありません。実測です。

これでこの表に対する一般化は**4 回試して 4 回とも失敗**しています。

```
「空中の攻撃は 0 固定」        レイレイの 5 で崩れた
「踏切はジャンプの性質」        ザベルの交差 4 で崩れた
「同じ向き同士だけ遅い」        Q-Bee の後→前 6 で崩れた
「和がキャラごとに一定」        Q-Bee の垂直→前 0 で崩れた
```

**組ごとに測って入れる、以外にありません。**

### `Auto (Recovered)` は攻撃の後だけ正しい

`Auto` の状態判定は「ダミーがやっていたことを終えた」で、**直前が攻撃なら
それが最速**です(ジェダの空中チェーンで 25/24 と実測、空中ダッシュ→攻撃→
攻撃の 3 歩目でも確認)。

**ジャンプ・ダッシュ・空中ダッシュの後は最速ではありません。**まだ弧の途中、
ダッシュの途中で、「終わった」は次を入れられるティックよりずっと後です。

そこで表に組の実測が無いときは `Auto (Recovered)` と言わず、

```
Auto (Not Measured)
```

と出します。最速だと約束してしまうより、誰も測っていないと言うほうが正確で、
やることも決まります。

### ⚠ ダッシュの入力自体が 4 ティック要ります

`{} {forward} {} {forward}` の 4 エントリなので、**Wait を 0〜4 のどれにしても
最後のタップは +4 に落ちます**(`service_body` が `wait - lead` から始めるため)。

```
wait=0  lead=4  →  最後のタップ +4
wait=4  lead=4  →  最後のタップ +4     すべて同じ
wait=5  lead=4  →  +5
```

**4 より下は測れません。**「0 で出た」は「4 で出た」と同じことなので、
記録すべき値は 4 です。

ジャンプのステップに `Hold` は要りません(実測)。入力が入れば軌道はゲームが
運ぶので、方向を握り続ける必要はありません。

### 空中ダッシュ・スーパージャンプを持つキャラ

```
空中ダッシュ 前    ザベル / Q-Bee / レイレイ / ジェダ
空中ダッシュ 後ろ  ザベル / レイレイ
スーパージャンプ   モリガン / リリス
```

**この 2 つは重なりません。**スーパージャンプを持つ 2 キャラに空中ダッシュは
無いので、「スーパージャンプから空中ダッシュ」という並びはどのキャラでも
作れず、その組み合わせを測る必要もありません。

エディタの `Air Dash` はこれで絞ってあります(`AIR_DASH_CHARS` /
`AIR_BACK_CHARS`)。**持たないキャラには分類ごと出しません** — 出せない行は
無い行より悪く、組んだシーケンスが正しく見えて走って何も起きず、タイミングの
間違いと区別が付かないからです。

Zabel 2 (`0x0B`) と Oboro (`0x18`) は別形態で、未確認なので入れていません。

エディタでは `Auto (8)` と表示され、`Air Dash` という分類のアクションになります。
**入力は地上ダッシュと同一**で、分けているのは `Auto` の解決先が違うからです
(地上ダッシュの次は `DASH_AUTO_TICKS` の値 − 1、空中ダッシュの次は 0)。

確認済みの並び(ザベル、最低空ダッシュ → 下大 P):

```
1  Auto (Fastest)  Jump Forward
2  Auto (8)        Air Dash Forward
3  Auto (0)        Attack Down + HP
```

**押しっぱなしの方向にはエッジが立ちません。**前ジャンプは前を握ったまま終わるので、
続けて前ダッシュを入れると 1 回目のタップが存在しないことになります。バックダッシュや
しゃがみからのスーパージャンプと同じ話で、**直前にニュートラルを 1 ティック挟む**必要が
あります(`actionSequenceRunner.lua` の `opens_with_direction`)。

---

## 12. ステージの座標とカメラ

`$10` は世界座標の X(16.16 の上位語)です。

### 書いた位置は補正されません

外から `$10` に書いた値は**ゲームに補正されません。**壁を越えて書けばキャラは
ステージ外へ出て画面から消えます。移動させるときは自前でクランプが要ります。

### キャラはカメラの枠に閉じ込められています

ゲームは 2 人を画面内に保つため、**片方が右にいると相手はそれ以上左へ行けません。**
その結果:

* **キャラ単独ではステージ端に到達できません。**壁を測るには 2 人とも同じ隅へ
  追い込む必要があります。
* 枠外へテレポートさせると**画面端まで引き戻されます。**カメラは自分から
  追いつきません。

### 壁は全ステージ共通

2 ステージで実測して同じ値でした。**ステージ別のテーブルは要りません。**

```
左壁 280    右壁 1000    幅 720
```

ラウンド開始位置は `554 / 728`(間隔 174)。その中点 641 は、壁から出る中央 640
と一致します(独立した 2 つの測定が揃っています)。

### カメラは 0xFF8280 のオブジェクトです

カメラはキャラや背景と同じ構造のオブジェクトで、基点は `0xFF8280`。

| 番地 | 中身 |
|---|---|
| **`0xFF82A0`** | **カメラ X の実体。ここに書くと画面が即座に動きます** |
| `0xFF8290` | 公開コピー(`A6 + 0x10`)。毎フレーム `PC 0x01E4FC` が実体から書き直すので**単独で書いても効きません**が、**キャラを画面内に閉じ込める処理はこちらを先に読みます** |
| `0xFF82A8` | 描画の基準点。カメラと常に一致しますが、書いても画面は動きません |
| `0xFF8390` | 別オブジェクト(`0xFF8380`)の `$10`。同じくカメラと一致 |
| `0xFF8294` | カメラ Y |
| `0xFF82D0` | 速度に相当。足すと画面が無限にスクロールします |

### ⚠ カメラは 1 フレーム 16px までしか動かせません

`0x01E466` のコードが答えです。

```asm
01E466: move.w D0, ($20,A6)      ; 新しいカメラ X
01E470: andi.b #$10, D0          ; bit4 だけ取り出す
01E474: move.b ($38,A6), D1      ; 前回の bit4
01E478: eor.b  D1, D0
01E47A: bne    ...               ; 変化なしなら何もしない
01E482: move.b ($3b,A6), D0      ; スクロール方向
01E486: jsr    ...               ; 列を 1 本だけ描く
```

**背景の列は「カメラ X の bit4 が反転したとき」＝ 16px 進むごとに 1 本だけ描かれます。**
16px はタイル 1 枚。判定が距離ではなく**ビットの反転**なので、1 フレームに
何 px 飛ばしても描かれるのは**最大 1 本**です。

384px 飛ばせば 24 本必要なところ 1 本しか描かれず、**残りは永久に描かれません**
(取りこぼしを記録する仕組みが無いため、待っても歩き直しても直りません)。

`positionModule.camera_restore_start()` はこの上限を守り、カメラと 2 人を
一緒に 16px/frame で運びます。全幅 384px で **24 フレーム**です。

関連する番地:

| 番地 | 中身 |
|---|---|
| `0xFF82A0` | カメラ X の本体(`$20,A6`) |
| `0xFF82A4` | カメラ Y の本体(`$24,A6`) |
| `0xFF82A8` | **前フレームのカメラ X**(`$28,A6`)。`0x01E52E` で毎フレーム更新 |
| `0xFF82B8` | 前回の bit4(`$38,A6`) |
| `0xFF82BB` | スクロール方向(`$3b,A6`)。8 か 0x0C |
| `0xFF8026` | ハード用スクロール値。`(カメラ + $58,A6) & 0x3FF` |

### カメラに書くのは 2 語

| 番地 | 誰が読むか |
|---|---|
| **`0xFF82A0`** | カメラ本体。**画面を動かすのはこれ** |
| **`0xFF8290`** | 公開コピー。**キャラを画面内に閉じ込める処理がこちらを読みます** |

**両方書きます。**枠の判定は公開コピーを読み、カメラの再構築はその後に走るので、
片方だけでは必ずどちらかがずれます。

| 書いたもの | 起きること |
|---|---|
| `FF8290` だけ | 画面は動かないが、**枠だけ動いて見えない壁ができる** |
| `FF82A0` だけ | 画面は動くが**枠が古いまま**で、置いたキャラが旧窓へ引き戻される(旧左端 + 壁の内寄せ 24px の位置へ。実測 664px) |
| **両方** | 位置もカメラも正しく決まる |

`cps2-hitboxes.lua` の vsav プロファイルが `screen_left = 0xFF8290` として
最初から持っていました。`screen_x = world_x - 0xFF8290` です。

### カメラと同じ値を持つ語は 6 つある。しかし増やしても意味はない

歩行 228 フレーム・両端を含む照合で、**カメラとの差が一定の語**は 6 つでした。

| 番地 | 差 |
|---|---|
| `FF8026` `FF8290` `FF82A0` `FF82A8` `FF8390` | 0 |
| `FF804E` | **−256**(ステージ左端からの相対位置。カメラと一度も等しくならないので等値検索では見つかりません) |

⚠ **これらを全部書いても背景の破れは直りません。**2 語でも 6 語でも破損率は
同じ(実測 26% と 24%)でした。**破れの原因は書く語ではなく 16px の上限**です。

### ⚠ 方向で壊れ方が違って見えるのは錯覚です

調査中、**左向きのジャンプは無傷、右向きは破れる**という観測が出て、これを
「方向依存」と誤解して数サイクルを空費しました。**そうではありません。**

左向きが無傷だったのは、テストが**直前まで居た場所へ戻っていた**からです。
その範囲のタイルはまだ描かれたままなので、飛んでも破れません。**保持範囲の外へ
飛べば方向に関係なく破れます。**

実測（背景アニメの画素を除外し、ジャンプなしの対照でノイズ床 2% を確認済み）:

| 飛距離 | 破損率 |
|---|---|
| 16 | 2%(＝ノイズ床。**タイル 1 枚ぶんなので正しく描かれる**) |
| 32 / 48 / 64 / 96 / 128 | 4 / 4 / 4 / 3 / 2% |
| **192 / 256 / 384** | **10 / 8 / 10%** |

16px は 1 本で足りる唯一の距離です。32px 以上は必要な本数に足りず、
**距離が伸びるほど取りこぼしが増えます。**

### カメラの動き方

```
target = clamp( (P1x + P2x) / 2 - 192, 256, 640 )
```

192 は画面幅 384 の半分。カメラはこの目標を**1 tick あたり 5px の上限で追う**
だけです(3 表示フレーム = 4 tick なので、表示上は `10, 5, 5` の繰り返しに
見えます)。イージングではなく速度上限なので、距離が開いても速くなりません。

**ステージ全幅 384px のズレは 77 tick ≒ 58 表示フレーム、約 1 秒。**キャラを
瞬間移動させると、この間ずっと画面が追いついていない状態になります。
`positionModule.camera_write()` はこれを飛ばすためのものです。

⚠ **256〜640 の外を書くと、存在しないタイルを描いて背景が壊れます。**
`camera_write()` は必ずこの範囲に収めます。

### 0xFFB810 群・0xFFD410 群は背景レイヤです

どちらも 0x80 刻みで並ぶ**別オブジェクトの `$10`** で、カメラではありません。
書いても画面は動きません。0xFFD410 群は視差つきで中点を追うため相関が高く出て
紛らわしいのですが、出力側です。

---

## 13. CPS2 の注意

**`mame_code` などの逆アセンブル結果は命令空間のものです。**CPS2 は命令フェッチ
のみ復号されるため、**データテーブルは逆アセンブル上ではゴミになります。**
`0x28D50` のような表の中身は、実機のメモリから読む必要があります。

判断材料にする前に、必ずどちらの空間の話か確認してください。

### 着地の予測は当たる。ずれるのはヒットストップの分だけ

`guardCancel.lua` の `ticks_to_landing()` は y / vy / ay と床 (`$3A`) から弾道を
解く。**物理計算そのものは正確**で、生の標本では `pred` が毎ティックちょうど
1 ずつ減る (2026-09-18 実測)。

```
t=165 pred=36 left=92  y=40 vy=245760 ay=-13312
t=166 pred=35 left=91  y=43 vy=232448 ay=-13312
t=167 pred=34 left=90  y=46 vy=219136 ay=-13312
```

**ずれるのは位置更新が止まる間。** ヒットストップ中は y も vy も ay も凍るのに
ティックカウンタだけ進む。`ticks_to_landing()` は物理の時間で答え、待ち時間は
時計の時間で測られるので、その差がそのまま誤差になる。

```
t=178 pred=23 left=79  y=70 vy=72704 ay=-13312
t=179 pred=23 left=78  y=70 vy=72704 ay=-13312   ← 全部同じ
t=180 pred=23 left=77  y=70 vy=72704 ay=-13312
```

**測定 (P1 サスカッチ、ダッシュキャンセル小技の連発):**

| 補正 | 結果 |
|---|---|
| 補正なし | `-11:187 -10:30 -9:30 … -1:30` の階段 |
| `$5C` をその場で足す | 階段が消える。`-11` が残る (予測の後で始まった凍結) |
| 実際に**動かなかった**ティックを足す | **`+0` が約 88%**。`-1` の山も消える |

**`$5C` は「凍るはずの理由」であって「凍った証拠」ではない。** 殴った側にも立つ
ので、立っていても物理が動いていることがある。数えるなら **y と vy が前ティック
と同じかどうか**を見る。

**残る約 8% (`+11` / `+16`) は凍結では説明できない。** 予測より**早く**着地した
もので、別の原因 (軌道の変化、`$38` が落ちる時点の定義) が要調査。

**対照:** 殴っていない側 (P2 の素のジャンプ) は補正なしで `+0` のみ。凍結が
起きないので当然で、これが弾道計算の正しさの裏付けになっている。

**実装の指針:** 未来の凍結を予測する必要はない。**止まっているティックは数えない**
— 待つ側も物理の時間で数えれば、`$5C` を足すより単純で観測に忠実になる。

**ヒットストップ中でもコマンド受付カウンタは減る** (本人の経験則、2026-09-18)。
物理は凍るが入力処理は走っており、測定でも裏が取れている: 凍結中も
`0x0221CC` のフックは毎ティック発火する (そこは入力処理ルーチンの中)。

**実装上の意味:** 入力列の配送を凍結中に止めると、その間に窓が減って切れる。
止めても続けても駄目で、**凍結中は撃たない / 撃った後に凍ったら捨てて撃ち直す**
という形になる (`design_landing_prediction.md`)。
